#pragma once

// Owned by plan 03-05. Header-only. Skeleton created in 03-01 for build wiring only.

#include "AnalysisResult.h"
#include <array>

std::array<float, 12> buildTemplate (int root, ChordQuality quality);
