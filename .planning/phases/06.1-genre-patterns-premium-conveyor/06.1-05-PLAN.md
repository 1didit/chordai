---
phase: 06.1-genre-patterns-premium-conveyor
plan: 05
type: execute
wave: 3
depends_on: ["06.1-03", "06.1-04"]
files_modified:
  - Source/MidiGen/MidiRowBuilder.h
  - Source/MidiGen/MidiRowBuilder.cpp
  - Source/MidiGen/AsIsRowGenerator.h
  - Source/MidiGen/AsIsRowGenerator.cpp
  - Source/MidiGen/StyleVoicingGenerators.h
  - Source/MidiGen/StyleVoicingGenerators.cpp
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
  - CMakeLists.txt
  - Tests/MidiRowBuilderGenreTests.cpp
  - Tests/GenreStateTests.cpp
  - Tests/PluginProcessorGenreTests.cpp
  - Tests/MidiRowBuilderTests.cpp
  - Tests/StyleVoicingTests.cpp
  - Tests/PluginProcessorMidiGenTests.cpp
autonomous: true
requirements: [GEN-09, GEN-10, GEN-11]

must_haves:
  truths:
    - "After analysis completes, the processor publishes 5 rows of the ACTIVE genre (generateGenreRows), in the same broadcast as the analysis result — rows and timeline can never desync"
    - "processor.regenerateRow(index) changes ONLY the target row, deterministically (same click sequence on two processors = byte-identical rows); out-of-range index is a safe no-op"
    - "processor.setActiveGenre(id) rebuilds all 5 rows for the same AnalysisResult without re-analyzing; distinct genres produce distinct row content"
    - "Active genre + main-5 live as GenreState ValueTree props on apvts.state (Phase 7 persistence-ready); unknown/corrupt genre id falls back safely to 'trap', never crashes"
    - "Phase 5's fixed generators (generateAllRows/AsIsRowGenerator/StyleVoicingGenerators) are retired; their tests migrated, not blindly deleted"
  artifacts:
    - path: "Source/MidiGen/MidiRowBuilder.h"
      provides: "generateGenreRows(result, genre, settings) + regenerateRow(result, genre, patternIndex, variationCounter, settings)"
    - path: "Source/PluginProcessor.h"
      provides: "setActiveGenre/setMainGenres/regenerateRow/getActiveGenreId/getMainGenreIds + patternVariationCounters"
    - path: "Tests/PluginProcessorGenreTests.cpp"
      provides: "RegenerateChangesOnlyTargetRow, RegenerateIsDeterministicGivenSameCounter, RegenerateOutOfRangeIndexIsNoOp, GenreSwitchRegeneratesDistinctRows, SetMainGenresValidatesAndPersists, UnknownActiveGenreIdFallsBackSafely"
    - path: "Tests/GenreStateTests.cpp"
      provides: "ValueTree round-trip incl. corrupt-state fallback (mirrors RegionStateTests)"
  key_links:
    - from: "Source/PluginProcessor.cpp (triggerAnalysis onDone)"
      to: "Source/MidiGen/MidiRowBuilder.h"
      via: "generateGenreRows(*result, *genre) replaces generateAllRows(*result), same atomic_store + broadcast ordering"
      pattern: "generateGenreRows"
    - from: "Source/PluginProcessor.cpp (regenerateRow)"
      to: "midiSetRows"
      via: "splices ONE row into a COPY of the current vector, publishes via atomic_store + analysisBroadcaster (Pitfall D: same full setRows path, no partial-update shortcut)"
      pattern: "atomic_store.*midiSetRows"
    - from: "Source/PluginProcessor.cpp (ctor + setActiveGenre/setMainGenres)"
      to: "Source/MidiGen/GenreState.h"
      via: "GenreState::readActiveGenre/readMainGenres on construct, GenreState::write on change (RegionState precedent)"
      pattern: "GenreState::"
---

<objective>
Wire the genre engine into the product: MidiRowBuilder gains generateGenreRows/regenerateRow over PatternEngine + GenreRegistry, PluginProcessor gains the genre API (active genre, main-5 with GenreState persistence-ready storage, per-row deterministic regenerate), and Phase 5's four fixed generator functions are retired with their tests migrated. After this plan the full GEN-09/GEN-11 behavior is real at the processor level — 06.1-06 only has to put buttons on it.

Purpose: This is where "5 patterns of the selected genre fitting the song's motif" and the per-row randomizer become end-to-end behavior.
Output: Reworked MidiRowBuilder, extended PluginProcessor, retired legacy generators, 3 new test files, updated CMake, full suite + pluginval wave gate green.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-genre-patterns-premium-conveyor/06.1-RESEARCH.md
@Source/MidiGen/MidiRowBuilder.h
@Source/MidiGen/MidiRowBuilder.cpp
@Source/MidiGen/PatternEngine.h
@Source/MidiGen/GenreRegistry.h
@Source/MidiGen/PatternSeed.h
@Source/MidiGen/GenreState.h
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Tests/PluginProcessorMidiGenTests.cpp
@Tests/RegionStateTests.cpp
@Tests/MidiGenFixtures.h

<interfaces>
Processor idioms this plan MUST reuse verbatim (verified against Source/PluginProcessor.cpp):

```cpp
// Publication idiom (onDone, line ~219-234): atomic_store BEFORE sendChangeMessage:
std::atomic_store (&analysisResult, result);
auto rows = std::make_shared<const std::vector<MidiSetRow>> (
    result != nullptr ? generateAllRows (*result) : std::vector<MidiSetRow>{});   // <- REPLACE this call site
std::atomic_store (&midiSetRows, rows);
// ... analyzingFlag/analysisProgress updates ...
analysisBroadcaster.sendChangeMessage();

// Members: midiSetRows accessed ONLY via std::atomic_load/atomic_store (Apple libc++ rule).
// selectedRegion/auditionRowId are message-thread-only members — patternVariationCounters,
// activeGenreId, mainGenreIds follow the same discipline.
// apvts.state is the ValueTree GenreState writes to (RegionState::write precedent in loadAudioFile/setSelectedRegion).
```

New MidiRowBuilder API (research Pattern 4):
```cpp
std::vector<MidiSetRow> generateGenreRows (const AnalysisResult&, const GenreSpec&, const GenerationSettings& = {});
// Builds the row for patternIndex ONLY; caller owns + pre-bumps the variation counter.
MidiSetRow regenerateRow (const AnalysisResult&, const GenreSpec&, int patternIndex, uint32_t variationCounter, const GenerationSettings& = {});
```

Row identity: id = "<genreId>-<slotSlug>" with slot slugs {"sustained", "rhythmic", "stab-arp", "bass", "top-line"};
label = "<GenreLabel> — <Slot Label>" with slot labels {"Sustained Chords", "Rhythmic Chords", "Stab / Arp", "Bass Line", "Top Line"};
kind = (PatternKind) i; patternIndex = i. MidiFileWriter::suggestedFileName uses row.id — new ids flow through untouched.

Test idioms to copy from Tests/PluginProcessorMidiGenTests.cpp: file-local writeToneWavFixture()/pumpUntil(predicate, timeoutMs)
+ RecordingListener ChangeListener snapshots. Two-processor determinism-comparison shape from MidiRowBuilderTests'
SameInputProducesByteIdenticalRows (migrate it, don't lose it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MidiRowBuilder generateGenreRows + regenerateRow (alongside old API, build stays green)</name>
  <files>Source/MidiGen/MidiRowBuilder.h, Source/MidiGen/MidiRowBuilder.cpp, Tests/MidiRowBuilderGenreTests.cpp</files>
  <behavior>
MidiRowBuilderGenreTests (new file, populated from the 06.1-01 skeleton; use makeFourChordFixture + findGenre("trap")):
- FiveRowsForKnownGenreAndProgression: generateGenreRows returns exactly 5 rows; rows[i].patternIndex == i; rows[i].kind == (PatternKind) i; ids == {"trap-sustained","trap-rhythmic","trap-stab-arp","trap-bass","trap-top-line"}; labels start with "Trap — "; every non-bass row non-empty for the 4-chord fixture.
- SameInputProducesByteIdenticalRows: two calls -> element-wise identical (migrates the retired MidiRowBuilderTests determinism test to the new API).
- NoChordSegmentEmitsNoNotesAcrossAllSlots: makeNoChordFixture() -> 5 rows, all notes vectors empty.
- DistinctGenresProduceDistinctRows: trap vs house on the same fixture -> at least 3 of 5 rows differ in note content.
- RegenerateRowMatchesCounterSemantics: regenerateRow(result, genre, 2, 0) == generateGenreRows(result, genre)[2] (counter 0 == baseline); regenerateRow(..., 2, 1) differs from counter 0 (trap's stab-arp slot has a 2-variant rhythm pool — variation material exists); regenerateRow is deterministic per counter value.
- RegenerateRowPreservesIdentity: regenerated row keeps id/label/kind/patternIndex of the slot.
  </behavior>
  <action>
Implement in MidiRowBuilder.h/.cpp ALONGSIDE the existing generateAllRows (deleted in Task 2 — each task leaves the build green):

generateGenreRows: for i in 0..4: seed = combineSeedWithVariation(computeBaseSeed(result, genre.id, i), 0); notes = generatePattern(result, genre.patterns[i], seed); assemble MidiSetRow with the id/label/kind/patternIndex convention from the interfaces block.

regenerateRow: same assembly for ONE patternIndex with the caller-supplied variationCounter; jassert + clamp-guard patternIndex to 0..4 (defensive; processor also guards).

Write tests first (RED), implement (GREEN).
Commit: `feat(06.1-05): generateGenreRows + regenerateRow over the pattern engine`
  </action>
  <verify>
    <automated>cd /Users/test/Documents/prjcts/chordai && cmake --build build && ctest --test-dir build -R "ChordAITests.MidiRowBuilderGenre" --output-on-failure</automated>
  </verify>
  <done>New API green including determinism + counter semantics; old API still present and untouched; full build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Processor genre API + GenreState wiring + legacy retirement</name>
  <files>Source/PluginProcessor.h, Source/PluginProcessor.cpp, Source/MidiGen/MidiRowBuilder.h, Source/MidiGen/MidiRowBuilder.cpp, Source/MidiGen/AsIsRowGenerator.h, Source/MidiGen/AsIsRowGenerator.cpp, Source/MidiGen/StyleVoicingGenerators.h, Source/MidiGen/StyleVoicingGenerators.cpp, CMakeLists.txt, Tests/GenreStateTests.cpp, Tests/MidiRowBuilderTests.cpp, Tests/StyleVoicingTests.cpp</files>
  <behavior>
GenreStateTests (mirrors Tests/RegionStateTests.cpp — read it first):
- RoundTripsThroughValueTreeXml: write("uk-drill", five ids) -> toXmlString -> fromXml -> readActiveGenre/readMainGenres return exactly what was written.
- MissingPropsReturnFallbacks: fresh tree -> readActiveGenre(state, "trap") == "trap"; readMainGenres == fallback array.
- CorruptMainGenresFallsBack: hand-set mainGenreIds to "a,b,c" (3 tokens) -> readMainGenres returns the fallback (never a wrong-sized array).
  </behavior>
  <action>
1. **PluginProcessor.h** additions (research Pattern 6 — copy its member/API shape):
```cpp
void setActiveGenre (const juce::String& genreId);   // full 5-row rebuild, counters reset, GenreState::write, broadcast
void setMainGenres (juce::StringArray exactlyFive);   // validates size==5 + all known; GenreState::write; broadcast; NO row regen
void regenerateRow (int patternIndex);                // 0-4; bump counter, splice one row, publish
juce::String getActiveGenreId() const;                // message-thread only
juce::StringArray getMainGenreIds() const;
// private: std::array<uint32_t, 5> patternVariationCounters {}; juce::String activeGenreId; juce::StringArray mainGenreIds;
```

2. **PluginProcessor.cpp**:
   - Ctor: after apvts init — `activeGenreId = GenreState::readActiveGenre (apvts.state, "trap"); mainGenreIds = GenreState::readMainGenres (apvts.state, kDefaultMainGenreIds);` (editor-reopen/state-restore defense, RegionState precedent).
   - `triggerAnalysis()` onDone: replace the `generateAllRows (*result)` call site with the research Pattern 6 shape — reset `patternVariationCounters = {};`, `auto* genre = findGenre (activeGenreId); if (genre == nullptr) genre = findGenre ("trap");` (Pitfall F — NEVER dereference findGenre unchecked), then `generateGenreRows (*result, *genre)`. Everything else (ordering, broadcast) byte-for-byte unchanged.
   - `setActiveGenre`: no-op if id unknown (findGenre nullptr) or == current; else set member, `GenreState::write (apvts.state, activeGenreId, mainGenreIds)`, reset counters, rebuild all 5 rows from getAnalysisResult() if non-null (empty vector publish if null), atomic_store + `analysisBroadcaster.sendChangeMessage()`. NO ThreadPool job — generation is sub-millisecond (06.1-03 budget test).
   - `setMainGenres`: defensive validate (size == 5, every id resolves via findGenre, no duplicates) else no-op; set member + GenreState::write + broadcast. If activeGenreId is NOT in the new five (FIFO eviction removed it), internally call setActiveGenre(newFive[4]) — the newest-added genre becomes active (documented discretion call).
   - `regenerateRow (int patternIndex)`: research Pattern 6 code — guard 0..4 else return; guard null result/genre/rows-size-!=5 else return; `++patternVariationCounters[i]` BEFORE the call (Pitfall B: counter must actually increment); splice into a COPY of the current vector; atomic_store + broadcast. NO partial-view-update path exists or is added (Pitfall D).
   - `setStateInformation`: after the existing state replacement, re-read GenreState into the two members (same fallbacks) so a DAW project reload lands coherent — Phase 7 verifies, this plan just doesn't strand the cache.

3. **Retire legacy** (research Pattern 10): delete AsIsRowGenerator.h/.cpp, StyleVoicingGenerators.h/.cpp, generateAllRows (decl + body + its transitional mapping comment). CMakeLists.txt: remove the two retired .cpp from CHORDAI_MIDIGEN_SOURCES; remove Tests/StyleVoicingTests.cpp and Tests/MidiRowBuilderTests.cpp from ChordAITests target_sources and DELETE both files — their surviving value was migrated: determinism test -> MidiRowBuilderGenreTests (Task 1); voicing-interval correctness -> ChordToneMapperTests (06.1-01) + PatternEngineTests (06.1-03); house-stab data -> GenreRegistryTests sweep (06.1-04). EXPLICIT GAP CLOSURE (plan-checker finding): StyleVoicingTests.RnbVoiceLeadingMinimizesMovement is the only test asserting voice-leading MINIMIZES total movement (not merely register-clamps) — before deleting it, port it as `PatternEngineTests.VoiceLeadingMinimizesMovementAcrossTransition` into Tests/PatternEngineTests.cpp (same assertion shape: total semitone movement across a chord transition under the smooth-voice-leading archetype is <= the naive re-anchor baseline), exercising VoiceLeadingEngine::nearestOctaveNote through the pattern engine. State this migration mapping (including the ported test) in the commit body.
   - Keep BassLineGeneratorTests.cpp + ChordToneMapperTests.cpp untouched (subjects kept).

4. Write GenreStateTests per <behavior> (test-first where practical).

Commit: `feat(06.1-05): processor genre API + GenreState; retire fixed Phase 5 generators`
  </action>
  <verify>
    <automated>cd /Users/test/Documents/prjcts/chordai && cmake --build build && ctest --test-dir build -R "ChordAITests.(GenreState|MidiRowBuilderGenre)" --output-on-failure && grep -rL "generateAllRows" Source/MidiGen/MidiRowBuilder.h</automated>
  </verify>
  <done>Processor exposes the full genre API; GenreState round-trips; generateAllRows/AsIsRowGenerator/StyleVoicingGenerators gone from the tree; build green with updated CMake.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Processor integration tests + full wave gate</name>
  <files>Tests/PluginProcessorGenreTests.cpp, Tests/PluginProcessorMidiGenTests.cpp</files>
  <behavior>
PluginProcessorGenreTests (populate the 06.1-01 skeleton; copy writeToneWavFixture/pumpUntil/RecordingListener from PluginProcessorMidiGenTests verbatim — file-local, per that file's own precedent):
- GenreSwitchRegeneratesDistinctRows: load fixture -> analysis completes -> capture rows; setActiveGenre("house") -> broadcast fires, 5 new rows with "house-" ids, content differs from trap rows; analysisResult pointer UNCHANGED (no re-analysis).
- RegenerateChangesOnlyTargetRow: capture rows; regenerateRow(2); new vector: index 2 notes differ, indices 0,1,3,4 byte-identical to before.
- RegenerateIsDeterministicGivenSameCounter: two independent processors, same fixture, same call sequence (regenerateRow(0) x3, regenerateRow(2) x1) -> byte-identical final row vectors (extends Phase 5 Pitfall 2's two-processor shape).
- RegenerateOutOfRangeIndexIsNoOp: regenerateRow(-1) and regenerateRow(5) -> rows pointer unchanged, no broadcast, no crash.
- RegenerateBeforeAnalysisIsNoOp: fresh processor, regenerateRow(0) -> no crash, rows still null/empty.
- SetMainGenresValidatesAndPersists: setMainGenres with 4 ids -> ignored (getMainGenreIds unchanged); with 5 valid ids -> getter reflects it AND apvts.state property "mainGenreIds" == comma-joined string; getStateInformation/setStateInformation round-trip onto a second processor restores both genre props.
- UnknownActiveGenreIdFallsBackSafely: hand-corrupt apvts.state ("activeGenreId" = "future-genre") BEFORE load -> full load+analysis completes with 5 valid non-empty rows (trap fallback), no crash (Pitfall F).
  </behavior>
  <action>
1. Write PluginProcessorGenreTests per <behavior>.
2. Update Tests/PluginProcessorMidiGenTests.cpp minimally: its three existing tests (RowsPublishedSynchronouslyWithAnalysisResult, RowsRegenerateOnRegionChange, FreshLoadClearsRows) are behavior-generic — fix only assertions that reference old row ids/styles/count semantics if any exist (read the file; expected: row count 5 still holds, ids now "trap-*"). Do NOT rewrite passing behavior tests.
3. Full wave gate: full suite + pluginval strictness 5 on BOTH formats.

Commit: `test(06.1-05): processor genre integration suite + wave gate`
  </action>
  <verify>
    <automated>cd /Users/test/Documents/prjcts/chordai && cmake --build build && ctest --test-dir build --output-on-failure && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3" && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component"</automated>
  </verify>
  <done>All 7 integration behaviors green; legacy tests migrated; FULL suite green; pluginval strictness 5 VST3+AU SUCCESS — GEN-09/GEN-11 processor-level behavior fully evidenced.</done>
</task>

</tasks>

<verification>
- `grep -rn "generateAllRows\|AsIsRowGenerator\|StyleVoicingGenerators" Source/ Tests/ CMakeLists.txt` returns nothing.
- `grep -n "findGenre" Source/PluginProcessor.cpp` — every call site null-checks before dereference (Pitfall F).
- regenerateRow publishes through atomic_store + analysisBroadcaster only — `grep -n "updateSingleRow\|updateRow" Source/` returns nothing (Pitfall D).
- Full suite + pluginval x2 green.
</verification>

<success_criteria>
- Processor-level GEN-09 (genre rows fit the detected progression, switchable without re-analysis), GEN-11 (per-row deterministic regenerate, harmony preserved upstream by construction), and the GEN-10 state half (main-5 validated + persisted via GenreState) all proven by automated tests.
- Phase 5 legacy fully retired with documented test migration.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-genre-patterns-premium-conveyor/06.1-05-SUMMARY.md`
</output>
