---
phase: 06.1-genre-patterns-premium-conveyor
plan: 07
type: execute
wave: 5
depends_on: ["06.1-02", "06.1-05", "06.1-06"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [GEN-09, GEN-10, GEN-11, UI-01]

must_haves:
  truths:
    - "Fresh Release build + full suite + pluginval strictness 5 (VST3+AU) are green at phase close"
    - "On a real track, all 5 main genres produce audibly genre-authentic, distinct pattern sets; regenerate audibly varies a row while the chords stay the same (human-confirmed by ear)"
    - "The genre menu keeps exactly 5 checked with chips updating live; the conveyor idle/animate states and premium look pass human review"
    - "GEN-09/GEN-10/GEN-11/UI-01 are checked complete in REQUIREMENTS.md only after the checkpoint passes"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "GEN-09/GEN-10/GEN-11/UI-01 marked [x] with Phase 6.1 traceability"
      contains: "GEN-09"
  key_links:
    - from: "build-release/ChordAI_artefacts/Release/Standalone/ChordAI.app"
      to: "human checkpoint"
      via: "fresh Release binary launched AFTER killing any stale instance (06-04 lesson)"
      pattern: "pkill|kill"
---

<objective>
Close Phase 6.1 with the full gate (fresh Release build, full suite, pluginval strictness 5 VST3+AU) and the human checkpoint the whole phase exists for: genre authenticity BY EAR on a real track (the user's stated top priority), per-genre regenerate variation, the exactly-5 genre menu UX, and the premium conveyor look. Musical judgment, pixel-art quality, and menu feel have no automated tooling (06.1-VALIDATION.md's four justified manual-only rows).

Purpose: Final evidence for GEN-09/GEN-10/GEN-11/UI-01; the tuning review gate for every research MEDIUM-confidence genre-data default.
Output: Green gate, approved checkpoint, REQUIREMENTS.md updated, phase complete.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06.1-genre-patterns-premium-conveyor/06.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase gate — fresh Release build, full suite, pluginval both formats</name>
  <files>(no source changes — gate only)</files>
  <action>
1. `cmake --build build` — Debug targets + tests current.
2. Full suite: `ctest --test-dir build --output-on-failure` — every test green (pre-existing + all Phase 6.1 suites).
3. pluginval strictness 5, VST3 then AU (COPY_PLUGIN_AFTER_BUILD keeps the installed copies fresh).
4. Fresh Release build: `cmake --build build-release` (configure first if the directory is missing: `cmake -B build-release -DCMAKE_BUILD_TYPE=Release`). Confirm the Standalone .app artefact exists and note its build timestamp (`ls -l` the binary) — the checkpoint MUST run this exact fresh binary.

If ANY step fails: fix forward within this plan only if the fix is small and obvious (test-data tune, missing include); otherwise report the failure honestly and stop — never present a checkpoint on a red gate.
  </action>
  <verify>
    <automated>cd /Users/test/Documents/prjcts/chordai && cmake --build build && ctest --test-dir build --output-on-failure && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3" && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component" && cmake --build build-release && ls build-release/ChordAI_artefacts/Release/Standalone/ChordAI.app</automated>
  </verify>
  <done>Full suite green, pluginval strictness 5 SUCCESS on VST3 and AU, fresh Release Standalone built with a current timestamp.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human checkpoint — genre authenticity by ear, menu UX, premium conveyor</name>
  <what-built>
Phase 6.1 complete build: genre-driven generation (10-genre library, 5 patterns per genre fitting the detected progression), per-row deterministic regenerate, genre chips + exactly-5 selector menu, and the premium idle-stopped conveyor — all on top of the unchanged Phase 6 audition/drag/save mechanics.
  </what-built>
  <how-to-verify>
ВАЖЛИВО ПЕРЕД СТАРТОМ (урок 06-04): спочатку вбити будь-який запущений ChordAI, інакше macOS `open` лише сфокусує старий процес зі старим білдом:
1. `pkill -x ChordAI || true`, потім `open build-release/ChordAI_artefacts/Release/Standalone/ChordAI.app` (перевір, що процес свіжий).

Конвеєр (UI-01):
2. До завантаження: верхня стрічка ЗУПИНЕНА, текст "drop song or melody here" читається; ролери не крутяться.
3. Перетягни реальний трек (напр. TOCK.mp3) НАД вікном не відпускаючи: стрічка починає рухатись; відпусти — під час аналізу стрічка крутиться швидше з золотим прогресом; після аналізу стрічка знову зупиняється і текст повертається.
4. Оціни якість: ролери обертаються синхронно зі стрічкою, текстура/дизеринг, падіння чанка з відскоком — виглядає преміальніше, ніж було?

Жанрова система (GEN-09/GEN-10):
5. Між таймлайном акордів і (вужчою) хвилею — 5 чипів: TRAP, DRILL, BOOM BAP, R&B, HOUSE + кнопка CHANGE.
6. Клікни кожен з 5 чипів по черзі: панель MIDI щоразу оновлюється на 5 патернів жанру (sustained / rhythmic / stab-arp / bass / top line). Прослухай (play) по 2-3 ряди на жанр: чи звучить автентично? (trap — темний/розріджений, drill — тресільйо/тріолі, boom bap — свінг, r&b — розширені акорди/плавний voice leading, house — оффбіт стаби). Акорди мають слідувати детектованій прогресії треку.
7. Кнопка CHANGE: відкривається піксель-арт меню з 10 жанрами і галочками. Клікни новий жанр (напр. LOFI): найстаріший вилітає, галочок завжди рівно 5, чипи оновились. Закрий (DONE або клік поза панеллю).

Regenerate (GEN-11):
8. На кожному з 5 головних жанрів натисни regenerate (ліва іконка-кубик) хоча б на одному ряді: ряд перемальовується з чутною/видимою варіацією (інший ритм/октава), але ГАРМОНІЯ та сама (ті самі акорди); інші 4 ряди не змінились. Кілька кліків поспіль дають нові варіації.
9. Regenerate під час відтворення ряду: звук зупиняється (авто-стоп), нічого не крашиться.

Регресія (Phase 6 механіка без змін):
10. Play/стоп іконки працюють; перетягни один ряд у DAW piano roll; Save-діалог відкривається з іменем виду "trap-stab-arp_G#m_128bpm.mid".
  </how-to-verify>
  <files>(none — manual verification)</files>
  <action>Present the how-to-verify steps to the user verbatim (INCLUDING the kill-stale-instance step 1 — 06-04 lesson), then STOP and wait for the resume signal. Do not proceed, commit, or write the SUMMARY until the user responds. If the user reports a defect or genre-tuning feedback, fix it in a surgical commit within this checkpoint plan (03-06/02-04/06-04 precedent), re-run the Task 1 gate, and re-present the affected checks — genre-data numeric defaults are planned MEDIUM-confidence values expected to need ear-tuning.</action>
  <verify>Human confirmation of steps 2-10 (manual-only: genre authenticity by ear, exactly-5 menu UX, premium conveyor look, and regression of Phase 6 mechanics cannot be automated — 06.1-VALIDATION.md Manual-Only Verifications)</verify>
  <done>User replied "approved" (or all reported defects fixed and re-approved); GEN-09/GEN-10/GEN-11/UI-01 evidenced end-to-end on the user's real track.</done>
  <resume-signal>Напиши "approved", або опиши проблеми (окремо: звучання жанрів / меню / конвеєр / регресії) — дрібні тюнінги жанрових даних можна виправити в цій же сесії, це заплановані MEDIUM-confidence дефолти</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Mark requirements complete + close the phase</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Only after checkpoint approval:
1. .planning/REQUIREMENTS.md: mark GEN-09, GEN-10, GEN-11, UI-01 as `[x]`; update the Traceability table rows to "Complete".
2. If the checkpoint produced genre-data tuning fixes, ensure they are committed with the full suite re-run green.

Commit: `docs(06.1-07): Phase 6.1 complete — GEN-09/10/11 + UI-01 human-verified`
  </action>
  <verify>
    <automated>cd /Users/test/Documents/prjcts/chordai && grep -c "\[x\] \*\*GEN-09\|\[x\] \*\*GEN-10\|\[x\] \*\*GEN-11\|\[x\] \*\*UI-01" .planning/REQUIREMENTS.md && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <done>All four Phase 6.1 requirements checked complete; suite green; phase closed.</done>
</task>

</tasks>

<verification>
- Task 1 gate green in full (suite + pluginval both formats + fresh Release artefact).
- Human checkpoint approved covering all four manual-only validation rows (genre authenticity, regenerate variation, exactly-5 menu, conveyor states/premium look).
- REQUIREMENTS.md reflects completion.
</verification>

<success_criteria>
- Phase 6.1 closes with the same gate discipline as every prior phase: automated everything first, then one focused human checkpoint on the judgments only a human can make.
- GEN-09/GEN-10/GEN-11/UI-01 fully evidenced end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-genre-patterns-premium-conveyor/06.1-07-SUMMARY.md`
</output>
