---
phase: 06.1-genre-patterns-premium-conveyor
plan: 04
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - Source/MidiGen/GenreRegistry.cpp
  - Tests/GenreRegistryTests.cpp
autonomous: true
requirements: [GEN-09, GEN-10]

must_haves:
  truths:
    - "allGenres() returns exactly 10 genres, each with exactly 5 PatternArchetypes whose patterns[i].kind == (PatternKind) i, unique ids, non-empty labels and shortLabels"
    - "kDefaultMainGenreIds ({trap, uk-drill, boom-bap, rnb-neosoul, house}) is a size-5 subset of allGenres() ids"
    - "Every onset and span in every RhythmVariant of every genre is an exact rational tick against TPQN 960 — no decimal swing anywhere in the data"
    - "The 5 main genres are meaningfully distinct as data: different rhythm pools, tone sets, registers and grooves per the research per-genre table (trap dark/sparse/root-dropped, uk drill tresillo/triplet, boom bap swung 7ths, r&b extended/voice-led, house offbeat stabs)"
  artifacts:
    - path: "Source/MidiGen/GenreRegistry.cpp"
      provides: "10 buildXxxSpec() functions + allGenres() static assembly + findGenre() lookup"
      min_lines: 250
    - path: "Tests/GenreRegistryTests.cpp"
      provides: "RegistryShapeIsWellFormed, AllRhythmPoolOnsetsDivide960Exactly, RegeneratePoolsOfferVariety, DefaultFiveAreRegistered"
  key_links:
    - from: "Source/MidiGen/GenreRegistry.cpp"
      to: "Source/MidiGen/GrooveEngine.h"
      via: "swung rhythm variants built from kSwingLight/kSwingTriplet constants, never decimal literals"
      pattern: "kSwing"
    - from: "Tests/GenreRegistryTests.cpp"
      to: "Source/MidiGen/GenreRegistry.cpp"
      via: "tick-exactness sweep iterates allGenres() -> patterns -> rhythmPool -> onsetsBeats"
      pattern: "allGenres"
---

<objective>
Author the genre library: 10 GenreSpecs × 5 PatternArchetype slots as declarative data (research Pattern 1 + the per-genre spec table), implementing allGenres()/findGenre() over the 06.1-01 skeleton. This is pure data authoring against the frozen types — it runs in parallel with 06.1-03's engine implementation (disjoint files) and is where "maximum attention to MIDI quality" becomes concrete per-genre rhythm/voicing/register/velocity choices.

Purpose: GEN-09's genre authenticity and GEN-10's all-genres menu list both live in this data. Every number here is a tunable product default (research MEDIUM confidence) — the 06.1-07 ear checkpoint is its review gate.
Output: Full GenreRegistry.cpp + GenreRegistryTests.cpp including the phase-critical tick-exactness sweep.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-genre-patterns-premium-conveyor/06.1-RESEARCH.md
@Source/MidiGen/GenreRegistry.h
@Source/MidiGen/GenreRegistry.cpp
@Source/MidiGen/GrooveEngine.h
@Source/MidiGen/ChordToneMapper.h
@Source/MidiGen/BassLineGenerator.h

<interfaces>
Types frozen by 06.1-01 (see GenreRegistry.h for the authoritative shape):
PatternArchetype { kind, toneSet, registerAnchor, registerLow/High, dropRoot, useVoiceLeading,
rhythmPool (vector<RhythmVariant>), octaveOffsetPool (vector<int>), bassRhythmPool (vector<BassRhythm>),
baseVelocity, accentPattern, jitterRange, ornamentProbability }.

Register anchors available from ChordToneMapper.h: kAnchorPopTrap=48, kAnchorRnbSeed=60, kAnchorHouse=72, kAnchorBass=36, kRnbRegisterLow=48, kRnbRegisterHigh=72.
Swing constants from GrooveEngine.h: kSwingStraight=1/2, kSwingLight=7/12, kSwingTriplet=2/3.
Existing house stab offsets (reuse): kHouseStabOffsetsBeats {0.5, 1.5, 2.5, 3.5}, kHouseStabLengthBeats 0.25.
BassRhythm { TrapSustain, RnbRootFifth, HouseFourOnFloor }.

TICK-SAFE onset vocabulary (denominator divides 960 = 2^6*3*5): halves, quarters (0.25, 0.75...),
eighths (0.125), sixteenths (0.0625), thirds (1/3, 2/3), sixths, twelfths (7/12), twenty-fourths (7/24).
Swung 16th positions within a beat at kSwingLight: {0, 7/24, 1/2, 1/2 + 7/24}. At kSwingTriplet: {0, 1/3, 1/2, 1/2 + 1/3}.
noteLengthRatio values: keep to {0.25, 0.3, 0.5, 0.6, 0.75, 0.85, 0.9, 1.0} so ratio × gap stays tick-friendly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registry shape tests + the 5 main genres (full research-table detail)</name>
  <files>Source/MidiGen/GenreRegistry.cpp, Tests/GenreRegistryTests.cpp</files>
  <behavior>
- GenreRegistryTests.RegistryShapeIsWellFormed: allGenres().size() == 10 (this test is written now, asserting 10, and will fail until Task 2 completes the set — during Task 1 assert the 5 built so far via a SECTION or write the full test and mark the count assertion for Task 2; prefer: write the full shape test now, Task 1 verify runs only the per-genre sections that exist. Simplest compliant option: Task 1 asserts >= 5 with a TODO bumped to == 10 in Task 2). For each genre: 5 patterns, patterns[i].kind == (PatternKind) i, id/label/shortLabel non-empty, every rhythmPool non-empty, every accentPattern non-empty, baseVelocity in (0, 1], registerLow < registerHigh.
- GenreRegistryTests.DefaultFiveAreRegistered: kDefaultMainGenreIds.size() == 5 and every id resolves via findGenre() != nullptr.
- GenreRegistryTests.FindGenreUnknownReturnsNullptr: findGenre("no-such-genre") == nullptr (Pitfall F contract).
- GenreRegistryTests.MainGenresAreDataDistinct: for each pair of the 5 main genres, their 5-slot data differs (compare rhythm pools / tone sets / registers — a simple serialized-fingerprint inequality is fine).
  </behavior>
  <action>
Implement in Source/MidiGen/GenreRegistry.cpp: `buildTrapSpec()`, `buildUkDrillSpec()`, `buildBoomBapSpec()`, `buildRnbNeoSoulSpec()`, `buildHouseSpec()` per the research per-genre table (research Code Examples section has the fully worked Trap spec — follow its style for all five). Key content, slot by slot (Sustained / Rhythmic / StabArp / Bass / TopLine):

- **trap** ("Trap", "TRAP"): the research's worked example verbatim — root-dropped triads at kAnchorPopTrap, staccato; half-bar re-strike with {1.0, 0.7} accents; dotted-8th syncopated stabs (SeventhExtension, dropRoot, rhythmPool variants {0, 0.75, 1.5, 2.5} and {0, 1.5, 2.25, 3.0}, noteLengthRatio 0.3); Bass: bassRhythmPool {TrapSustain}, kAnchorBass; TopLine: SingleTopTone at 72, {0, 1.5} sparse, ornamentProbability 0.15.
- **uk-drill** ("UK Drill", "DRILL"): Sustained = SingleTopTone sustained (NOT a block triad — research: drill often has no block chords), anchor 72; Rhythmic = Triad dropRoot minimal stabs on tresillo-like {0, 0.75, 1.25} (1.25 = 5/4 ✓), staccato 0.3; StabArp = SeventhExtension arp on triplet grid {0, 1/3, 2/3, 1, 4/3, 5/3} (fast run feel), noteLengthRatio 0.25; Bass: RootOnly generic path at kAnchorBass with grace-note-pickup variant {0, 3.75} (approximated glide — NO pitch bend, deferred per research) — or bassRhythmPool {TrapSustain} plus a second rhythm variant; pick one and document; TopLine: SingleTopTone 1-2 bar repeating minor motif {0, 0.75, 2.0, 2.75}, ornamentProbability 0.2.
- **boom-bap** ("Boom Bap", "BOOM BAP"): Sustained = SeventhExtension (simple 7ths — but rnbExtensionIntervals gives 9/11ths; acceptable v1 approximation, note it) at kAnchorRnbSeed, long 0.9; Rhythmic = swung 8th re-strike: onsets {0, kSwingLight, 1, 1 + kSwingLight, ...} across a 4-beat span (each 7/12 offset ✓); StabArp = swung 16th ascending arp (swung positions {0, 7/24, 1/2, 1/2+7/24} per beat); Bass = RootOnly with root+octave alternation via octaveOffsetPool {0, 12}, swung onsets; TopLine = SingleTopTone call-response {0.5, 1.0, 2.5, 3.0} swung, ornamentProbability 0.25.
- **rnb-neosoul** ("R&B / Neo-Soul", "R&B"): Sustained = SeventhExtension with useVoiceLeading=true, registerLow/High = kRnbRegisterLow/High, legato 1.0, soft 0.62 velocity (Phase 5's kVelRnb precedent); Rhythmic = gentle syncopated re-strike {0, 2.5} legato, voice-led; StabArp = soft broken-chord arp through extension tones, straight 8ths {0, 0.5, 1.0, 1.5}, 0.85 ratio; Bass: bassRhythmPool {RnbRootFifth}; TopLine = SingleTopTone legato motif, voice-led register 72, ornamentProbability 0.3.
- **house** ("Electronic / House", "HOUSE"): Sustained = Triad pad at kAnchorHouse wide/soft; Rhythmic = syncopated chord hits {0, 1.5, 2.0, 3.5} matching four-on-floor pulse; StabArp = the existing kHouseStabOffsetsBeats {0.5, 1.5, 2.5, 3.5} off-beat stabs (Phase 5 logic absorbed as data — research Pattern 10), noteLengthRatio for 0.25-beat stabs, velocity 0.88 (kVelHouse precedent); Bass: bassRhythmPool {HouseFourOnFloor}; TopLine = rising arp (StabArp-style Up), straight 8ths, bright register 84 clamp-checked.

Every slot gets >= 2 rhythmPool variants where regenerate variety matters most (Rhythmic, StabArp, TopLine at minimum for all 5 main genres) and octaveOffsetPool {0, 12} or {0, -12} where register variety is genre-plausible — GEN-11's variation dimensions need material to select from.

allGenres(): function-local `static const std::vector<GenreSpec>` assembled from the build functions (Task 1: 5 entries; Task 2 appends 5 more). findGenre(): linear search by id, nullptr when absent.

Write tests first (RED against the empty skeleton), then implement (GREEN).
Commit: `feat(06.1-04): genre library — 5 main genres (trap, uk drill, boom bap, r&b, house)`
  </action>
  <verify>
    <automated>cd /Users/test/Documents/prjcts/chordai && cmake --build build && ctest --test-dir build -R "ChordAITests.GenreRegistry" --output-on-failure</automated>
  </verify>
  <done>5 main-genre specs registered and shape-valid; kDefaultMainGenreIds all resolve; unknown id returns nullptr; per-pair data distinctness holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remaining 5 genres + phase-critical tick-exactness sweep</name>
  <files>Source/MidiGen/GenreRegistry.cpp, Tests/GenreRegistryTests.cpp</files>
  <behavior>
- GenreRegistryTests.RegistryShapeIsWellFormed now asserts allGenres().size() == 10 with all per-genre invariants (bump the Task 1 count).
- GenreRegistryTests.AllRhythmPoolOnsetsDivide960Exactly (Pitfall C — THE phase-critical sweep): for every genre, every pattern, every RhythmVariant: every onset in onsetsBeats AND spanBeats satisfy isTickExact(x, 960); onsets are strictly ascending, >= 0 and < spanBeats; spanBeats > 0.
- GenreRegistryTests.UniqueIdsAndUniqueShortLabels: all 10 ids unique; all 10 shortLabels unique and <= 9 characters (must fit a chip).
- GenreRegistryTests.RegeneratePoolsOfferVariety: for each of the 5 MAIN genres, at least 3 of the 5 slots have (rhythmPool.size() >= 2 || octaveOffsetPool.size() >= 2) — regenerate must have something to vary.
  </behavior>
  <action>
Author the remaining 5 specs per the research starter-guidance rows (same build-function style):

- **pop** ("Pop", "POP"): straight grid throughout; Sustained full triad warm register 60; Rhythmic straight-8th re-strike; StabArp rising root-3rd-5th-octave straight 16ths; Bass RootOnly on beats {0, 2}; TopLine stepwise chord-tone melody, ornamentProbability 0.2.
- **lofi** ("Lofi", "LOFI"): SeventhExtension warm/legato/soft (velocity ~0.55); swung (kSwingLight) gentle re-strikes and broken-chord arp; Bass RootOnly sustain with occasional passing-tone feel via ornament-free sparse variants; TopLine slow sparse swung entrances, ornamentProbability 0.3.
- **afrobeats** ("Afrobeats", "AFRO"): straight grid + explicit syncopated onset lists (NOT swing): clave-ish re-strike {0, 0.75, 1.5, 2.5, 3.25} (all quarter-multiples ✓); log-drum-style plucks (RootOnly/PowerChord staccato 0.25-0.3 ratio) with alternating {1.0, 0.7} accents; Bass follows clave onsets {0, 0.75, 2.5} rather than straight sustain; TopLine call-response clave-aligned.
- **reggaeton** ("Reggaeton", "DEMBOW"): tresillo everywhere — {0, 0.75, 1.5} per 2-beat span (spanBeats 2.0); Sustained minor-leaning triad pad; StabArp arpeggiated root-fifth-octave on the tresillo; Bass = root on downbeat + tresillo "and" {0, 0.75, 1.5}; TopLine follows tresillo accents.
- **techno** ("Techno", "TECHNO"): hypnotic/static by design — Sustained PowerChord long pad; Rhythmic pulsing quarter-note re-strike {0,1,2,3} (PowerChord); StabArp repeating root-fifth 8th arp; Bass RootOnly straight quarter pulse; TopLine 2-note repeating phrase {0, 1.75}, ornamentProbability 0.0.

All numeric choices MUST come from the tick-safe vocabulary (see interfaces block). Write the sweep test FIRST — it is the guardrail while authoring; if any authored constant fails the sweep, fix the CONSTANT (never loosen the test).

Append the 5 specs to allGenres(); update RegistryShapeIsWellFormed to == 10.

Commit: `feat(06.1-04): genre library complete — 10 genres + tick-exactness sweep`
  </action>
  <verify>
    <automated>cd /Users/test/Documents/prjcts/chordai && cmake --build build && ctest --test-dir build -R "ChordAITests.(GenreRegistry|GrooveEngine)" --output-on-failure && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <done>10 genres registered; tick-exactness sweep green across every onset/span in the registry; unique ids/shortLabels; variety pools present; full suite green.</done>
</task>

</tasks>

<verification>
- `ctest --test-dir build -R "ChordAITests.GenreRegistry" --output-on-failure` all green.
- `grep -nE "0\.58|0\.54|0\.62[^5]" Source/MidiGen/GenreRegistry.cpp` — spot-check: no production-article decimal swing percentages leaked into onset data (velocities are floats and exempt; onsets must be exact-fraction expressions or tick-safe literals).
- Full suite green.
</verification>

<success_criteria>
- The complete 10-genre × 5-slot library exists as reviewable data, with the 5 main genres carrying full research-table authenticity detail and the other 5 solid starters.
- Every rhythm constant in the registry is provably tick-exact against TPQN 960 (automated sweep, not convention).
- Regenerate has real variation material (pools >= 2) in the slots where it matters.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-genre-patterns-premium-conveyor/06.1-04-SUMMARY.md`
</output>
