---
phase: 03-core-chord-detection-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - THIRD_PARTY_LICENSES.md
  - Source/Analysis/ChordAnalyzer.h
  - Source/Analysis/AnalysisResult.h
  - Source/Analysis/AudioPreprocessing.h
  - Source/Analysis/AudioPreprocessing.cpp
  - Source/Analysis/ConstantQAnalysis.h
  - Source/Analysis/ConstantQAnalysis.cpp
  - Source/Analysis/TuningEstimator.h
  - Source/Analysis/TuningEstimator.cpp
  - Source/Analysis/HarmonicPercussiveFilter.h
  - Source/Analysis/HarmonicPercussiveFilter.cpp
  - Source/Analysis/ChromaExtractor.h
  - Source/Analysis/ChromaExtractor.cpp
  - Source/Analysis/OnsetEnvelope.h
  - Source/Analysis/OnsetEnvelope.cpp
  - Source/Analysis/TempoBeatTracker.h
  - Source/Analysis/TempoBeatTracker.cpp
  - Source/Analysis/KeyDetector.h
  - Source/Analysis/KeyDetector.cpp
  - Source/Analysis/ChordTemplates.h
  - Source/Analysis/ChordDecoder.h
  - Source/Analysis/ChordDecoder.cpp
  - Source/Analysis/ClassicDspChordAnalyzer.h
  - Source/Analysis/ClassicDspChordAnalyzer.cpp
  - Tests/SyntheticFixtures.h
  - Tests/SyntheticFixturesTests.cpp
  - Tests/AudioPreprocessingTests.cpp
  - Tests/ChromaExtractorTests.cpp
  - Tests/TuningEstimatorTests.cpp
  - Tests/TempoBeatTrackerTests.cpp
  - Tests/KeyDetectorTests.cpp
  - Tests/ChordDecoderTests.cpp
  - Tests/ClassicDspChordAnalyzerTests.cpp
autonomous: true
requirements: [ANL-06, ANL-03]

must_haves:
  truths:
    - "Project builds all targets (ChordAI plugin + ChordAITests) with constant-q-cpp compiled in, and all 15 pre-existing tests still pass"
    - "ChordAnalyzer interface and AnalysisResult value types exist, compile, and have zero GUI/ThreadPool dependencies"
    - "A raw CQSpectrogram run on a known sine produces sane, runtime-queried parameters (bins, hop, min frequency) and the streaming flush covers the full input duration"
    - "Synthetic chord-progression and click-track fixtures render in-memory audio verified by self-tests"
    - "Audio preprocessing produces mono 8000 Hz and 11025 Hz sample streams from any decoded buffer/region"
  artifacts:
    - path: "Source/Analysis/ChordAnalyzer.h"
      provides: "Pure-virtual ChordAnalyzer with CancelToken, ProgressCallback, analyse() signature"
      contains: "virtual AnalysisResult analyse"
    - path: "Source/Analysis/AnalysisResult.h"
      provides: "ChordQuality, ChordSymbol, ChordSegment, KeyResult, AnalysisResult value types"
      contains: "struct AnalysisResult"
    - path: "THIRD_PARTY_LICENSES.md"
      provides: "constant-q-cpp license text + pinned commit SHA"
      contains: "constant-q-cpp"
    - path: "Tests/SyntheticFixtures.h"
      provides: "renderChordProgression / renderClickTrack / addPercussiveBursts in-memory fixture generators"
      min_lines: 80
    - path: "Source/Analysis/AudioPreprocessing.cpp"
      provides: "Mono downmix + region extraction + dual WindowedSincInterpolator resample"
      min_lines: 40
  key_links:
    - from: "CMakeLists.txt"
      to: "constant-q-cpp static library"
      via: "FetchContent + explicit source list, linked into ChordAI and ChordAITests"
      pattern: "constant_q_cpp"
    - from: "CMakeLists.txt"
      to: "Source/Analysis/*.cpp"
      via: "target_sources of BOTH targets (ChordAI + ChordAITests)"
      pattern: "Source/Analysis/"
    - from: "Tests/ChromaExtractorTests.cpp"
      to: "CQSpectrogram"
      via: "process() + getRemainingOutput() streaming pattern"
      pattern: "getRemainingOutput"
---

<objective>
Lay the entire build/test foundation for the Phase 3 DSP engine so that all downstream plans can run in parallel without ever touching CMakeLists.txt: frozen interface contracts (ChordAnalyzer/AnalysisResult), module skeletons for every pipeline stage registered in both CMake targets, the constant-q-cpp dependency (MIT) with its license captured, in-memory synthetic audio fixtures, and the dual-rate audio preprocessing stage.

Purpose: ANL-06 requires a swappable, headless `ChordAnalyzer` interface; every other Phase 3 plan needs the fixtures, the CQT dependency, and the build wiring created here. Registering ALL module/test files now (as compilable stubs) is what makes Wave 2/3 plans conflict-free and parallel.
Output: Compiling skeleton of Source/Analysis/, constant-q-cpp wired + licensed, Tests/SyntheticFixtures.h + self-tests, working AudioPreprocessing, all existing tests still green.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-core-chord-detection-engine/03-RESEARCH.md
@.planning/phases/03-core-chord-detection-engine/03-VALIDATION.md
@CMakeLists.txt
@Source/Import/LoadedAudio.h
@Source/Import/AudioFileLoader.h
@Tests/AudioFileLoaderTests.cpp

Working directory: /Users/test/Documents/prjcts/chordai (all commands run from here).
Build commands may take several minutes (new third-party sources) — use 600000 ms Bash timeouts for every `cmake --build` invocation.
Git: plain conventional commit messages only — NO co-author or attribution trailers of any kind.
License gate: NO GPL/AGPL code or code ports anywhere in this phase. constant-q-cpp (MIT-style) is the ONLY new third-party dependency.

<interfaces>
Existing conventions to match (extracted from codebase):

From Source/Import/LoadedAudio.h — immutable value-struct style:
```cpp
struct LoadedAudio
{
    juce::File sourceFile;
    juce::AudioBuffer<float> buffer;
    double sampleRate = 0.0;
    double lengthSeconds = 0.0;
};
```

From Source/Import/AudioFileLoader.h — error convention: "Returns nullptr on an unsupported/corrupt file — never throws, never asserts on bad input."

From Tests/AudioFileLoaderTests.cpp — test convention:
```cpp
TEST_CASE ("AudioFileLoaderTests.WavDecode", "[audiofileloader]") { ... }
```
Phase 3 tests use the same `ModuleTests.CaseName` naming but the tag `[chordanalysis]` (per 03-VALIDATION.md quick-run filter).

From CMakeLists.txt — Catch2 v3.7.1 FetchContent already present; `catch_discover_tests(ChordAITests TEST_PREFIX "ChordAITests.")`; test sources listed explicitly in `target_sources(ChordAITests PRIVATE ...)`. Neither target currently links `juce::juce_dsp` — this plan must add it to BOTH targets (needed for `juce::dsp::FFT` / `WindowingFunction` in the onset path).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frozen analysis contracts + module skeletons + CMake registration</name>
  <files>Source/Analysis/ChordAnalyzer.h, Source/Analysis/AnalysisResult.h, Source/Analysis/AudioPreprocessing.h, Source/Analysis/AudioPreprocessing.cpp, Source/Analysis/ConstantQAnalysis.h, Source/Analysis/ConstantQAnalysis.cpp, Source/Analysis/TuningEstimator.h, Source/Analysis/TuningEstimator.cpp, Source/Analysis/HarmonicPercussiveFilter.h, Source/Analysis/HarmonicPercussiveFilter.cpp, Source/Analysis/ChromaExtractor.h, Source/Analysis/ChromaExtractor.cpp, Source/Analysis/OnsetEnvelope.h, Source/Analysis/OnsetEnvelope.cpp, Source/Analysis/TempoBeatTracker.h, Source/Analysis/TempoBeatTracker.cpp, Source/Analysis/KeyDetector.h, Source/Analysis/KeyDetector.cpp, Source/Analysis/ChordTemplates.h, Source/Analysis/ChordDecoder.h, Source/Analysis/ChordDecoder.cpp, Source/Analysis/ClassicDspChordAnalyzer.h, Source/Analysis/ClassicDspChordAnalyzer.cpp, Tests/SyntheticFixturesTests.cpp, Tests/AudioPreprocessingTests.cpp, Tests/ChromaExtractorTests.cpp, Tests/TuningEstimatorTests.cpp, Tests/TempoBeatTrackerTests.cpp, Tests/KeyDetectorTests.cpp, Tests/ChordDecoderTests.cpp, Tests/ClassicDspChordAnalyzerTests.cpp, CMakeLists.txt</files>
  <action>
Create `Source/Analysis/ChordAnalyzer.h` and `Source/Analysis/AnalysisResult.h` EXACTLY as specified in 03-RESEARCH.md section "ChordAnalyzer Interface &amp; AnalysisResult" (the full code blocks are in that file — transcribe them, including the `CancelToken` struct, `ProgressCallback` alias, and the `analyse(const juce::AudioBuffer&lt;float&gt;&amp;, double, juce::Range&lt;double&gt;, const ProgressCallback&amp;, const CancelToken&amp;)` signature, and all of `ChordQuality`/`ChordSymbol`/`ChordSegment`/`KeyResult`/`AnalysisResult`). These two headers are FROZEN contracts — Phase 4 depends on them; mark them with a comment `// FROZEN CONTRACT — do not change shape without updating Phase 4 plans`. `enum class ChordQuality` is declared ONLY in AnalysisResult.h (the ChordTemplates.h code sample in research re-declares it — do NOT duplicate; include AnalysisResult.h instead).

Create skeleton headers for every pipeline module with these starting contracts (downstream plans own and may refine INTERNAL signatures of the modules they implement, but structs marked FROZEN below are cross-plan boundaries consumed by later waves — keep their shapes):

- `AudioPreprocessing.h` (implemented in Task 3 of THIS plan; FROZEN):
  ```cpp
  struct PreprocessedAudio
  {
      std::vector<float> onsetSamples;    // mono @ 8000 Hz (Ellis 2007 path)
      std::vector<float> chromaSamples;   // mono @ 11025 Hz (CQT path)
      double onsetRate  = 8000.0;
      double chromaRate = 11025.0;
      double regionStartSeconds = 0.0;    // absolute source-file time of sample 0
  };
  PreprocessedAudio preprocessForAnalysis (const juce::AudioBuffer<float>& audio,
                                           double sourceSampleRate,
                                           juce::Range<double> regionSeconds);
  ```
- `ConstantQAnalysis.h` (owned by plan 03-02):
  ```cpp
  struct CqtFrames
  {
      std::vector<std::vector<double>> columns;  // [frame][bin] magnitudes, rectangular
      std::vector<double> binFrequenciesHz;      // per-bin center Hz, queried via getBinFrequency()
      double columnHopSeconds = 0.0;
      double latencySeconds = 0.0;
      double frameTimeSeconds (int frameIndex) const; // hop*i - latency, clamped >= 0
  };
  CqtFrames computeCqt (const std::vector<float>& samples, double sampleRate);
  ```
- `TuningEstimator.h` (owned by 03-02): `double estimateTuningCents (const CqtFrames& cqt);`
- `HarmonicPercussiveFilter.h` (owned by 03-02): `void suppressPercussion (CqtFrames& cqt, double kernelSeconds);`
- `ChromaExtractor.h` (owned by 03-02; ChromaFrame/ChromaSequence FROZEN — consumed by 03-04/03-05/03-06):
  ```cpp
  struct ChromaFrame
  {
      std::array<float, 12> harmonic {};  // full-range fold (>= ~80 Hz), tuning-corrected, L2-normalized
      std::array<float, 12> bass {};      // 55..250 Hz fold, tuning-corrected, L2-normalized
      float harmonicPreNormL2 = 0.0f;     // pre-normalization norm — silence / N-state detection
      double timeSeconds = 0.0;           // relative to analyzed-region start
  };
  struct ChromaSequence { std::vector<ChromaFrame> frames; };
  ChromaSequence extractChroma (const CqtFrames& cqt, double tuningCents);
  ```
- `OnsetEnvelope.h` (owned by 03-03): `struct OnsetEnvelopeResult { std::vector<double> envelope; double rateHz = 250.0; }; OnsetEnvelopeResult computeOnsetEnvelope (const std::vector<float>& samples8k);`
- `TempoBeatTracker.h` (owned by 03-03; BeatGrid FROZEN — consumed by 03-05/03-06):
  ```cpp
  struct BeatGrid
  {
      double bpm = 0.0;
      std::vector<double> beatTimesSeconds;   // relative to analyzed-region start
      std::vector<int> barStartBeatIndices;   // v1: every 4th beat (4/4 assumption)
  };
  BeatGrid trackBeats (const OnsetEnvelopeResult& onset);
  ```
- `KeyDetector.h` (owned by 03-04): `std::array<float,12> accumulateChroma (const ChromaSequence&); KeyResult detectKey (const std::array<float,12>& accumulatedChroma);`
- `ChordTemplates.h` (owned by 03-05): header-only; declare `std::array<float,12> buildTemplate (int root, ChordQuality quality);` — include AnalysisResult.h for ChordQuality.
- `ChordDecoder.h` (owned by 03-05): `std::vector<ChordSegment> decodeChords (const ChromaSequence& chroma, const BeatGrid& beats);`
- `ClassicDspChordAnalyzer.h` (owned by 03-06): `class ClassicDspChordAnalyzer : public ChordAnalyzer { public: AnalysisResult analyse (...) override; };`

Each module's `.cpp` is a stub: `#include "ModuleName.h"` plus, where a free function is declared, a placeholder definition that returns a default-constructed value (so both targets LINK, not just compile). `ClassicDspChordAnalyzer.cpp` stub returns an empty `AnalysisResult`. `CqtFrames::frameTimeSeconds` gets its trivial real implementation inline in the header now.

Create the eight Phase 3 test files as near-empty translation units: each contains only `#include <catch2/catch_test_macros.hpp>` (zero TEST_CASEs is fine — Catch2 links them as empty TUs). Real tests are added by the owning plans.

CMakeLists.txt changes:
1. Add ALL `Source/Analysis/*.cpp` files to `target_sources(ChordAI PRIVATE ...)` AND `target_sources(ChordAITests PRIVATE ...)`.
2. Add the eight new `Tests/*.cpp` files to `target_sources(ChordAITests PRIVATE ...)`.
3. Add `juce::juce_dsp` to `target_link_libraries` of BOTH targets (needed for juce::dsp::FFT / WindowingFunction in the onset path).
  </action>
  <verify>
    <automated>cmake --build build 2>&1 | tail -5 && ctest --test-dir build --output-on-failure 2>&1 | tail -5</automated>
  </verify>
  <done>All targets (ChordAI VST3/AU/Standalone + ChordAITests) build; all 15 pre-existing tests pass; Source/Analysis/ contains all 23 skeleton files; both frozen contract headers match the research spec exactly.</done>
</task>

<task type="auto">
  <name>Task 2: constant-q-cpp dependency + license capture + CQT engine sanity test</name>
  <files>CMakeLists.txt, THIRD_PARTY_LICENSES.md, Tests/ChromaExtractorTests.cpp</files>
  <action>
Wire `constant-q-cpp` (Chris Cannam / QMUL, MIT-style license) into the build, pinned to a specific commit:

```cmake
FetchContent_Declare(constant_q_cpp
    GIT_REPOSITORY https://github.com/cannam/constant-q-cpp.git
    GIT_TAG        7ac84048e3e43c433d88e12d221af15e50f41591)  # master HEAD as of 2026-07-12 — verify fetch succeeds
FetchContent_MakeAvailable(constant_q_cpp)
```

The repo has NO upstream CMakeLists.txt (verified in research — only Makefile.linux/.osx/.mingw32), so `FetchContent_MakeAvailable` only populates the source tree. After configure, LIST the fetched tree (`ls build/_deps/constant_q_cpp-src/`) and create a STATIC library target enumerating its sources explicitly: all of `src/*.cpp`, `src/dsp/*.cpp`, and the bundled KissFFT C sources under `src/ext/kissfft/` (typically `kiss_fft.c` and `tools/kiss_fftr.c` — enumerate what is ACTUALLY there, do not guess). Use `target_include_directories(<lib> SYSTEM PUBLIC ${constant_q_cpp_SOURCE_DIR})` so `#include "cq/CQSpectrogram.h"` works and third-party warnings are suppressed. Link the static lib into BOTH `ChordAI` and `ChordAITests`.

Create `THIRD_PARTY_LICENSES.md` at repo root containing: (1) a heading per dependency, (2) for constant-q-cpp: the repo URL, the pinned commit SHA, and the FULL verbatim text of its `COPYING` file (read it from `build/_deps/constant_q_cpp-src/COPYING` after fetch), (3) a note that KissFFT (BSD-3-Clause) is bundled inside constant-q-cpp under `src/ext/kissfft/` with its own license text captured too (read from the fetched tree). This satisfies PITFALLS.md #1's license-capture gate.

Add the first real test to `Tests/ChromaExtractorTests.cpp` — `TEST_CASE ("ChromaExtractorTests.CqtEngineSanity", "[chordanalysis]")` — exercising the RAW library API (not the wrapper, which is still a stub):
- Construct `CQParameters (11025.0, 32.70, 4186.01, 36)` with library defaults, then a `CQSpectrogram`. IMPORTANT: check the actual `CQSpectrogram` constructor signature in the fetched header `cq/CQSpectrogram.h` — it takes the parameters plus an interpolation mode; pick the mode that produces fully-populated rectangular columns (linear interpolation variant) after reading the header's doc comments. Do not invent the enum name — read it from the header.
- Feed a 2.0 s, 440 Hz sine at 11025 Hz via `process()` (chunked or single call), then ALWAYS call `getRemainingOutput()` and append (research pitfall: skipping the flush silently drops the tail).
- Assert runtime-queried values are sane (query, never hardcode — research pitfall #2): `getTotalBins()` &gt;= 36*6; `getMinFrequency()` in [25, 70]; `getColumnHop()` &gt; 0 with hop/11025 in [0.001, 0.1] s.
- Assert coverage: totalFrames * (getColumnHop()/11025.0) &gt;= 1.9 s (flush worked).
- Find the bin with maximum mean magnitude across frames; assert `getBinFrequency(thatBin)` is within ±3% of 440 Hz.
Also log (INFO/WARN in Catch2) the actual getColumnHop/getTotalBins/getMinFrequency values — this resolves 03-RESEARCH.md Open Question 1; record the observed values in the SUMMARY.
  </action>
  <verify>
    <automated>cmake --build build --target ChordAITests && ./build/ChordAITests_artefacts/Debug/ChordAITests "[chordanalysis]"</automated>
  </verify>
  <done>constant-q-cpp builds from a pinned SHA as a static lib linked into both targets; THIRD_PARTY_LICENSES.md contains verbatim COPYING text + SHA for constant-q-cpp and bundled KissFFT; CqtEngineSanity passes and logs the real hop/bin-count/min-freq values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Synthetic audio fixtures + dual-rate audio preprocessing</name>
  <files>Tests/SyntheticFixtures.h, Tests/SyntheticFixturesTests.cpp, Source/Analysis/AudioPreprocessing.cpp, Tests/AudioPreprocessingTests.cpp</files>
  <behavior>
    - SyntheticFixturesTests.ClickTrackHasExpectedBursts: renderClickTrack(120, 44100, 4.0) produces ~8 energy bursts whose peak positions match beat times 0.0, 0.5, 1.0 ... within ±5 ms
    - SyntheticFixturesTests.ChordRenderHasChordToneEnergy: renderChordProgression({C major}) — Goertzel energy at C4/E4/G4 frequencies each at least 10x the energy at a non-chord-tone (e.g. C#4)
    - SyntheticFixturesTests.DetuneShiftsFrequencies: detuneCents=-30 shifts the Goertzel peak from 261.63 Hz toward 257.1 Hz (energy at detuned frequency > energy at in-tune frequency)
    - AudioPreprocessingTests.DownmixAndDualRate: 2 s stereo 440 Hz @ 44100 → onsetSamples ≈ 16000 ±1%, chromaSamples ≈ 22050 ±1%, both mono, RMS within 25% of source RMS
    - AudioPreprocessingTests.RegionExtraction: buffer with sine only in [1.0, 2.0) s, region {1.0, 2.0} → output RMS ≈ full-signal RMS and regionStartSeconds == 1.0; empty/zero-length region → whole buffer processed
  </behavior>
  <action>
Write the failing tests first (RED), then implement (GREEN). All test cases tagged `[chordanalysis]`.

`Tests/SyntheticFixtures.h` (header-only, in-memory `juce::AudioBuffer<float>` — NO file I/O, per 03-VALIDATION.md Wave 0 spec). Include `Source/Analysis/AnalysisResult.h` and reuse `ChordSymbol`/`ChordQuality`. Provide, inside a `fixtures` namespace:
- `struct ChordSpec { ChordSymbol chord; int bassPitchClass = -1; }` (-1 → bass = root).
- `juce::AudioBuffer<float> renderChordProgression (const std::vector<ChordSpec>& chords, double bpm, double sampleRate, int beatsPerChord = 4, double detuneCents = 0.0)` — for each chord: chord tones as sines around octave 4 (MIDI 60–71 for pitch class p → MIDI = 60 + p), a bass note one/two octaves below (MIDI = 36 + bassPitchClass) at slightly higher amplitude, plus 2–3 integer harmonics per tone at decaying amplitude (1, 0.4, 0.2) so chroma looks mix-like, all frequencies scaled by `2^(detuneCents/1200)`; ~10 ms fade in/out per chord boundary to avoid clicks. Deterministic (no RNG or fixed seed).
- `juce::AudioBuffer<float> renderClickTrack (double bpm, double sampleRate, double durationSeconds, bool syncopated = false)` — 5 ms exponentially-decaying 1 kHz burst on every beat; when `syncopated`, add extra louder bursts on off-beats (the "and" of beats 2 and 4 of each 4-beat group) to tempt half/double-tempo errors (needed by 03-03's OctaveErrorResistance test).
- `void addPercussiveBursts (juce::AudioBuffer<float>& buffer, double bpm, double sampleRate)` — overlay broadband (white-noise, fixed-seed `juce::Random(42)`) 10 ms bursts on every beat at amplitude comparable to the harmonic content (needed by 03-02's PercussionRobustness test).
- A small Goertzel helper `double toneEnergy (const juce::AudioBuffer<float>&, double freqHz, double sampleRate)` used by self-tests (and reusable by later plans' tests).

`Source/Analysis/AudioPreprocessing.cpp` — real implementation replacing the Task 1 stub:
- Clamp/normalize `regionSeconds` (empty, inverted, or out-of-range → whole buffer; matches Phase 2's default-whole-file convention).
- Downmix: average all channels into one float vector over the region samples.
- Resample twice with `juce::WindowedSincInterpolator` (NOT Lagrange — research: 4x downsample of a full mix needs the 200-tap sinc's stopband rejection to avoid aliasing into the CQT range). Call `.reset()` before each use; `process (speedRatio, in, out, numOut)` where speedRatio = sourceRate/targetRate and numOut = floor(regionLen * targetRate/sourceRate). Guard: if source is already at/below target rate, still run through the interpolator (speedRatio &lt;= 1 upsampling is fine).
- Fill `regionStartSeconds` with the clamped region start.
  </action>
  <verify>
    <automated>cmake --build build --target ChordAITests && ./build/ChordAITests_artefacts/Debug/ChordAITests "[chordanalysis]"</automated>
  </verify>
  <done>All five behavior tests pass plus CqtEngineSanity from Task 2; fixtures render deterministic in-memory audio with chord/click/percussion/detune/syncopation variants; preprocessForAnalysis returns correct dual-rate mono streams for whole-file and region inputs.</done>
</task>

</tasks>

<verification>
- `cmake --build build` (600000 ms timeout) succeeds for ChordAI (VST3/AU/Standalone) and ChordAITests
- `ctest --test-dir build --output-on-failure` — 15 pre-existing tests + all new `[chordanalysis]` tests green
- `grep -c "Source/Analysis/" CMakeLists.txt` shows analysis sources registered in both targets
- THIRD_PARTY_LICENSES.md exists with verbatim license text and pinned SHA
</verification>

<success_criteria>
- Every downstream plan (03-02..03-06) can implement its modules WITHOUT modifying CMakeLists.txt
- ChordAnalyzer.h / AnalysisResult.h match the research contract exactly and are GUI/ThreadPool-free (headers include only JuceHeader.h + std)
- Fixture generators cover: chord progressions (with bass override + detune), click tracks (with syncopation), percussive-burst overlay
- Observed CQT runtime parameters (hop, bins, min freq) recorded in SUMMARY (resolves research Open Question 1)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-chord-detection-engine/03-01-SUMMARY.md`
</output>
