---
phase: 03-core-chord-detection-engine
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - Source/Analysis/KeyDetector.h
  - Source/Analysis/KeyDetector.cpp
  - Tests/KeyDetectorTests.cpp
autonomous: true
requirements: [ANL-01]

must_haves:
  truths:
    - "A synthetic C-major progression rendered to audio is detected as C major"
    - "A synthetic A-minor progression (containing the E major dominant) is detected as A minor, NOT C major (relative-key confusion)"
    - "KeyResult.confidence reflects the margin between best and second-best profile correlation"
  artifacts:
    - path: "Source/Analysis/KeyDetector.cpp"
      provides: "Krumhansl-Kessler 24-profile Pearson correlation key detection + chroma accumulation"
      min_lines: 50
      contains: "6.35"
    - path: "Tests/KeyDetectorTests.cpp"
      provides: "Pure-vector and audio-integration key tests including RelativeMinor"
      min_lines: 40
  key_links:
    - from: "Source/Analysis/KeyDetector.cpp"
      to: "ChromaSequence"
      via: "accumulateChroma energy-weighted sum of harmonic frames"
      pattern: "harmonicPreNormL2"
    - from: "Source/Analysis/KeyDetector.cpp"
      to: "KeyResult (AnalysisResult.h)"
      via: "tonicPitchClass/isMajor/confidence population"
      pattern: "KeyResult"
---

<objective>
Implement automatic key detection: accumulate a single tuning-corrected harmonic chroma vector over the analyzed region and correlate it against the 24 rotated Krumhansl-Kessler major/minor profiles, picking the best Pearson correlation with a confidence margin.

Purpose: ANL-01 directly. Phase 5's style-voicing generation depends on a correct key; the confidence margin lets Phase 4 surface ambiguity honestly.
Output: Working KeyDetector with green tests including the relative-minor disambiguation case.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-chord-detection-engine/03-RESEARCH.md
@.planning/phases/03-core-chord-detection-engine/03-02-SUMMARY.md
@Source/Analysis/KeyDetector.h
@Source/Analysis/ChromaExtractor.h
@Source/Analysis/AnalysisResult.h
@Tests/SyntheticFixtures.h

Working directory: /Users/test/Documents/prjcts/chordai. Build timeouts: 600000 ms.
Git: plain conventional commits, NO co-author/attribution trailers.
Do NOT modify CMakeLists.txt. This plan runs in parallel with 03-05 — touch ONLY the three files listed.

<interfaces>
From Source/Analysis/KeyDetector.h (skeleton from 03-01):
```cpp
std::array<float, 12> accumulateChroma (const ChromaSequence&);
KeyResult detectKey (const std::array<float, 12>& accumulatedChroma);
```

From Source/Analysis/AnalysisResult.h (FROZEN):
```cpp
struct KeyResult
{
    int tonicPitchClass = 0;   // 0=C..11=B
    bool isMajor = true;
    float confidence = 0.0f;   // normalized best-vs-second correlation margin
};
```

From Source/Analysis/ChromaExtractor.h (FROZEN, implemented in 03-02):
`ChromaFrame { std::array<float,12> harmonic; std::array<float,12> bass; float harmonicPreNormL2; double timeSeconds; }`, `ChromaSequence { std::vector<ChromaFrame> frames; }`

Krumhansl-Kessler profiles (03-RESEARCH.md "Key Detection" — embed EXACTLY, index 0 = tonic):
- Major: [6.35, 2.23, 3.48, 2.33, 4.38, 4.09, 2.52, 5.19, 2.39, 3.66, 2.29, 2.88]
- Minor: [6.33, 2.68, 3.52, 5.38, 2.60, 3.53, 2.54, 4.75, 3.98, 2.69, 3.34, 3.17]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KeyDetector core — K-K profile Pearson correlation over a 12-bin vector</name>
  <files>Source/Analysis/KeyDetector.h, Source/Analysis/KeyDetector.cpp, Tests/KeyDetectorTests.cpp</files>
  <behavior>
    - KeyDetectorTests.PureCMajorVector: hand-built chroma (energy at C-major scale degrees weighted like a I-IV-V progression: strong C, E, G; medium F, A; light D, B) → tonicPitchClass 0, isMajor true
    - KeyDetectorTests.PureAMinorVector: hand-built chroma with strong A, C, E plus G# (leading tone from the E-major dominant) → tonicPitchClass 9, isMajor false
    - KeyDetectorTests.TransposedProfileInvariance: rotating the C-major test vector by +2 semitones → D major detected (rotation logic correct for all 12 tonics)
    - KeyDetectorTests.ConfidenceMargins: a strongly-keyed vector yields higher confidence than a near-uniform (ambiguous) vector; ambiguous vector confidence is small but detectKey never crashes
  </behavior>
  <action>
Implement `detectKey`:
- Embed the two K-K profile tables from the interfaces block verbatim as `static constexpr` arrays (the values ARE the algorithm — any transcription error breaks detection silently; double-check all 24 numbers).
- For each of 24 candidates (12 tonics × {major, minor}): rotate the profile so index 0 lands on the candidate tonic, compute PEARSON correlation (subtract means of both vectors, normalized dot product; guard zero-variance input → correlation 0).
- Best candidate wins → tonicPitchClass/isMajor. `confidence = clamp((rBest - rSecond) / (std::abs(rBest) + 1e-9), 0, 1)` where rSecond is the second-best among ALL 24 candidates.
- Degenerate all-zero chroma → return default KeyResult with confidence 0 (never throw).
Write the four behavior tests first (RED), tag `[chordanalysis]`, then implement (GREEN).
  </action>
  <verify>
    <automated>cmake --build build --target ChordAITests && ./build/ChordAITests_artefacts/Debug/ChordAITests "KeyDetectorTests.*"</automated>
  </verify>
  <done>All 24 keys reachable via rotation; Pearson (not raw dot-product) correlation; confidence = normalized best-vs-second margin; zero-variance safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audio-integration key tests through the real chroma path</name>
  <files>Source/Analysis/KeyDetector.cpp, Tests/KeyDetectorTests.cpp</files>
  <behavior>
    - KeyDetectorTests.CMajorFixture: render C-F-G-C progression (all major, roots 0/5/7/0) @ 120 BPM → preprocess → computeCqt → suppressPercussion → extractChroma → accumulateChroma → detectKey → C major
    - KeyDetectorTests.RelativeMinor: render Am-Dm-E-Am (roots 9/2/4/9, qualities minor/minor/MAJOR/minor — the E major dominant supplies the G# leading tone that breaks the C-major tie) → A minor detected, not C major
    - KeyDetectorTests.DetunedKeyStillDetected: C-F-G-C at detuneCents=-30 with tuning estimate applied → still C major
  </behavior>
  <action>
Implement `accumulateChroma(const ChromaSequence&)`:
- Energy-weighted sum: for each frame, add `harmonic[i] * harmonicPreNormL2` (frames with more energy count more; silent frames contribute nothing) — research: key detection wants maximum data over the whole region, NOT beat-synchronized granularity.
- Normalize the accumulated vector (L2) before returning; all-silent sequence → all-zero vector.

Add the three integration tests (RED first, tag `[chordanalysis]`) using the full real chain: `fixtures::renderChordProgression` @ 44100 → `preprocessForAnalysis` (whole buffer) → `computeCqt(chromaSamples, 11025)` → `suppressPercussion` → `extractChroma(cqt, estimateTuningCents(cqt))` → `accumulateChroma` → `detectKey`. Use >= 8 s of audio per fixture (2 bars per chord at 120 BPM) so the accumulated chroma is stable. The RelativeMinor fixture MUST include the E major chord — a natural-minor-only progression is genuinely ambiguous with C major and would make the test flaky by design.
  </action>
  <verify>
    <automated>cmake --build build --target ChordAITests && ./build/ChordAITests_artefacts/Debug/ChordAITests "KeyDetectorTests.*"</automated>
  </verify>
  <done>C major, A minor (with dominant), and detuned C major all detected correctly through the full audio → chroma → key chain.</done>
</task>

</tasks>

<verification>
- `./build/ChordAITests_artefacts/Debug/ChordAITests "[chordanalysis]"` — all tests green
- `ctest --test-dir build --output-on-failure` — full suite green
- CMakeLists.txt untouched
</verification>

<success_criteria>
- ANL-01 evidenced end-to-end at module level: synthetic audio in → correct KeyResult out
- Relative major/minor confusion demonstrably avoided on the dominant-bearing fixture
- KeyDetector consumes only ChromaSequence + std types (headless, facade-ready)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-chord-detection-engine/03-04-SUMMARY.md`
</output>
