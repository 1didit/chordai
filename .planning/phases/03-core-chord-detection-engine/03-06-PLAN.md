---
phase: 03-core-chord-detection-engine
plan: 06
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified:
  - Source/Analysis/ClassicDspChordAnalyzer.h
  - Source/Analysis/ClassicDspChordAnalyzer.cpp
  - Source/Analysis/ConstantQAnalysis.h
  - Source/Analysis/ConstantQAnalysis.cpp
  - Tests/ClassicDspChordAnalyzerTests.cpp
autonomous: false
requirements: [ANL-01, ANL-02, ANL-03, ANL-06]

must_haves:
  truths:
    - "analyse() on a synthetic song returns correct key, BPM (±3), and exact chord sequence from a bare Catch2 test — no GUI, no ThreadPool, no MessageManager"
    - "A CancelToken returning true stops analysis mid-run and returns wasCancelled=true with empty chords"
    - "ProgressCallback fires with monotonically non-decreasing fractions from 0 to 1.0 across named stages"
    - "A 3-minute synthetic song analyzes in under ~10 s wall clock"
    - "Full test suite green — no Phase 1/2 regressions"
    - "A real track produces a musically plausible progression (human-verified)"
  artifacts:
    - path: "Source/Analysis/ClassicDspChordAnalyzer.cpp"
      provides: "Synchronous pipeline orchestration behind ChordAnalyzer: preprocess -> beat -> chroma -> key -> chords"
      min_lines: 80
    - path: "Tests/ClassicDspChordAnalyzerTests.cpp"
      provides: "HeadlessInvocation, Cancellation, ProgressMonotonic, PerformanceBudget tests + hidden real-track harness"
      min_lines: 80
  key_links:
    - from: "Source/Analysis/ClassicDspChordAnalyzer.cpp"
      to: "preprocessForAnalysis / computeOnsetEnvelope / trackBeats / computeCqt / suppressPercussion / extractChroma / accumulateChroma / detectKey / decodeChords"
      via: "sequential synchronous orchestration with per-stage progress + cancel checks"
      pattern: "decodeChords"
    - from: "Source/Analysis/ClassicDspChordAnalyzer.cpp"
      to: "AnalysisResult"
      via: "full population: bpm, beatTimesSeconds, barStartBeatIndices, key, chords, analyzedRegionSeconds, wasCancelled"
      pattern: "wasCancelled"
    - from: "Tests/ClassicDspChordAnalyzerTests.cpp"
      to: "ChordAnalyzer interface"
      via: "invocation through a ChordAnalyzer& (not the concrete type) proving the swappable-interface contract"
      pattern: "ChordAnalyzer&"
---

<objective>
Assemble the complete pipeline behind the ChordAnalyzer interface as ClassicDspChordAnalyzer: synchronous, headless, cancellable, progress-reporting orchestration of preprocessing → beat tracking → chroma → key → chord decoding, with the phase performance budget enforced by test and a human listening check on a real track.

Purpose: ANL-06's swappable interface becomes real; ANL-01/02/03 become end-to-end verifiable through one call. This is the exact object Phase 4 wraps in a ThreadPoolJob and Phase 5 consumes AnalysisResult from.
Output: Working ClassicDspChordAnalyzer, all Phase 3 tests + full regression green, performance budget met, human-verified real-track plausibility.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-chord-detection-engine/03-RESEARCH.md
@.planning/phases/03-core-chord-detection-engine/03-VALIDATION.md
@.planning/phases/03-core-chord-detection-engine/03-02-SUMMARY.md
@.planning/phases/03-core-chord-detection-engine/03-03-SUMMARY.md
@.planning/phases/03-core-chord-detection-engine/03-04-SUMMARY.md
@.planning/phases/03-core-chord-detection-engine/03-05-SUMMARY.md
@Source/Analysis/ChordAnalyzer.h
@Source/Analysis/AnalysisResult.h
@Source/Analysis/ClassicDspChordAnalyzer.h
@Source/Import/AudioFileLoader.h
@Tests/SyntheticFixtures.h

Working directory: /Users/test/Documents/prjcts/chordai. Build timeouts: 600000 ms.
Git: plain conventional commits, NO co-author/attribution trailers.
Do NOT modify CMakeLists.txt.
FROZEN: ChordAnalyzer.h and AnalysisResult.h must NOT change shape (Phase 4 contract). ConstantQAnalysis MAY gain an optional cancel-callback parameter (backward-compatible default) — 03-02 structured its feed loop for this.

<interfaces>
From Source/Analysis/ChordAnalyzer.h (FROZEN — implement exactly):
```cpp
struct CancelToken { virtual bool shouldCancel() const = 0; ... };
using ProgressCallback = std::function<void (double fractionComplete, const juce::String& stage)>;
virtual AnalysisResult analyse (const juce::AudioBuffer<float>& audio,
                                 double sampleRate,
                                 juce::Range<double> regionSeconds,
                                 const ProgressCallback& onProgress,
                                 const CancelToken& cancelToken) = 0;
```

Module chain available (all implemented and tested by prior plans):
`preprocessForAnalysis(buffer, rate, region)` → PreprocessedAudio { onsetSamples@8k, chromaSamples@11025, regionStartSeconds } →
`computeOnsetEnvelope(onsetSamples)` → `trackBeats(onset)` → BeatGrid (region-relative) →
`computeCqt(chromaSamples, 11025)` → `estimateTuningCents(cqt)` → `suppressPercussion(cqt, kernel)` → `extractChroma(cqt, cents)` → ChromaSequence (region-relative) →
`accumulateChroma(chroma)` → `detectKey(...)` → KeyResult; `decodeChords(chroma, beats)` → std::vector&lt;ChordSegment&gt;.

Error convention (Source/Import/AudioFileLoader.h): never throws, never asserts on bad input — degrade to an empty-but-valid result.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClassicDspChordAnalyzer — synchronous orchestration with progress + cancellation</name>
  <files>Source/Analysis/ClassicDspChordAnalyzer.h, Source/Analysis/ClassicDspChordAnalyzer.cpp, Source/Analysis/ConstantQAnalysis.h, Source/Analysis/ConstantQAnalysis.cpp, Tests/ClassicDspChordAnalyzerTests.cpp</files>
  <behavior>
    - ClassicDspChordAnalyzerTests.HeadlessInvocation: 16 s C-F-G-Am fixture @ 120 BPM, called through a `ChordAnalyzer&` reference with no-op token/callback → key C major (fixture is C-major-weighted), bpm within ±3 of 120, chords contain the exact ordered subsequence [C, F, G, Am] (major/major/major/minor), beatTimesSeconds non-empty, barStartBeatIndices = every 4th index — all from a plain Catch2 test (no MessageManager/ThreadPool constructed anywhere)
    - ClassicDspChordAnalyzerTests.Cancellation: token whose shouldCancel() returns true from the start → returns quickly with wasCancelled == true and chords.empty(); token that flips true after the first progress callback → same guarantees
    - ClassicDspChordAnalyzerTests.ProgressMonotonic: recorded fractions are non-decreasing, first <= 0.1, last == 1.0 (Catch2 Approx), and stage labels include at least "beat", "chroma", "key", "chords"
    - ClassicDspChordAnalyzerTests.RegionRestriction: 20 s buffer where only [5, 15) s contains the progression, regionSeconds = {5, 15} → chord segment times fall within ~[5, 15] in ABSOLUTE source-file seconds (regionStartSeconds offset applied)
    - ClassicDspChordAnalyzerTests.DegenerateInput: empty buffer and 0.2 s buffer → valid result, bpm 0, empty chords, wasCancelled false, no throw
  </behavior>
  <action>
Implement `ClassicDspChordAnalyzer::analyse` as a pure synchronous sequence (research threading split: NO threads, NO MessageManager, NO GUI includes):
1. Clamp region (empty/invalid → whole buffer, matching Phase 2 semantics); `preprocessForAnalysis` — progress ("decoding", 0.0 → 0.10).
2. `computeOnsetEnvelope` + `trackBeats` — progress ("beat", 0.10 → 0.35).
3. `computeCqt` + `estimateTuningCents` + `suppressPercussion` + `extractChroma` — progress ("chroma", 0.35 → 0.70). Add an optional `const std::function<bool()>& shouldCancel = {}` parameter to `computeCqt` (backward-compatible — existing 03-02 tests must still compile unchanged) and check it per feed chunk so cancellation latency inside the longest stage is sub-second.
4. `accumulateChroma` + `detectKey` — progress ("key", 0.70 → 0.80).
5. `decodeChords` — progress ("chords", 0.80 → 1.0); ALWAYS emit a final (1.0, "chords") on success.
- Check `cancelToken.shouldCancel()` before/after every stage (plus the in-CQT hook): on cancel, return AnalysisResult with `wasCancelled = true`, `chords` empty, whatever fields were already computed left as-is.
- TIME BASE: BeatGrid and ChromaSequence are region-relative; shift `beatTimesSeconds` and all ChordSegment start/end seconds by `regionStartSeconds` so AnalysisResult carries ABSOLUTE source-file time (Phase 4 overlays these on the waveform; document this in AnalysisResult-population comments). `analyzedRegionSeconds` = the clamped region; `sampleRate` = source rate.
- Degenerate results at any stage (bpm 0 / no beats / empty chroma) → return valid result with what exists, empty chords, never throw (loadAudioFileSync convention).
Tests RED first, tag `[chordanalysis]`. HeadlessInvocation MUST call through `ChordAnalyzer&` (proves ANL-06's swappable-interface contract, not just the concrete class).
  </action>
  <verify>
    <automated>cmake --build build --target ChordAITests && ./build/ChordAITests_artefacts/Debug/ChordAITests "ClassicDspChordAnalyzerTests.*"</automated>
  </verify>
  <done>Full pipeline runs end-to-end behind the interface: correct key/bpm/chords on the synthetic song, cancellation and monotonic progress proven, region times absolute, degenerate input safe.</done>
</task>

<task type="auto">
  <name>Task 2: Performance budget test + hidden real-track harness + full regression</name>
  <files>Tests/ClassicDspChordAnalyzerTests.cpp</files>
  <action>
Add `TEST_CASE ("ClassicDspChordAnalyzerTests.PerformanceBudget", "[chordanalysis]")`:
- Render a 180 s (3-minute) synthetic progression at 44100 Hz stereo (8-chord loop, beatsPerChord=4 @ 120 BPM).
- Time a full `analyse()` with `std::chrono::steady_clock`; `REQUIRE (elapsedSeconds < 10.0)` (phase success criterion: "seconds, not minutes"; research budget ~5-10 s).
- Also REQUIRE the result is non-degenerate (bpm > 0, chords non-empty) so the budget isn't "met" by an early bail.
- If the Debug build misses 10 s: profile the obvious hotspots FIRST (HarmonicPercussiveFilter's median filter and the mel filterbank are the likely candidates — fix algorithmic waste, e.g. recomputed kernels or per-frame allocations) before touching the threshold. Only if a genuinely-optimized Debug build still misses, document the measured time in the SUMMARY and raise the constant to ceil(measured × 1.5) with an explanatory comment — never silently delete the test.

Add a hidden manual harness `TEST_CASE ("ClassicDspChordAnalyzerTests.RealTrackHarness", "[.realtrack]")` (leading dot = hidden tag, never runs in ctest/default runs):
- Read env var `CHORDAI_REAL_TRACK`; if unset, `SUCCEED("set CHORDAI_REAL_TRACK=/path/to/audio to run")` and return.
- Load via `loadAudioFileSync` (AudioFormatManager + registerBasicFormats — same as production import path), run `analyse()` on the whole file, print to stdout: detected BPM, key (e.g. "A minor"), and each chord segment as `mm:ss.cc  <ChordName>` (pitch-class → name table C, C#, D, ... B + maj/min/7 suffix), plus wall-clock analysis time.

Run the FULL regression gate: `ctest --test-dir build --output-on-failure` — all Phase 1/2 tests (15) plus every Phase 3 test must be green.
  </action>
  <verify>
    <automated>cmake --build build --target ChordAITests && ./build/ChordAITests_artefacts/Debug/ChordAITests "[chordanalysis]" && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <done>3-minute synthetic analyzed inside the budget with a real (non-degenerate) result; hidden [.realtrack] harness prints BPM/key/timed chord list for any local audio file; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real-track listening check (musical plausibility)</name>
  <what-built>Complete headless chord-detection engine (ClassicDspChordAnalyzer): key, tempo/bar grid, and beat-aligned chord progression from any decoded audio, verified green on all synthetic fixtures and the performance budget. A hidden test harness prints the analysis of any real audio file.</what-built>
  <how-to-verify>
1. Pick 1-3 real songs you know the chords of (royalty-cleared / your own library; MP3/WAV/AIFF/FLAC all work).
2. From /Users/test/Documents/prjcts/chordai run:
   `CHORDAI_REAL_TRACK="/path/to/song.mp3" ./build/ChordAITests_artefacts/Debug/ChordAITests "[.realtrack]" -s`
3. Check the printed output:
   - BPM: within a few BPM of the song's real tempo (watch for exact half/double — that's the octave-error failure mode)
   - Key: matches the song's actual key (relative major/minor confusion is the known trap)
   - Chords: the progression is musically plausible — the main loop's chords appear at roughly the right times; occasional wrong qualities (maj vs dom7) are acceptable at the documented ~75-80% domain ceiling, systematic nonsense is not
   - Analysis time: seconds, not minutes, for a ~3-minute song
4. Known v1 limitations (NOT failures): everything is labeled root-position (no slash chords), vocabulary is maj/min/dom7 only, bar grid assumes 4/4.
  </how-to-verify>
  <action>Present the verification steps above to the user, wait for their listening check on 1-3 real tracks, and record the outcome (approved / issues with song+timestamp+expected-vs-detected details). No code changes in this task — it gates phase completion on human musical-plausibility evidence that synthetic fixtures cannot provide (03-VALIDATION.md manual-only row).</action>
  <verify>User responds with "approved" or a concrete issue report</verify>
  <done>User confirms real-track BPM/key/chord output is musically plausible within the documented v1 limitations (root-position maj/min/dom7 vocabulary, 4/4 bar grid, ~75-80% domain accuracy ceiling)</done>
  <resume-signal>Type "approved" if the results are musically plausible, or describe what sounded wrong (song, timestamp, expected vs detected) so gaps can be diagnosed</resume-signal>
</task>

</tasks>

<verification>
- `ctest --test-dir build --output-on-failure` — full suite green (15 pre-existing + all Phase 3 tests)
- `./build/ChordAITests_artefacts/Debug/ChordAITests "[chordanalysis]"` — all green including PerformanceBudget
- Human approval on real-track plausibility recorded
- ChordAnalyzer.h / AnalysisResult.h unchanged since 03-01 (`git diff 03-01-commit..HEAD -- Source/Analysis/ChordAnalyzer.h Source/Analysis/AnalysisResult.h` shape-identical)
</verification>

<success_criteria>
- ANL-01/02/03 all observable through one `analyse()` call from a bare test harness (phase success criteria 1-4)
- ANL-06: pipeline invoked exclusively through the ChordAnalyzer interface; Phase 4 needs only a ThreadPoolJob-backed CancelToken adapter and progress marshalling
- Performance: 3-minute song in seconds (budget test enforced in-suite)
- Real-track musical plausibility human-verified (03-VALIDATION.md manual-only row satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-chord-detection-engine/03-06-SUMMARY.md`
</output>
