---
phase: 03-core-chord-detection-engine
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - Source/Analysis/ConstantQAnalysis.h
  - Source/Analysis/ConstantQAnalysis.cpp
  - Source/Analysis/TuningEstimator.h
  - Source/Analysis/TuningEstimator.cpp
  - Source/Analysis/HarmonicPercussiveFilter.h
  - Source/Analysis/HarmonicPercussiveFilter.cpp
  - Source/Analysis/ChromaExtractor.h
  - Source/Analysis/ChromaExtractor.cpp
  - Tests/ChromaExtractorTests.cpp
  - Tests/TuningEstimatorTests.cpp
autonomous: true
requirements: [ANL-03]

must_haves:
  truths:
    - "A synthetic triad rendered to audio yields harmonic chroma whose top-3 pitch classes are exactly the chord tones"
    - "A fixture detuned by -30 cents is measured within tolerance by TuningEstimator and still folds to the correct pitch classes after correction"
    - "Injecting percussive bursts onto a progression does not change the dominant pitch classes of its harmonic chroma"
    - "Bass chroma reflects only 55-250 Hz content; harmonic chroma excludes sub-80 Hz content"
    - "CQT output covers the full input duration (streaming flush works) with runtime-queried parameters"
  artifacts:
    - path: "Source/Analysis/ConstantQAnalysis.cpp"
      provides: "CQSpectrogram wrapper: streaming process + flush, runtime param query, time alignment"
      min_lines: 50
    - path: "Source/Analysis/TuningEstimator.cpp"
      provides: "Magnitude-weighted circular cents-histogram tuning estimate"
      min_lines: 30
    - path: "Source/Analysis/HarmonicPercussiveFilter.cpp"
      provides: "Median filter along time axis per CQT bin (Fitzgerald 2010-style)"
      min_lines: 25
    - path: "Source/Analysis/ChromaExtractor.cpp"
      provides: "Dual harmonic+bass 12-bin chroma fold with tuning correction and thresholded L2 normalization"
      min_lines: 50
  key_links:
    - from: "Source/Analysis/ChromaExtractor.cpp"
      to: "CqtFrames.binFrequenciesHz"
      via: "pitchClassForBin fold using per-bin queried frequency (never assumed bin ordering)"
      pattern: "binFrequenciesHz"
    - from: "Source/Analysis/ChromaExtractor.cpp"
      to: "TuningEstimator thetaCents"
      via: "tuning-corrected MIDI-note rounding before pitch-class fold"
      pattern: "tuningCents"
    - from: "Source/Analysis/ConstantQAnalysis.cpp"
      to: "CQSpectrogram"
      via: "process() + getRemainingOutput() streaming contract"
      pattern: "getRemainingOutput"
---

<objective>
Implement the full chroma-extraction path of the chord-detection pipeline: CQT wrapper (ConstantQAnalysis), tuning-offset estimation (TuningEstimator), percussion suppression (HarmonicPercussiveFilter), and the dual harmonic+bass chroma fold (ChromaExtractor) — TDD against the synthetic fixtures from plan 03-01.

Purpose: This is the observation front-end for ANL-03 (chord recognition) and ANL-01 (key detection). Getting robust, tuning-corrected, percussion-suppressed chroma is what separates a working detector from the "hello world" pipeline that fails on real mixes (PITFALLS.md #2/#3).
Output: Working ConstantQAnalysis/TuningEstimator/HarmonicPercussiveFilter/ChromaExtractor with green [chordanalysis] tests.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-chord-detection-engine/03-RESEARCH.md
@.planning/phases/03-core-chord-detection-engine/03-01-SUMMARY.md
@Source/Analysis/ConstantQAnalysis.h
@Source/Analysis/ChromaExtractor.h
@Source/Analysis/AudioPreprocessing.h
@Tests/SyntheticFixtures.h

Working directory: /Users/test/Documents/prjcts/chordai. Build timeouts: 600000 ms.
Git: plain conventional commits, NO co-author/attribution trailers.
Do NOT modify CMakeLists.txt — all files in this plan are already registered (03-01).
FROZEN cross-plan contracts: `ChromaFrame`/`ChromaSequence` struct shapes in ChromaExtractor.h (consumed by plans 03-04/03-05/03-06) and `CqtFrames` public fields. You may refine internal/private details freely.

<interfaces>
From Source/Analysis/ConstantQAnalysis.h (skeleton from 03-01 — implement against this):
```cpp
struct CqtFrames
{
    std::vector<std::vector<double>> columns;  // [frame][bin] magnitudes, rectangular
    std::vector<double> binFrequenciesHz;      // per-bin center Hz via getBinFrequency()
    double columnHopSeconds = 0.0;
    double latencySeconds = 0.0;
    double frameTimeSeconds (int frameIndex) const;
};
CqtFrames computeCqt (const std::vector<float>& samples, double sampleRate);
```

From Source/Analysis/ChromaExtractor.h (FROZEN output shape):
```cpp
struct ChromaFrame
{
    std::array<float, 12> harmonic {};
    std::array<float, 12> bass {};
    float harmonicPreNormL2 = 0.0f;
    double timeSeconds = 0.0;
};
struct ChromaSequence { std::vector<ChromaFrame> frames; };
ChromaSequence extractChroma (const CqtFrames& cqt, double tuningCents);
```

Pitch-class fold formula (03-RESEARCH.md Pattern 2 — use verbatim):
```cpp
double correctedA4 = 440.0 * std::pow (2.0, thetaCents / 1200.0);
double midiNote = 69.0 + 12.0 * std::log2 (freqHz / correctedA4);
int pitchClass = ((int) std::lround (midiNote)) % 12;  // + 12 if negative
```

Fixtures available (Tests/SyntheticFixtures.h, from 03-01): `fixtures::renderChordProgression(chords, bpm, sampleRate, beatsPerChord, detuneCents)`, `fixtures::addPercussiveBursts(buffer, bpm, sampleRate)`, `fixtures::toneEnergy(...)`; preprocessing via `preprocessForAnalysis(...)` → `.chromaSamples` @ 11025 Hz.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConstantQAnalysis — streaming CQT wrapper with runtime-queried parameters</name>
  <files>Source/Analysis/ConstantQAnalysis.h, Source/Analysis/ConstantQAnalysis.cpp, Tests/ChromaExtractorTests.cpp</files>
  <behavior>
    - ChromaExtractorTests.CqtWrapperCoversFullDuration: computeCqt on 3.0 s of audio @ 11025 Hz → columns.size() * columnHopSeconds >= 2.9 s; all columns have identical size == binFrequenciesHz.size()
    - ChromaExtractorTests.CqtWrapperSinePeak: 440 Hz sine → argmax-mean-magnitude bin's binFrequenciesHz within ±3% of 440
    - ChromaExtractorTests.CqtWrapperEmptyInput: empty sample vector → empty columns, no crash (never-throws convention)
  </behavior>
  <action>
Implement `computeCqt` in ConstantQAnalysis.cpp against the raw library API already proven by 03-01's CqtEngineSanity test:
- `CQParameters (sampleRate, 32.70, 4186.01, 36)`, library defaults kept (q=1.0, atomHopFactor=0.25, threshold=0.0005, window=SqrtBlackmanHarris, decimator=BetterDecimator — these ARE the defaults, don't set them explicitly unless the header requires it).
- Construct `CQSpectrogram` with the same interpolation mode 03-01 validated (read from cq/CQSpectrogram.h).
- Feed samples via `process()` in chunks (e.g. 16384) with a final `getRemainingOutput()` append — the flush is mandatory (research pitfall: tail silently dropped otherwise).
- Populate `binFrequenciesHz[b] = getBinFrequency(b)` for every b in [0, getTotalBins()) — do NOT assume bin ordering (low-to-high or high-to-low); every consumer must go through this array.
- `columnHopSeconds = getColumnHop() / sampleRate`; `latencySeconds = getLatency() / sampleRate`.
- Convert magnitude columns to `std::vector<std::vector<double>>`; drop any leading latency-only columns OR keep them and rely on frameTimeSeconds clamping — choose one, document in a comment, and keep frameTimeSeconds consistent with the choice.
Keep a `shouldCancel` extension point in mind (plan 03-06 will add an optional cancel callback parameter) — structure the feed loop so a check can be inserted per chunk.
  </action>
  <verify>
    <automated>cmake --build build --target ChordAITests && ./build/ChordAITests_artefacts/Debug/ChordAITests "ChromaExtractorTests.*"</automated>
  </verify>
  <done>computeCqt returns rectangular, duration-covering, frequency-annotated CQT frames for arbitrary-length input; empty input degrades gracefully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TuningEstimator — magnitude-weighted circular cents estimate</name>
  <files>Source/Analysis/TuningEstimator.h, Source/Analysis/TuningEstimator.cpp, Tests/TuningEstimatorTests.cpp</files>
  <behavior>
    - TuningEstimatorTests.NearZeroOnTunedFixture: in-tune C-F-G-C progression → |estimateTuningCents| < 8 cents
    - TuningEstimatorTests.DetectsMinus30Cents: same progression with detuneCents=-30 → estimate in [-40, -20]
    - TuningEstimatorTests.DetectsPlus25Cents: detuneCents=+25 → estimate in [15, 35]
  </behavior>
  <action>
Implement the Harte &amp; Sandler-style cents-histogram estimate (research: this project's own reasoned implementation of the published concept — validate empirically, which is exactly what these tests do):
- For each CQT frame, take bins whose magnitude exceeds a relative threshold (e.g. > 0.3 × frame max) — the "strong" bins.
- For each strong bin: `midiNote = 69 + 12*log2(binFrequenciesHz[b]/440)`; cents deviation `d = (midiNote - round(midiNote)) * 100` ∈ [-50, 50).
- Accumulate as a CIRCULAR weighted mean (deviation space wraps at ±50 cents): map d → angle θ = d/50 · π, sum magnitude-weighted (cos θ, sin θ) vectors across all strong bins of all frames, `thetaCents = atan2(Σsin, Σcos)/π · 50`.
- Guard: if total weight ~0 (silence) return 0.0.
Expose the relative-threshold constant as a named constant (kStrongBinThreshold), not a magic number. Write the three tests (RED) before implementing (GREEN); tag `[chordanalysis]`; fixtures: `renderChordProgression` at 44100 → `preprocessForAnalysis` → `.chromaSamples` → `computeCqt`.
  </action>
  <verify>
    <automated>cmake --build build --target ChordAITests && ./build/ChordAITests_artefacts/Debug/ChordAITests "TuningEstimatorTests.*"</automated>
  </verify>
  <done>Tuning offset recovered within ±10 cents for -30/+25/0-cent fixtures; silence-safe; threshold is a named constant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HarmonicPercussiveFilter + ChromaExtractor dual fold</name>
  <files>Source/Analysis/HarmonicPercussiveFilter.h, Source/Analysis/HarmonicPercussiveFilter.cpp, Source/Analysis/ChromaExtractor.h, Source/Analysis/ChromaExtractor.cpp, Tests/ChromaExtractorTests.cpp</files>
  <behavior>
    - ChromaExtractorTests.TriadFoldTopPitchClasses: C major triad fixture (root pc 0) → mean harmonic chroma's top-3 bins == {0, 4, 7} (any order)
    - ChromaExtractorTests.PercussionRobustness: same fixture + addPercussiveBursts → top-3 mean harmonic chroma bins STILL {0, 4, 7}
    - ChromaExtractorTests.DetunedFixtureFoldsCorrectly: -30 cents C major fixture, extractChroma with thetaCents from TuningEstimator → top-3 still {0, 4, 7}
    - ChromaExtractorTests.BassChromaIsolatesBassNote: C-E-G upper voices with bassPitchClass=9 (A bass) → mean bass chroma argmax == 9 while harmonic top-3 still contains {0, 4, 7}
    - ChromaExtractorTests.SilenceFramesFlagged: 1 s silence appended to fixture → trailing frames have harmonicPreNormL2 below the audible frames' values and all-zero normalized chroma (no NaNs — thresholded normalization)
  </behavior>
  <action>
`HarmonicPercussiveFilter.cpp` — `suppressPercussion(CqtFrames&, double kernelSeconds)`:
- Per frequency bin (row), median-filter the magnitude sequence along the TIME axis (Fitzgerald 2010-style harmonic enhancement): kernel length = odd number of frames ≈ kernelSeconds / columnHopSeconds (clamp to >= 3, force odd). Default kernelSeconds ≈ 0.3 (named constant kHpssKernelSeconds; research flags 200-400 ms as the tunable range).
- Replace each column's magnitudes with the median-filtered values in place. Edge frames: shrink the window (median of available neighbors).
- Keep it O(frames × bins × kernel) with std::nth_element on a reused scratch buffer — the performance-budget test in 03-06 will catch a pathologically slow implementation.

`ChromaExtractor.cpp` — `extractChroma(const CqtFrames&, double tuningCents)`:
- For each frame, accumulate two 12-bin vectors using the pitch-class fold formula from the interfaces block (tuning-corrected):
  - `harmonic`: bins with `binFrequenciesHz[b] >= 80.0` (named constant kHarmonicMinHz — PITFALLS #2 sub-bass exclusion)
  - `bass`: bins with `55.0 <= binFrequenciesHz[b] <= 250.0` (named constants — verbatim PITFALLS #3 range)
- Record `harmonicPreNormL2` BEFORE normalization; L2-normalize both vectors with a floor threshold (named kSilenceEpsilon): if pre-norm L2 < epsilon, leave the vector all-zero rather than dividing (FMP thresholded-normalization pattern — no NaN/Inf ever).
- `timeSeconds = cqt.frameTimeSeconds(i)`.
- extractChroma itself does NOT call suppressPercussion — the caller (facade in 03-06, and these tests) applies `suppressPercussion` first. Document the expected call order in the header comment.
Tests (RED first): use fixtures at 44100 → preprocessForAnalysis → computeCqt → suppressPercussion → extractChroma. PercussionRobustness MUST go through suppressPercussion; also assert the same fixture WITHOUT suppression is measurably worse or at minimum that suppression does not degrade the clean fixture (guards against a no-op filter).
  </action>
  <verify>
    <automated>cmake --build build --target ChordAITests && ./build/ChordAITests_artefacts/Debug/ChordAITests "[chordanalysis]"</automated>
  </verify>
  <done>All chroma-path tests green including percussion robustness, detune correction, and bass isolation; no NaN/Inf on silence; constants named, not inline magic numbers.</done>
</task>

</tasks>

<verification>
- `./build/ChordAITests_artefacts/Debug/ChordAITests "[chordanalysis]"` — all chroma-path + tuning tests green
- `ctest --test-dir build --output-on-failure` — full suite green (no Phase 1-2 regressions)
- CMakeLists.txt untouched (`git diff --name-only` contains no CMakeLists.txt)
</verification>

<success_criteria>
- ChromaSequence output (FROZEN shape) ready for consumption by KeyDetector (03-04) and ChordDecoder (03-05)
- Every tunable (kHpssKernelSeconds, kHarmonicMinHz, kSilenceEpsilon, kStrongBinThreshold) is a named constant per research Open Question 3
- Chroma survives the three adversarial fixtures: percussion, -30 cents detune, inverted bass
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-chord-detection-engine/03-02-SUMMARY.md`
</output>
