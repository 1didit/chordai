---
phase: 02-audio-import-waveform
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - Source/UI/WaveformMath.h
  - Source/UI/RegionSelectionModel.h
  - Source/UI/WaveformView.h
  - Source/UI/WaveformView.cpp
  - Source/UI/RegionSelectorOverlay.h
  - Source/UI/RegionSelectorOverlay.cpp
  - Source/PluginEditor.h
  - Source/PluginEditor.cpp
  - Tests/WaveformRegionTests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [IMP-02, IMP-03]

must_haves:
  truths:
    - "After a file loads, its waveform renders in the middle band (AudioThumbnail drawChannels)"
    - "Dragging horizontally on the waveform selects a region, shown as a highlighted range with dimmed outside areas"
    - "With no drag performed (or after a plain click), the selected region equals the whole file"
    - "A drag beyond the waveform edges produces a region clamped to [0, totalLength]"
    - "Region changes reach processor.setSelectedRegion and land as apvts.state properties"
    - "A failed load shows an error message in the waveform band instead of crashing or silently doing nothing"
  artifacts:
    - path: "Source/UI/WaveformMath.h"
      provides: "Pure timeToX/xToTime conversion (free functions, unit-tested)"
      contains: "xToTime"
    - path: "Source/UI/RegionSelectionModel.h"
      provides: "Pure drag-selection state machine: whole-file default, clamping, normalization, click-resets-to-whole-file"
      contains: "struct RegionSelectionModel"
    - path: "Source/UI/WaveformView.h"
      provides: "AudioThumbnail + AudioThumbnailCache wrapper with setSource/error/empty states"
      contains: "AudioThumbnail"
    - path: "Source/UI/RegionSelectorOverlay.h"
      provides: "Transparent mouse-handling overlay painting the selection, onRegionChanged callback"
      contains: "onRegionChanged"
    - path: "Tests/WaveformRegionTests.cpp"
      provides: "PixelTimeConversion, DefaultWholeFile, DragSelectionClamped, ClickResetsToWholeFile, ThumbnailPopulates tests"
      contains: "DragSelectionClamped"
  key_links:
    - from: "Source/PluginEditor.cpp"
      to: "Source/PluginProcessor.h"
      via: "editor is ChangeListener on processor.loadBroadcaster; callback pulls getLoadedAudio()"
      pattern: "loadBroadcaster.addChangeListener"
    - from: "Source/PluginEditor.cpp"
      to: "Source/UI/WaveformView.h"
      via: "waveformView.setSource(loaded->sourceFile) on load complete"
      pattern: "setSource"
    - from: "Source/UI/RegionSelectorOverlay.cpp"
      to: "Source/PluginProcessor.h"
      via: "onRegionChanged lambda calls processor.setSelectedRegion"
      pattern: "setSelectedRegion"
---

<objective>
Deliver IMP-02 (waveform display) and IMP-03 (region selection, whole-file default): a WaveformView over AudioThumbnail in the editor's middle band, a RegionSelectorOverlay with unit-tested pure selection logic, and the full wiring loop — load completes → waveform appears → user drags a region → processor state updates.

Purpose: completes the phase's interactive core. After this plan, everything Phase 3 needs (decoded buffer + selected region) is user-drivable.
Output: waveform + region selection working in all formats; WaveformRegionTests green; state properties updating on selection.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-audio-import-waveform/02-RESEARCH.md
@.planning/phases/02-audio-import-waveform/02-01-SUMMARY.md
@.planning/phases/02-audio-import-waveform/02-02-SUMMARY.md
@Source/PluginEditor.h
@Source/PluginEditor.cpp

<interfaces>
<!-- Contracts from Plans 02-01/02-02 — use directly, no exploration needed. -->

From Source/PluginProcessor.h (Plan 02-01):
```cpp
void loadAudioFile (const juce::File& file);
std::shared_ptr<const LoadedAudio> getLoadedAudio() const;   // nullptr until first successful load
juce::Range<double> getSelectedRegion() const;               // {0, lengthSeconds} default after load
void setSelectedRegion (juce::Range<double> regionSeconds);  // clamps + writes apvts.state props
juce::String getLastLoadError() const;                       // non-empty after a failed load
juce::ChangeBroadcaster loadBroadcaster;                     // sendChangeMessage on complete AND failure
```

From Source/Import/LoadedAudio.h (Plan 02-01):
```cpp
struct LoadedAudio { juce::File sourceFile; juce::AudioBuffer<float> buffer;
                     double sampleRate; double lengthSeconds; };
```

From Source/PluginEditor (Plan 02-02):
- `ConveyorBeltComponent conveyor;` with `triggerChunkFallStub()`; `juce::Rectangle<int> waveformArea` member = middle band bounds set in resized()
- Editor size 800x520; bands: conveyor top 120 px, bottom placeholder 140 px, middle ~260 px

Test target facts (Plan 02-01): ChordAITests console app; `JUCE_MODAL_LOOPS_PERMITTED=1` already defined (enables MessageManager::runDispatchLoopUntil); `CHORDAI_FIXTURES_DIR` macro = absolute path to Tests/fixtures; catch_discover_tests TEST_PREFIX "ChordAITests.".

JUCE pattern (research Pattern 2, from vendored examples/Audio/AudioPlaybackDemo.h): AudioThumbnailCache{5} + AudioThumbnail{512, formatManager, cache}; setSource(new FileInputSource(file)); thumbnail is a ChangeBroadcaster — listen and repaint; drawChannels(g, bounds, startTime, endTime, 1.0f).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure selection/conversion logic (WaveformMath + RegionSelectionModel) with unit tests</name>
  <files>Source/UI/WaveformMath.h, Source/UI/RegionSelectionModel.h, Tests/WaveformRegionTests.cpp, CMakeLists.txt</files>
  <behavior>
    Tests first (Tests/WaveformRegionTests.cpp, added to ChordAITests target_sources; both headers are header-only so no source additions needed):
    - "WaveformRegionTests.PixelTimeConversion": with visibleRange {0,10} and width 800 — timeToX(5.0) == 400.0f; xToTime(400.0f) == 5.0; xToTime(timeToX(t)) round-trips within 1e-9 for t in {0, 3.3, 10}; degenerate visibleRange (length 0) returns 0 from timeToX without dividing by zero.
    - "WaveformRegionTests.DefaultWholeFile": model.setTotalLength(30.0) → getRegion() == {0.0, 30.0} and isWholeFile() == true, with no mouse interaction (IMP-03 default).
    - "WaveformRegionTests.DragSelectionClamped": setTotalLength(10); beginDrag(2.0); dragTo(8.0); endDrag() → {2,8}. beginDrag(5.0); dragTo(-3.0) → normalized+clamped {0,5}. beginDrag(9.0); dragTo(25.0) → {9,10}. Right-to-left drags normalize (start < end always).
    - "WaveformRegionTests.ClickResetsToWholeFile": after a {2,8} selection, beginDrag(4.0); dragTo(4.01); endDrag() with selection length < 0.05 s → region resets to {0,10}, isWholeFile() == true (a click/micro-drag clears the selection back to the default).
  </behavior>
  <action>
    RED first, then implement two header-only files (no JUCE Component dependency — pure logic, message-thread agnostic):
    1. Source/UI/WaveformMath.h — free functions mirroring the vendored AudioPlaybackDemo pattern but stateless/testable:
       ```cpp
       float  timeToX (double time, juce::Range<double> visibleRange, int width);
       double xToTime (float x,     juce::Range<double> visibleRange, int width);
       ```
       Guard the zero-length-range case (return 0.0f / visibleRange.getStart()).
    2. Source/UI/RegionSelectionModel.h — struct with: setTotalLength(double) (stores length, resets region to {0,length}); beginDrag(double t), dragTo(double t) (live region = normalized min/max of anchor and t, clamped to [0,total]); endDrag() (if region length < 0.05 s → reset to whole file); getRegion(); isWholeFile() (region == {0,total} within 1e-9). All double-precision seconds; no pixels — the overlay converts via WaveformMath.
    3. GREEN: build ChordAITests + run. Commit tests first, implementation second.
  </action>
  <verify>
    <automated>ctest --test-dir build -R WaveformRegionTests --output-on-failure</automated>
  </verify>
  <done>All four pure-logic test cases green; both headers compile with no Component/GUI includes beyond juce_core types.</done>
</task>

<task type="auto">
  <name>Task 2: WaveformView + RegionSelectorOverlay components (+ ThumbnailPopulates test)</name>
  <files>Source/UI/WaveformView.h, Source/UI/WaveformView.cpp, Source/UI/RegionSelectorOverlay.h, Source/UI/RegionSelectorOverlay.cpp, Tests/WaveformRegionTests.cpp, CMakeLists.txt</files>
  <action>
    1. WaveformView : public juce::Component, public juce::ChangeListener (add .cpp to ChordAI target_sources only):
       - Members: `juce::AudioFormatManager formatManager;` (registerBasicFormats in ctor — own instance keeps the component self-contained; thumbnail scanning is independent of the processor's decode, per research Pitfall 3), `juce::AudioThumbnailCache thumbnailCache {5};`, `juce::AudioThumbnail thumbnail {512, formatManager, thumbnailCache};` (declaration order matters: cache before thumbnail).
       - `void setSource (const juce::File& f)`: thumbnail.setSource(new juce::FileInputSource(f)); clear errorMessage; repaint. `void setErrorMessage (juce::String msg)`: store + repaint. `double getTotalLength() const` → thumbnail.getTotalLength().
       - ctor: thumbnail.addChangeListener(this); changeListenerCallback → repaint() (thumbnail broadcasts as its async scan progresses — the waveform "fills in" live).
       - paint(): fill 0xff14141c. If errorMessage non-empty → centered dimmed red-ish text (e.g. 0xffc85050). Else if getTotalLength() <= 0 → dimmed hint "DROP A TRACK ONTO THE CONVEYOR". Else → g.setColour(0xff7ec8c8-style accent); thumbnail.drawChannels(g, getLocalBounds().reduced(4), 0.0, thumbnail.getTotalLength(), 0.95f).
    2. RegionSelectorOverlay : public juce::Component (transparent; sits exactly on top of WaveformView; add .cpp to ChordAI target_sources only):
       - Members: `RegionSelectionModel model;` public `std::function<void (juce::Range<double>)> onRegionChanged;`
       - `void setTotalLength (double s)`: model.setTotalLength(s); repaint; fire onRegionChanged(model.getRegion()) so the whole-file default is registered downstream.
       - mouseDown/mouseDrag/mouseUp: convert e.x via `xToTime((float) e.x, {0.0, model total length}, getWidth())`, drive model.beginDrag/dragTo/endDrag; repaint on each; on mouseUp fire onRegionChanged(model.getRegion()).
       - paint(): nothing when isWholeFile() (whole file selected = no visual clutter — the default state looks clean). Otherwise: dim the areas OUTSIDE the region (fill 0x99000000), draw 1-px bright edges (0xffe8c547) at region start/end x-positions via timeToX, and a subtle 0x22e8c547 tint inside the region.
       - setInterceptsMouseClicks(true, false); the view below never needs mouse events.
    3. Append to Tests/WaveformRegionTests.cpp: "WaveformRegionTests.ThumbnailPopulates" — juce::ScopedJuceInitialiser_GUI scope; AudioFormatManager + registerBasicFormats; AudioThumbnailCache(5) + AudioThumbnail(512,...); write a 1 s WAV fixture to a temp file (same helper as AudioFileLoaderTests); setSource(new FileInputSource(wav)); loop `MessageManager::getInstance()->runDispatchLoopUntil (50)` until thumbnail.isFullyLoaded() or 5 s timeout; REQUIRE(thumbnail.getTotalLength() > 0.9). (JUCE_MODAL_LOOPS_PERMITTED=1 is already set on the test target — Plan 02-01.) This test exercises the same AudioThumbnail mechanism WaveformView uses, without instantiating the component.
    4. Build + run full ctest.
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build -R WaveformRegionTests --output-on-failure</automated>
  </verify>
  <done>Both components compile into the plugin; ThumbnailPopulates green (getTotalLength > 0.9 after pumped message loop); overlay logic delegates entirely to the tested model + math headers.</done>
</task>

<task type="auto">
  <name>Task 3: Editor wiring — load completion to waveform, selection to processor state</name>
  <files>Source/PluginEditor.h, Source/PluginEditor.cpp, CMakeLists.txt</files>
  <action>
    1. PluginEditor.h: editor additionally inherits `public juce::ChangeListener`; add members `WaveformView waveformView;` and `RegionSelectorOverlay regionOverlay;`; declare a real destructor (must removeChangeListener).
    2. PluginEditor.cpp:
       - ctor: addAndMakeVisible(waveformView); addAndMakeVisible(regionOverlay) AFTER it (overlay on top, z-order). `processor.loadBroadcaster.addChangeListener (this);` Wire `regionOverlay.onRegionChanged = [this] (juce::Range<double> r) { processor.setSelectedRegion (r); };`
       - IMPORTANT editor-reopen case (DAW closes/reopens editor while processor keeps state): at end of ctor, if `processor.getLoadedAudio() != nullptr`, immediately run the same load-complete path below so the waveform + region restore without a re-drop.
       - dtor: `processor.loadBroadcaster.removeChangeListener (this);` (dangling-listener crash on editor close otherwise).
       - changeListenerCallback(): `if (auto loaded = processor.getLoadedAudio(); loaded != nullptr && processor.getLastLoadError().isEmpty()) { waveformView.setSource (loaded->sourceFile); regionOverlay.setTotalLength (loaded->lengthSeconds); conveyor.triggerChunkFallStub(); } else if (processor.getLastLoadError().isNotEmpty()) { waveformView.setErrorMessage (processor.getLastLoadError()); }` — the stub trigger is the placeholder "something came off the belt" feedback, per locked scope (visual stub only).
       - resized(): waveformView.setBounds(waveformArea); regionOverlay.setBounds(waveformArea); remove the Plan 02-02 hint text from editor paint() (WaveformView owns the empty-state hint now).
    3. Full wave regression gate (per 02-VALIDATION.md): `cmake --build build`; full ctest; pluginval strictness 5 `--validate-in-process` on both `$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3` and `$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component` (both SUCCESS — the ChangeListener add/remove pattern is exactly what pluginval's repeated editor open/close catches); `tools/smoke-test-standalone.sh` PASS.
  </action>
  <verify>
    <automated>ctest --test-dir build -R ChordAITests --output-on-failure && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3" && tools/smoke-test-standalone.sh</automated>
  </verify>
  <done>Load→waveform→select→state loop fully wired; editor reopen restores waveform from processor state; load errors display; pluginval strictness 5 SUCCESS both formats; smoke test PASS.</done>
</task>

</tasks>

<verification>
- Full ChordAITests suite green: Infrastructure + AudioFileLoader (5) + RegionState (4) + WaveformRegion (5 incl. ThumbnailPopulates)
- pluginval strictness 5 SUCCESS on VST3 + AU (repeated editor open/close with ChangeListener wiring)
- tools/smoke-test-standalone.sh PASS
- grep `removeChangeListener` present in the editor destructor (dangling-listener guard)
- grep confirms processBlock untouched
</verification>

<success_criteria>
- IMP-02: loaded file's waveform renders (thumbnail fills in progressively during scan)
- IMP-03: drag selects a clamped region; click/micro-drag resets to whole file; whole file is the default with zero interaction; selection lands in apvts.state via setSelectedRegion
- Load failure is visible (error text), not a silent no-op or crash
- Editor destroy/recreate cycle is safe and state-restoring
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-import-waveform/02-03-SUMMARY.md`
</output>
