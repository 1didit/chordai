---
phase: 02-audio-import-waveform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - Tests/InfrastructureTests.cpp
  - Tests/fixtures/silence_1s.mp3
  - Tests/AudioFileLoaderTests.cpp
  - Tests/RegionStateTests.cpp
  - Source/Import/LoadedAudio.h
  - Source/Import/AudioFileLoader.h
  - Source/Import/AudioFileLoader.cpp
  - Source/Import/RegionState.h
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
autonomous: true
requirements: [IMP-01]

must_haves:
  truths:
    - "ctest --test-dir build -R ChordAITests runs a Catch2 suite and exits green"
    - "A WAV, AIFF, and FLAC file each decode to a non-empty AudioBuffer via loadAudioFileSync (fixtures written by JUCE writers at test setup)"
    - "The committed MP3 fixture decodes via CoreAudioFormat to ~1s of audio"
    - "An unsupported file (.txt) returns nullptr from loadAudioFileSync without crashing"
    - "PluginProcessor::loadAudioFile decodes on a ThreadPool background thread and publishes the result to the message thread"
    - "Loaded file path and selected region live as custom non-parameter properties on apvts.state and survive an XML round-trip"
  artifacts:
    - path: "CMakeLists.txt"
      provides: "Catch2 v3.7.1 via FetchContent + ChordAITests console-app target + CTest wiring + explicit juce_audio_formats link"
      contains: "catch_discover_tests"
    - path: "Tests/fixtures/silence_1s.mp3"
      provides: "Committed MP3 decode fixture (JUCE cannot write MP3 — CoreAudioFormat::createWriterFor is a stub)"
    - path: "Source/Import/LoadedAudio.h"
      provides: "Immutable value type: sourceFile + decoded AudioBuffer<float> + sampleRate + lengthSeconds"
      contains: "struct LoadedAudio"
    - path: "Source/Import/AudioFileLoader.h"
      provides: "loadAudioFileSync (testable core) + AudioFileLoadJob (ThreadPoolJob async wrapper)"
      exports: ["loadAudioFileSync", "AudioFileLoadJob"]
    - path: "Source/Import/RegionState.h"
      provides: "Pure helpers writing/reading sourceFilePath + regionStart/EndSeconds properties on a ValueTree, plus region clamping"
      contains: "regionStartSeconds"
    - path: "Source/PluginProcessor.h"
      provides: "loadAudioFile / getLoadedAudio / get+setSelectedRegion / getLastLoadError / loadBroadcaster public API for the editor"
      contains: "loadBroadcaster"
  key_links:
    - from: "Source/PluginProcessor.cpp"
      to: "Source/Import/AudioFileLoader.h"
      via: "loaderPool.addJob(new AudioFileLoadJob(...), true)"
      pattern: "addJob"
    - from: "Source/Import/AudioFileLoader.cpp"
      to: "message thread"
      via: "MessageManager::callAsync delivering shared_ptr<const LoadedAudio>"
      pattern: "callAsync"
    - from: "Source/PluginProcessor.cpp"
      to: "apvts.state"
      via: "RegionState::write on load complete and on setSelectedRegion"
      pattern: "RegionState::write"
---

<objective>
Wire the project's first test infrastructure (Catch2 + CTest — the Wave 0 gap flagged in 02-VALIDATION.md) and build the audio-import backend: a background-decoded LoadedAudio published to the message thread, with file path + analysis region persisted as custom APVTS state properties for Phase 7.

Purpose: IMP-01's decode path for all four formats (WAV/MP3/AIFF/FLAC), proven by unit tests before any UI exists. Every later plan in this phase builds on this processor API and this test harness.
Output: green ChordAITests suite (decode + region-state tests), committed MP3 fixture, PluginProcessor with a load/region API the editor consumes in Plans 02-02/02-03.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-audio-import-waveform/02-RESEARCH.md
@.planning/phases/02-audio-import-waveform/02-VALIDATION.md
@CMakeLists.txt
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp

<interfaces>
<!-- Existing contracts (Phase 1) — use directly, no exploration needed. -->

From Source/PluginProcessor.h:
```cpp
class ChordAIAudioProcessor : public juce::AudioProcessor
{
public:
    juce::AudioProcessorValueTreeState apvts;   // public; state tree type "PARAMETERS"; empty ParameterLayout
    // getStateInformation/setStateInformation already round-trip apvts.state as XML
};
```

Build facts (from CMakeLists.txt / Phase 1 summaries):
- Build dir: `build/` (Ninja). Rebuild: `cmake --build build`. Configure: `cmake -B build -G Ninja`
- Currently linked: juce::juce_audio_utils, juce::juce_audio_processors, juce::juce_gui_extra
- juce_audio_formats is ALREADY transitively linked via juce_audio_utils (verified in research — Pitfall 1). Adding it explicitly documents the dependency; it does not "unlock" anything.
- Target compile defs: JUCE_WEB_BROWSER=0 JUCE_USE_CURL=0 JUCE_VST3_CAN_REPLACE_VST2=0

Machine facts (verified during planning, 2026-07-12):
- `afconvert` CANNOT encode MP3 (CoreAudio has no MP3 encoder — MPG3 is decode/container listing only). Do not attempt it.
- `/opt/homebrew/bin/ffmpeg` exists with libmp3lame; this exact command was test-run successfully:
  `ffmpeg -hide_banner -loglevel error -f lavfi -i anullsrc=r=44100:cl=mono -t 1 -codec:a libmp3lame -b:a 128k -y Tests/fixtures/silence_1s.mp3`
- The resulting MP3 decodes via CoreAudio at ~1.045 s (encoder padding) — decode tests must use tolerance [0.9, 1.2] s, not == 1.0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Catch2 + CTest into CMake, commit the MP3 fixture, link juce_audio_formats explicitly</name>
  <files>CMakeLists.txt, Tests/InfrastructureTests.cpp, Tests/fixtures/silence_1s.mp3</files>
  <action>
    All work in /Users/test/Documents/prjcts/chordai.

    1. CMakeLists.txt — append after the existing ChordAI target setup:
       - Add `juce::juce_audio_formats` to the existing ChordAI target_link_libraries (explicit documentation of a real dependency; already transitive via juce_audio_utils — expect NO behavior change).
       - `enable_testing()` + `include(FetchContent)`.
       - `FetchContent_Declare(Catch2 GIT_REPOSITORY https://github.com/catchorg/Catch2.git GIT_TAG v3.7.1)` + `FetchContent_MakeAvailable(Catch2)`.
       - `juce_add_console_app(ChordAITests PRODUCT_NAME "ChordAITests")` — a console app target, NOT a second plugin. Catch2WithMain supplies main(); JUCE console apps do not generate one.
       - `target_sources(ChordAITests PRIVATE Tests/InfrastructureTests.cpp)`.
       - `target_compile_definitions(ChordAITests PRIVATE JUCE_WEB_BROWSER=0 JUCE_USE_CURL=0 JUCE_MODAL_LOOPS_PERMITTED=1 CHORDAI_FIXTURES_DIR="${CMAKE_CURRENT_SOURCE_DIR}/Tests/fixtures")` — MODAL_LOOPS enables MessageManager::runDispatchLoopUntil needed by Plan 02-03's thumbnail test; FIXTURES_DIR gives tests an absolute fixture path.
       - `target_link_libraries(ChordAITests PRIVATE Catch2::Catch2WithMain juce::juce_audio_formats juce::juce_audio_utils juce::juce_gui_basics)` (juce_core/juce_events/juce_graphics come transitively).
       - `juce_generate_juce_header(ChordAITests)` so test-target sources can `#include <JuceHeader.h>` like the rest of the codebase.
       - `list(APPEND CMAKE_MODULE_PATH ${catch2_SOURCE_DIR}/extras)` + `include(CTest)` + `include(Catch)` + `catch_discover_tests(ChordAITests TEST_PREFIX "ChordAITests.")` — the prefix makes `ctest -R ChordAITests` select the whole suite while `ctest -R AudioFileLoaderTests.WavDecode` still matches individual cases (validation-contract commands).
    2. Tests/InfrastructureTests.cpp — one sanity TEST_CASE named "Infrastructure.CatchAndJuceLink": REQUIRE juce::SystemStats::getJUCEVersion() is non-empty and starts with "JUCE v8". Proves Catch2 + JUCE headers + link all work before real tests exist.
    3. Generate the MP3 fixture ONCE and commit it (JUCE cannot write MP3 — CoreAudioFormat::createWriterFor is `jassertfalse; return nullptr;` per research; afconvert also cannot encode MP3):
       `mkdir -p Tests/fixtures && ffmpeg -hide_banner -loglevel error -f lavfi -i anullsrc=r=44100:cl=mono -t 1 -codec:a libmp3lame -b:a 128k -y Tests/fixtures/silence_1s.mp3`
       Then verify: `afinfo Tests/fixtures/silence_1s.mp3` must report "File type ID: MPG3" and an estimated duration ~1.0-1.1 s. Confirm .gitignore does not exclude it (it's ~17 KB; commit the binary).
    4. Reconfigure + build + run: `cmake -B build -G Ninja && cmake --build build --target ChordAITests && ctest --test-dir build -R ChordAITests --output-on-failure`. Also `cmake --build build` (full) to prove the plugin targets still build with the explicit juce_audio_formats link.
  </action>
  <verify>
    <automated>ctest --test-dir build -R ChordAITests --output-on-failure</automated>
  </verify>
  <done>ChordAITests target builds, the sanity test passes under ctest, silence_1s.mp3 exists and afinfo confirms MPG3, full plugin build still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LoadedAudio + AudioFileLoader (sync core + ThreadPoolJob wrapper) with decode tests for all four formats</name>
  <files>Source/Import/LoadedAudio.h, Source/Import/AudioFileLoader.h, Source/Import/AudioFileLoader.cpp, Tests/AudioFileLoaderTests.cpp, CMakeLists.txt</files>
  <behavior>
    Tests first (Tests/AudioFileLoaderTests.cpp, added to ChordAITests target_sources; AudioFileLoader.cpp also added to ChordAITests sources):
    - "AudioFileLoaderTests.WavDecode": write a 1 s, 440 Hz sine, 44.1 kHz stereo WAV into a juce::TemporaryFile-style temp dir using WavAudioFormat::createWriterFor at test setup → loadAudioFileSync returns non-null; sampleRate == 44100; buffer.getNumChannels() == 2; buffer.getNumSamples() == 44100; lengthSeconds ≈ 1.0 (±0.01).
    - "AudioFileLoaderTests.AiffDecode": same via AiffAudioFormat writer.
    - "AudioFileLoaderTests.FlacDecode": same via FlacAudioFormat writer (16-bit — FLAC writer does not accept 32-bit float).
    - "AudioFileLoaderTests.Mp3Decode": load CHORDAI_FIXTURES_DIR "/silence_1s.mp3" → non-null; sampleRate == 44100; lengthSeconds in [0.9, 1.2] (MP3 encoder padding — measured 1.045 s at plan time).
    - "AudioFileLoaderTests.UnsupportedFileFails": a temp .txt file with garbage bytes → loadAudioFileSync returns nullptr; no crash, no assert.
    All tests construct their own juce::AudioFormatManager and call registerBasicFormats().
  </behavior>
  <action>
    RED first: write the tests above against the not-yet-existing header, confirm compile failure, then implement:

    1. Source/Import/LoadedAudio.h — plain immutable value struct (matches research Pattern 1, minus selectedRegion — mutable selection state lives on the processor, NOT inside a shared const object):
       ```cpp
       struct LoadedAudio
       {
           juce::File sourceFile;
           juce::AudioBuffer<float> buffer;   // native channel count and sample rate, no downmix/resample
           double sampleRate = 0.0;
           double lengthSeconds = 0.0;
       };
       ```
    2. Source/Import/AudioFileLoader.h/.cpp:
       - Free function `std::shared_ptr<const LoadedAudio> loadAudioFileSync (const juce::File&, juce::AudioFormatManager&)` — the testable core: createReaderFor → nullptr check → setSize + reader->read(...) → fill fields → return. Returns nullptr on unsupported/corrupt file.
       - `class AudioFileLoadJob : public juce::ThreadPoolJob` — thin async wrapper: ctor takes (juce::File, juce::AudioFormatManager&, std::function<void(std::shared_ptr<const LoadedAudio>, juce::String errorMessage)>). runJob(): shouldExit() check, call loadAudioFileSync, then `juce::MessageManager::callAsync` delivering EITHER the result OR (nullptr, "Couldn't load <filename>") so load failure is observable (research Open Question 3: decided — surface the error, no silent no-op). Job name "AudioFileLoad".
    3. Include via `#include <JuceHeader.h>` per existing codebase pattern. Add AudioFileLoader.cpp to BOTH targets' sources in CMakeLists.txt (ChordAI and ChordAITests); headers need no target entry.
    4. GREEN: build + ctest until all five decode tests pass. Commit tests and implementation separately (test commit first) per TDD discipline.
  </action>
  <verify>
    <automated>ctest --test-dir build -R AudioFileLoaderTests --output-on-failure</automated>
  </verify>
  <done>All five AudioFileLoaderTests cases green; loadAudioFileSync handles WAV/AIFF/FLAC/MP3 and rejects garbage gracefully; AudioFileLoadJob compiles into the plugin target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Processor load/region API + APVTS custom state properties (RegionState helpers)</name>
  <files>Source/Import/RegionState.h, Tests/RegionStateTests.cpp, Source/PluginProcessor.h, Source/PluginProcessor.cpp, CMakeLists.txt</files>
  <behavior>
    Tests first (Tests/RegionStateTests.cpp, added to ChordAITests sources — pure ValueTree logic, no processor instantiation needed):
    - "RegionStateTests.WriteReadRoundtrip": ValueTree("PARAMETERS"); RegionState::write(state, File("/tmp/x.wav"), {1.5, 42.0}) → readSourceFile returns the same path, readRegion returns {1.5, 42.0}.
    - "RegionStateTests.XmlSurvival": after write, state.createXml() → ValueTree::fromXml → properties still read back identically (proves the Phase 7 persistence path: these are non-parameter properties that ride along getStateInformation/setStateInformation).
    - "RegionStateTests.ClampRegion": clampRegion({-5.0, 999.0}, 10.0) == {0.0, 10.0}; clampRegion with inverted input ({8.0, 2.0}) returns normalized {2.0, 8.0}; clampRegion({0,0}, 10.0) == {0.0, 10.0} (empty → whole file).
    - "RegionStateTests.ReadDefaults": reading a tree with no properties returns an empty File and an empty Range (caller decides the default).
  </behavior>
  <action>
    1. Source/Import/RegionState.h — header-only namespace `RegionState` with property ids as inline constexpr identifiers: `sourceFilePath`, `regionStartSeconds`, `regionEndSeconds`; functions `write(juce::ValueTree&, const juce::File&, juce::Range<double>)`, `readSourceFile(const juce::ValueTree&)`, `readRegion(const juce::ValueTree&)`, `clampRegion(juce::Range<double>, double totalLengthSeconds)` (normalize inverted, clamp to [0,total], empty → {0,total}). Uses setProperty with nullptr UndoManager.
    2. PluginProcessor.h — add members and API (message-thread usage only in Phase 2; the audio thread never touches any of this, preserving processBlock RT-safety untouched):
       ```cpp
       void loadAudioFile (const juce::File& file);                        // kicks background decode
       std::shared_ptr<const LoadedAudio> getLoadedAudio() const;          // std::atomic_load on the member
       juce::Range<double> getSelectedRegion() const;                      // seconds; {0,length} default
       void setSelectedRegion (juce::Range<double> regionSeconds);         // clamps via RegionState, mirrors to apvts.state
       juce::String getLastLoadError() const;                              // empty when last load succeeded
       juce::ChangeBroadcaster loadBroadcaster;                            // fires on load complete AND on load failure
       private:
       juce::AudioFormatManager formatManager;                             // registerBasicFormats() in ctor
       juce::ThreadPool loaderPool { 1 };                                  // single decode worker
       std::shared_ptr<const LoadedAudio> loadedAudio;                     // access ONLY via std::atomic_load/atomic_store
       juce::Range<double> selectedRegion;                                 // message-thread only
       juce::String lastLoadError;
       ```
       Use the free functions std::atomic_load/std::atomic_store on the shared_ptr (NOT std::atomic<std::shared_ptr<T>> — incomplete in Apple libc++). This satisfies the atomic-swap publication pattern portably.
    3. PluginProcessor.cpp:
       - ctor: `formatManager.registerBasicFormats();`
       - loadAudioFile: `loaderPool.addJob(new AudioFileLoadJob(file, formatManager, callback), true)`. The callback (runs on message thread via callAsync): on success — atomic_store loadedAudio; selectedRegion = {0, lengthSeconds} (whole-file default, IMP-03); lastLoadError.clear(); `RegionState::write(apvts.state, file, selectedRegion)`; loadBroadcaster.sendChangeMessage(). On failure — lastLoadError = message; loadBroadcaster.sendChangeMessage().
       - setSelectedRegion: clamp against getLoadedAudio()->lengthSeconds (no-op if nothing loaded); update selectedRegion; RegionState::write with the current source file; do NOT broadcast (the UI initiated the change).
       - Destructor consideration: loaderPool member declared AFTER apvts so it is destroyed FIRST (JUCE ThreadPool dtor waits for jobs); the job's callAsync capture holds shared state by value — capture the callback + result only, never `this`, OR capture `this` guarded by a `juce::Component::SafePointer`-equivalent: simplest safe pattern — have AudioFileLoadJob's callback capture a `std::weak_ptr` handle. Concretely: give the processor a `std::shared_ptr<int> aliveToken = std::make_shared<int>(0)` member; the callback captures `std::weak_ptr<int>` of it and bails if expired. Document this in a comment.
    4. Build the full project and run ALL tests. Then run the per-wave regression gate (validation contract):
       - `cmake --build build`
       - `tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3"` → must end SUCCESS
       - same for `"$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component"` → must end SUCCESS
  </action>
  <verify>
    <automated>ctest --test-dir build -R ChordAITests --output-on-failure && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3"</automated>
  </verify>
  <done>RegionStateTests green; full ChordAITests suite green; plugin builds all three formats; pluginval strictness 5 SUCCESS on VST3 and AU with the new backend code linked in.</done>
</task>

</tasks>

<verification>
- `ctest --test-dir build -R ChordAITests --output-on-failure` — full suite green (Infrastructure + AudioFileLoader x5 + RegionState x4)
- `cmake --build build` — VST3/AU/Standalone all still build
- pluginval strictness 5 SUCCESS on both installed VST3 and AU (wave regression gate per 02-VALIDATION.md)
- `afinfo Tests/fixtures/silence_1s.mp3` reports MPG3; file is committed to git
- grep confirms processBlock unchanged (RT-safety discipline intact — no new code touches the audio thread)
</verification>

<success_criteria>
- Catch2 + CTest wired for the first time; validation contract commands (`ctest -R ChordAITests`, `ctest -R AudioFileLoaderTests.WavDecode`) work as written
- IMP-01 decode path proven by unit tests for WAV/AIFF/FLAC (generated fixtures) and MP3 (committed fixture via CoreAudioFormat)
- Processor exposes the exact load/region API Plans 02-02 and 02-03 consume (loadAudioFile, getLoadedAudio, get/setSelectedRegion, getLastLoadError, loadBroadcaster)
- File path + region persisted as apvts.state custom properties, XML-round-trip proven
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-import-waveform/02-01-SUMMARY.md`
</output>
