---
phase: 02-audio-import-waveform
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified: []
autonomous: false
requirements: [IMP-01, IMP-02, IMP-03]

must_haves:
  truths:
    - "All four formats (WAV/MP3/AIFF/FLAC) load via real OS drag-and-drop onto the conveyor in the Standalone app"
    - "The waveform is visible and legible after each load"
    - "Region selection works by hand: drag selects, click resets to whole file"
    - "A 3+ minute file decodes without freezing the UI — the conveyor animation keeps running throughout"
    - "Full automated gate green with all Phase 2 code: ctest suite, pluginval strictness 5 (VST3+AU), standalone smoke test"
  artifacts: []
  key_links: []
---

<objective>
Phase 2 gate: run the complete automated regression suite one final time against the finished phase, then a human verifies the three manual-only behaviors that 02-VALIDATION.md justified as non-automatable (OS-level drag delivery, waveform legibility, message-thread responsiveness under real decode load).

Purpose: IMP-01/02/03 acceptance evidence before /gsd:verify-work. Full per-DAW drag matrix remains Phase 7's job (PLT-03) — this checkpoint only confirms the interactions work in Standalone.
Output: green full gate + human sign-off on the manual matrix.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-audio-import-waveform/02-VALIDATION.md
@.planning/phases/02-audio-import-waveform/02-01-SUMMARY.md
@.planning/phases/02-audio-import-waveform/02-02-SUMMARY.md
@.planning/phases/02-audio-import-waveform/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated regression gate + generate manual-test audio files</name>
  <files>none (verification + temp fixtures only)</files>
  <action>
    From /Users/test/Documents/prjcts/chordai:
    1. Incremental build: `cmake --build build` — all targets (VST3/AU/Standalone/ChordAITests) compile.
    2. Full test suite: `ctest --test-dir build -R ChordAITests --output-on-failure` — every case green (Infrastructure, AudioFileLoader x5, RegionState x4, WaveformRegion x5).
    3. pluginval strictness 5, both formats, both must end SUCCESS:
       `tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3"`
       `tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component"`
    4. `tools/smoke-test-standalone.sh` — PASS.
    5. Generate manual-test files for the checkpoint into /tmp/chordai-manual-fixtures/ (so the user doesn't have to hunt for AIFF/FLAC files):
       - Write a 200-second stereo 44.1 kHz WAV via python3 stdlib (wave module; a slow sine sweep or alternating tone blocks — anything visibly non-flat on a waveform): `/tmp/chordai-manual-fixtures/long_test.wav` (~3.3 min — exercises the responsiveness check).
       - Also a short 10 s WAV `short_test.wav`, then convert: `afconvert short_test.wav -f AIFF -d BEI16 short_test.aiff` and `afconvert short_test.wav -f flac -d flac short_test.flac`.
       - MP3: afconvert cannot encode MP3 — use `ffmpeg -hide_banner -loglevel error -i short_test.wav -codec:a libmp3lame -b:a 192k short_test.mp3` (ffmpeg verified present at /opt/homebrew/bin/ffmpeg).
       - Verify all four exist and `afinfo` reads each one.
    6. Launch the Standalone app for the user: `open "$(find build -type d -name 'ChordAI.app' -print -quit)"`.
  </action>
  <verify>
    <automated>ctest --test-dir build -R ChordAITests --output-on-failure && tools/smoke-test-standalone.sh && ls /tmp/chordai-manual-fixtures/short_test.mp3 /tmp/chordai-manual-fixtures/short_test.aiff /tmp/chordai-manual-fixtures/short_test.flac /tmp/chordai-manual-fixtures/long_test.wav</automated>
  </verify>
  <done>Full automated gate green (ctest + pluginval x2 + smoke test); four manual-test files exist and are CoreAudio-readable; Standalone is running for the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification — drag-and-drop matrix, waveform, region selection, responsiveness</name>
  <what-built>Phase 2 complete: pixel-art conveyor belt editor (drop target), background audio import for WAV/MP3/AIFF/FLAC, waveform display, drag-to-select analysis region with whole-file default. Test files prepared in /tmp/chordai-manual-fixtures/.</what-built>
  <how-to-verify>
    In the running ChordAI Standalone window (relaunch anytime: open the app found under build/ChordAI_artefacts):

    1. Conveyor idle state: the top strip shows a chunky pixel-art belt animating LEFT→RIGHT continuously. Bottom band shows the dimmed "MIDI SETS…" placeholder.
    2. Drag /tmp/chordai-manual-fixtures/short_test.wav from Finder over the conveyor strip — the belt highlights while hovering. Drop it: within a moment the waveform appears in the middle band (visible peaks, not a flat line or garbage).
    3. Repeat the drop with short_test.mp3, short_test.aiff, short_test.flac — each loads and shows a waveform. (This is IMP-01's manual-only half: OS drag actually delivering files.)
    4. Region selection (IMP-03): click-drag horizontally across part of the waveform — a highlighted region with bright edges appears, areas outside dim. Drag past the left/right edge — selection clamps to the file bounds. Single-click (no drag) — the selection clears back to whole-file (no highlight shown).
    5. Responsiveness: drop long_test.wav (3.3 min). While it decodes, the conveyor belt must keep animating smoothly and the window must stay draggable/responsive — no freeze, no beachball.
    6. Negative case: drag a non-audio file (e.g. any .txt) over the conveyor — no highlight appears and dropping does nothing.
    7. Optional extra: drop one of YOUR real songs (any MP3/WAV) — waveform should look like the actual track.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass, or describe exactly which step failed and what you saw.</resume-signal>
  <files></files>
  <action>Present the verification checklist above to the user and PAUSE. Do not attempt the checks yourself — OS-level drag simulation is not practically automatable and waveform legibility is a visual judgment (justified in 02-VALIDATION.md Manual-Only Verifications; the same code paths are already covered by unit tests + pluginval in Task 1). Wait for the resume signal before proceeding.</action>
  <verify>
    <automated>MISSING — manual-only by nature: OS drag delivery, visual waveform legibility, and message-thread responsiveness under real scheduling cannot be unit-tested (per 02-VALIDATION.md); automated coverage of the underlying decode/selection logic already green via ChordAITests in Task 1</automated>
  </verify>
  <done>User confirms "approved": all four formats loaded via real drag-and-drop, waveform legible, region select + click-to-reset working, UI responsive during 3+ minute decode, belt animation continuous, non-audio files rejected.</done>
</task>

</tasks>

<verification>
- Automated gate re-run green immediately before human verification (Task 1)
- Human confirmed: 4-format drop matrix, waveform legibility, region select + whole-file default via click, responsiveness during a 3+ minute decode, belt animation continuity, non-audio rejection
</verification>

<success_criteria>
- IMP-01, IMP-02, IMP-03 all human-verified in the Standalone app
- No regression in the Phase 1 gate (pluginval strictness 5, smoke test) with all Phase 2 code present
- Phase ready for /gsd:verify-work; per-DAW drag matrix explicitly deferred to Phase 7 (PLT-03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-import-waveform/02-04-SUMMARY.md`
</output>
