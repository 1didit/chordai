---
phase: 01-plugin-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: []
autonomous: false
requirements: [PLT-01]

must_haves:
  truths:
    - "Plugin loads without crashing as VST3 in Ableton Live (cold scan, not cached)"
    - "Plugin loads without crashing as VST3 in FL Studio (cold scan, not cached)"
    - "Plugin loads without crashing as AU in Logic Pro"
    - "Standalone app opens a visible window that renders correctly"
  artifacts:
    - path: "~/Library/Audio/Plug-Ins/VST3/ChordAI.vst3"
      provides: "Installed VST3 bundle, current build"
    - path: "~/Library/Audio/Plug-Ins/Components/ChordAI.component"
      provides: "Installed AU bundle, current build"
  key_links:
    - from: "DAW host plugin scanners"
      to: "installed ChordAI bundles"
      via: "cold rescan (cleared cache), not incremental cached load"
      pattern: "N/A - manual verification"
---

<objective>
Phase gate: prove the built plugin actually loads in the three target hosts. auval/pluginval (Plan 02) validate the binaries, but no CLI/headless mode exists for Ableton Live, FL Studio, or Logic Pro plugin loading — this is a genuinely manual verification (per 01-VALIDATION.md Manual-Only Verifications), preceded by an automated pre-flight so the human never tests a stale build.

Purpose: PLT-01 success criteria 1-3 as literally stated. Pitfall 11: a load from an already-cached scan proves nothing — cold scans required.
Output: Human-confirmed DAW load matrix; Phase 1 complete and ready for /gsd:verify-work.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-plugin-foundation/01-VALIDATION.md
@.planning/phases/01-plugin-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — ensure installed bundles are current and still valid</name>
  <files></files>
  <action>
    Guarantee the human tests the latest build, not a stale bundle:
    1. `cmake --build build` — incremental rebuild; COPY_PLUGIN_AFTER_BUILD re-copies bundles to ~/Library/Audio/Plug-Ins on any change
    2. Confirm installed bundle timestamps are >= newest Source/ file timestamp
    3. Quick revalidation: `auval -v aufx Cha1 Chai` must still PASS and `bash tools/smoke-test-standalone.sh` must still exit 0
    If anything fails, fix before presenting the checkpoint — never hand a broken build to manual verification.
  </action>
  <verify>
    <automated>cmake --build build && auval -v aufx Cha1 Chai && bash tools/smoke-test-standalone.sh</automated>
  </verify>
  <done>Installed VST3/AU bundles reflect the current source; auval and standalone smoke test are green immediately before the manual gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual DAW load matrix (Ableton, FL Studio, Logic Pro, Standalone)</name>
  <what-built>Complete ChordAI plugin shell: VST3 + AU installed to system plugin folders, Standalone app in build artefacts. Automated validation already green: auval PASS, pluginval strictness 5 PASS on both formats (including state round-trip and RT-safety checks), standalone smoke test PASS.</what-built>
  <how-to-verify>
    IMPORTANT: use COLD scans — a load from an already-cached scan proves nothing (Pitfall 11).

    1. Ableton Live (VST3):
       - Preferences → Plug-Ins → toggle "Use VST3 Plug-In System Folders" off/on, or hold Alt and click "Rescan" for a deep rescan (clears cache)
       - Browser → Plug-Ins → VST3 → find "ChordAI" (vendor "ChordAI Dev") → drop on an audio track
       - Expected: device loads, plugin window opens showing a dark-grey 400x300 editor, no crash, audio on the track passes through unchanged
    2. FL Studio (VST3):
       - Options → Manage plugins → "Find installed plugins" (full scan with "Rescan previously verified plugins" checked)
       - Add ChordAI as an effect on a mixer insert slot
       - Expected: loads without crash, editor window renders
    3. Logic Pro (AU):
       - If ChordAI does not appear: Logic Pro → Settings → Plug-in Manager → select ChordAI → "Reset & Rescan Selection"
       - Insert ChordAI as an Audio FX on an audio track
       - Expected: passes Logic's AU check, editor opens, no crash
    4. Standalone:
       - Open the ChordAI.app under build/ChordAI_artefacts/ (Finder or `open`)
       - Expected: a 400x300 dark-grey window appears and the app stays responsive until you quit it
    5. In ONE of the DAWs, save the project with ChordAI on a track, close, reopen — plugin must reload without error (empty-state persistence sanity check on top of pluginval's round-trip test)
  </how-to-verify>
  <resume-signal>Type "approved" if all four hosts load cleanly, or describe exactly which host failed and what happened (crash log / error dialog text)</resume-signal>
  <files></files>
  <action>Present the verification checklist above to the user and PAUSE. Do not perform the DAW loads yourself — Ableton Live, FL Studio, and Logic Pro have no headless/CLI plugin-scan mode, so this is human-only verification. Wait for the resume signal before proceeding.</action>
  <verify>
    <automated>MISSING — manual-only by nature: no CLI/headless plugin-load mode exists for Ableton Live, FL Studio, or Logic Pro (justified in 01-VALIDATION.md Manual-Only Verifications; automated coverage of the same binaries already provided by auval + pluginval in Plan 02)</automated>
  </verify>
  <done>User confirms "approved": ChordAI loaded without crashing in Ableton Live (VST3, cold scan), FL Studio (VST3, cold scan), Logic Pro (AU), and the Standalone window rendered; one DAW project save/reload cycle succeeded.</done>
</task>

</tasks>

<verification>
- Pre-flight automated suite green immediately before manual testing
- All four manual loads confirmed by user: Ableton (VST3), FL Studio (VST3), Logic Pro (AU), Standalone
- At least one DAW project save/reload cycle with the plugin present succeeded
</verification>

<success_criteria>
- PLT-01 fully evidenced: builds (Plan 01) + automated validity (Plan 02) + real host loads (this plan)
- Phase 1 success criteria 1-5 all TRUE; phase ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-foundation/01-03-SUMMARY.md`
</output>
