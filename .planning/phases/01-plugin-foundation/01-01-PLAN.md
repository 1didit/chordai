---
phase: 01-plugin-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .gitmodules
  - external/JUCE
  - CMakeLists.txt
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
  - Source/PluginEditor.h
  - Source/PluginEditor.cpp
autonomous: true
requirements: [PLT-01]

must_haves:
  truths:
    - "cmake -B build -G Ninja configures the project without errors"
    - "cmake --build build compiles VST3, AU, and Standalone targets with zero errors"
    - "ChordAI.vst3 and ChordAI.component are auto-installed to ~/Library/Audio/Plug-Ins after build (COPY_PLUGIN_AFTER_BUILD)"
    - "Plugin identity (PLUGIN_CODE, PLUGIN_MANUFACTURER_CODE, BUNDLE_ID, COMPANY_NAME) is explicit in CMakeLists.txt and stable across builds"
    - "processBlock contains zero heap allocation, zero locks, zero file I/O"
  artifacts:
    - path: "CMakeLists.txt"
      provides: "Single juce_add_plugin() target producing VST3 + AU + Standalone"
      contains: "PLUGIN_CODE"
    - path: "Source/PluginProcessor.h"
      provides: "AudioProcessor with public APVTS member"
      contains: "AudioProcessorValueTreeState"
    - path: "Source/PluginProcessor.cpp"
      provides: "APVTS-backed state save/restore + RT-safe processBlock + createPluginFilter"
      min_lines: 40
    - path: "Source/PluginEditor.h"
      provides: "Minimal AudioProcessorEditor shell"
    - path: "Source/PluginEditor.cpp"
      provides: "Editor paint/resized implementation"
    - path: "external/JUCE"
      provides: "JUCE framework pinned as git submodule to exact 8.0.x tag"
    - path: ".gitignore"
      provides: "Excludes build/, validation-logs/, tools/pluginval.app"
  key_links:
    - from: "CMakeLists.txt"
      to: "external/JUCE"
      via: "add_subdirectory"
      pattern: "add_subdirectory\\(external/JUCE\\)"
    - from: "CMakeLists.txt"
      to: "Source/PluginProcessor.cpp"
      via: "target_sources"
      pattern: "Source/PluginProcessor\\.cpp"
    - from: "Source/PluginProcessor.cpp getStateInformation"
      to: "apvts"
      via: "copyState + copyXmlToBinary"
      pattern: "copyState"
    - from: "Source/PluginProcessor.cpp setStateInformation"
      to: "apvts"
      via: "replaceState from XML"
      pattern: "replaceState"
---

<objective>
Stand up the entire ChordAI plugin skeleton from an empty repo: install the missing build toolchain (CMake is NOT installed on this machine), pin JUCE as a git submodule at an exact 8.0.x tag, write the CMakeLists.txt + minimal AudioProcessor/Editor pair with APVTS state persistence and a real-time-safe processBlock, and produce a successful build of all three formats (VST3, AU, Standalone).

Purpose: PLT-01 — everything in ChordAI (Phases 2-7) builds on this shell. State-persistence and RT-safety discipline are established here so they never need retrofitting (PITFALLS.md Pitfalls 6 and 12).
Output: Building project with installed VST3/AU bundles and a Standalone .app.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-foundation/01-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install build toolchain and pin JUCE submodule</name>
  <files>.gitignore, .gitmodules, external/JUCE</files>
  <action>
    Work from /Users/test/Documents/prjcts/chordai (repo root).

    1. Install the toolchain (CMake is verified NOT installed on this machine; Homebrew 6.x is present):
       `brew install cmake ninja`
       Expect cmake >= 3.25 and ninja on PATH afterwards.
    2. Add JUCE as a git submodule pinned to an EXACT tag (never master/develop):
       ```
       git submodule add https://github.com/juce-framework/JUCE.git external/JUCE
       git -C external/JUCE fetch --tags
       git -C external/JUCE tag -l '8.0.*' --sort=-v:refname | head -5
       ```
       Pin the NEWEST 8.0.x tag found (8.0.13 is the verified floor per research; if a newer 8.0.x such as 8.0.14 exists, use it; do NOT jump to a 9.x or any non-8.0.x tag):
       `git -C external/JUCE checkout <newest-8.0.x-tag>`
       Then stage the submodule pointer so the pin is recorded.
    3. Create .gitignore at repo root with:
       ```
       build/
       validation-logs/
       tools/pluginval.app/
       tools/pluginval.zip
       .DS_Store
       ```
    Do NOT create any Projucer .jucer files — CMake is the only workflow for this project.
  </action>
  <verify>
    <automated>cmake --version && ninja --version && git -C external/JUCE describe --tags --exact-match | grep -E '^8\.0\.[0-9]+$'</automated>
  </verify>
  <done>cmake and ninja resolve on PATH; external/JUCE exists as a submodule checked out at an exact 8.0.x tag (>= 8.0.13); .gitignore excludes build artifacts and pluginval binary.</done>
</task>

<task type="auto">
  <name>Task 2: Create CMakeLists.txt and JUCE plugin skeleton sources</name>
  <files>CMakeLists.txt, Source/PluginProcessor.h, Source/PluginProcessor.cpp, Source/PluginEditor.h, Source/PluginEditor.cpp</files>
  <action>
    Create CMakeLists.txt at repo root following the research code example exactly:
    - `cmake_minimum_required(VERSION 3.25)` (JUCE hard minimum is 3.22; 3.25+ avoids known target-property edge cases)
    - `project(ChordAI VERSION 0.1.0 LANGUAGES CXX)`, `set(CMAKE_CXX_STANDARD 20)`, `set(CMAKE_CXX_STANDARD_REQUIRED ON)`
    - `add_subdirectory(external/JUCE)`
    - `juce_add_plugin(ChordAI ...)` with ALL identity args EXPLICIT (omitting PLUGIN_CODE randomizes plugin identity per build — hard trap):
      VERSION 0.1.0, COMPANY_NAME "ChordAI Dev" (placeholder, marked TODO in a comment — final name not locked yet per research Open Question 2), BUNDLE_ID "com.chordai-dev.chordai", IS_SYNTH FALSE, NEEDS_MIDI_INPUT FALSE, NEEDS_MIDI_OUTPUT FALSE, IS_MIDI_EFFECT FALSE, EDITOR_WANTS_KEYBOARD_FOCUS FALSE, COPY_PLUGIN_AFTER_BUILD TRUE, PLUGIN_MANUFACTURER_CODE Chai, PLUGIN_CODE Cha1, FORMATS VST3 AU Standalone, PRODUCT_NAME "ChordAI"
    - `target_sources(ChordAI PRIVATE Source/PluginProcessor.cpp Source/PluginEditor.cpp)`
    - `target_link_libraries(ChordAI PRIVATE juce::juce_audio_utils juce::juce_audio_processors juce::juce_gui_extra)` — do NOT add juce_dsp or juce_audio_formats (deferred to Phases 2-3, keep link surface minimal)
    - `target_compile_definitions(ChordAI PUBLIC JUCE_WEB_BROWSER=0 JUCE_USE_CURL=0 JUCE_VST3_CAN_REPLACE_VST2=0)`
    - `juce_generate_juce_header(ChordAI)`

    Create Source/PluginProcessor.h + .cpp per research Pattern 2 and Pattern 3:
    - Class `ChordAIAudioProcessor : public juce::AudioProcessor` with public `juce::AudioProcessorValueTreeState apvts;` constructed in the member-init list as `apvts (*this, nullptr, "PARAMETERS", createParameterLayout())`
    - `createParameterLayout()` returns an empty layout `return {};` with a comment: real parameters arrive in later phases; if Plan 02's auval/pluginval run complains about zero parameters, a placeholder param gets added then (research Open Question 3 — empirical check, do not preemptively add)
    - Constructor bus layout: use `BusesProperties().withInput("Input", juce::AudioChannelSet::stereo(), true).withOutput("Output", juce::AudioChannelSet::stereo(), true)`. NOTE: research Pattern 2 shows bare `BusesProperties()`, but its own Pattern 3 processBlock passes audio through (`getTotalNumOutputChannels()`, clearing extra channels), which requires buses to exist, and auval channel-config tests on an `aufx` effect expect at least one I/O configuration. Stereo in/out is the standard JUCE effect default and keeps the plugin inert-but-valid. Document this rationale in a code comment.
    - `getStateInformation`: `apvts.copyState()` -> `state.createXml()` -> `copyXmlToBinary` (guard both with validity checks, per research snippet)
    - `setStateInformation`: `getXmlFromBinary` -> check `xml->hasTagName (apvts.state.getType())` -> `apvts.replaceState (juce::ValueTree::fromXml (*xml))` — defensive against foreign/corrupt data (Pitfall 12)
    - `processBlock`: EXACTLY the RT-safe skeleton — `juce::ScopedNoDenormals noDenormals;`, `juce::ignoreUnused (midiMessages);`, clear any output channels beyond input count, nothing else. ZERO heap allocation, zero locks, zero file I/O, zero juce::String construction. `prepareToPlay` stays empty but carries the comment that it is the designated allocation site for all future phases.
    - All the boilerplate overrides from the research header snippet (getName via JucePlugin_Name, acceptsMidi/producesMidi false, getTailLengthSeconds 0.0, program stubs, hasEditor true)
    - At the bottom of PluginProcessor.cpp add the REQUIRED factory function (not shown in research snippet but mandatory for every JUCE plugin — build fails at link without it):
      `juce::AudioProcessor* JUCE_CALLTYPE createPluginFilter() { return new ChordAIAudioProcessor(); }`
    - Use `#include <JuceHeader.h>` (juce_generate_juce_header provides it)

    Create Source/PluginEditor.h + .cpp per research "Minimal AudioProcessorEditor": setSize(400, 300), paint fills dark grey, resized() empty, holds `ChordAIAudioProcessor&` reference. No waveform/timeline UI — that is Phase 2.
  </action>
  <verify>
    <automated>cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug</automated>
  </verify>
  <done>CMake configure completes with zero errors; all three format targets (ChordAI_VST3, ChordAI_AU, ChordAI_Standalone) are generated; identity codes Chai/Cha1 and bundle ID are explicit in CMakeLists.txt.</done>
</task>

<task type="auto">
  <name>Task 3: Build all three formats and verify installed artifacts</name>
  <files>CMakeLists.txt, Source/PluginProcessor.cpp, Source/PluginEditor.cpp</files>
  <action>
    Run `cmake --build build` (first build compiles all JUCE modules — expect 5-10 minutes; use a Bash timeout of 600000 ms).
    Fix any compile/link errors in the skeleton sources (files listed above are the only ones you may touch; do not modify anything under external/JUCE).
    After a green build, confirm COPY_PLUGIN_AFTER_BUILD delivered the bundles to the system plugin folders:
    - ~/Library/Audio/Plug-Ins/VST3/ChordAI.vst3
    - ~/Library/Audio/Plug-Ins/Components/ChordAI.component
    and that a Standalone ChordAI.app exists under build/ChordAI_artefacts/.
    Then run a manual RT-safety review of processBlock: grep Source/PluginProcessor.cpp for `new `, `malloc`, `std::mutex`, `resize`, `fopen`, `juce::String` inside processBlock and anything it calls — must be clean (pluginval's RT check in Plan 02 cannot catch mutexes, so this grep is part of Definition of Done).
  </action>
  <verify>
    <automated>cmake --build build && test -d "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3" && test -d "$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component" && [ -n "$(find build -type d -name 'ChordAI.app' -print -quit)" ] && ! grep -E 'new |malloc|std::mutex|fopen' Source/PluginProcessor.cpp</automated>
  </verify>
  <done>All three targets build with zero errors; VST3 and AU bundles are installed in ~/Library/Audio/Plug-Ins; Standalone .app exists in build artefacts; processBlock grep audit is clean.</done>
</task>

</tasks>

<verification>
- `cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug` configures cleanly
- `cmake --build build` produces ChordAI_VST3, ChordAI_AU, ChordAI_Standalone with zero errors
- VST3 + AU bundles present in ~/Library/Audio/Plug-Ins (auto-copied)
- external/JUCE pinned at exact 8.0.x tag, recorded in .gitmodules + gitlink
- processBlock is provably inert: ScopedNoDenormals + channel clear only
</verification>

<success_criteria>
- Repo builds VST3, AU, and Standalone from one CMakeLists.txt (PLT-01 build half)
- Plugin identity fixed from commit 1: PLUGIN_CODE Cha1 / MANUFACTURER_CODE Chai / com.chordai-dev.chordai
- APVTS + getStateInformation/setStateInformation wired (validated end-to-end in Plan 02 via pluginval state round-trip)
- No Projucer artifacts, no juce_dsp/juce_audio_formats dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-foundation/01-01-SUMMARY.md`
</output>
