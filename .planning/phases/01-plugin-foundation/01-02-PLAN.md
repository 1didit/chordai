---
phase: 01-plugin-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tools/fetch-pluginval.sh
  - tools/smoke-test-standalone.sh
  - Source/PluginProcessor.cpp
  - Source/PluginProcessor.h
autonomous: true
requirements: [PLT-01]

must_haves:
  truths:
    - "auval -v aufx Cha1 Chai reports PASS (AU validity — success criterion 2's automatable half)"
    - "pluginval at strictness 5 passes against the installed VST3, including its built-in state round-trip test (success criterion 5)"
    - "pluginval at strictness 5 passes against the installed AU component"
    - "tools/smoke-test-standalone.sh exits 0: Standalone launches, stays alive, leaves no crash report (success criterion 3's automatable half)"
    - "processBlock RT-safety confirmed by pluginval run + manual mutex/alloc grep (success criterion 4)"
  artifacts:
    - path: "tools/fetch-pluginval.sh"
      provides: "Idempotent download of prebuilt pluginval macOS binary (binary itself stays gitignored)"
      contains: "pluginval_macOS.zip"
    - path: "tools/smoke-test-standalone.sh"
      provides: "Standalone launch smoke test: process stays alive N seconds, no new crash report"
      contains: "DiagnosticReports"
  key_links:
    - from: "tools/smoke-test-standalone.sh"
      to: "build/ChordAI_artefacts/**/ChordAI.app"
      via: "find + direct binary launch"
      pattern: "ChordAI\\.app"
    - from: "tools/fetch-pluginval.sh"
      to: "tools/pluginval.app"
      via: "curl + unzip of GitHub release"
      pattern: "Tracktion/pluginval"
---

<objective>
Wire the automated validation gate for the plugin shell built in Plan 01: fetch pluginval, author the Standalone smoke-test script, then run the full suite — auval, pluginval strictness 5 on both VST3 and AU (covers state round-trip and RT-safety checks), and the smoke script — fixing anything the validators flag.

Purpose: PLT-01 success criteria 2-5 get a scriptable, repeatable pre-DAW feedback loop. Per research, automated validators are the primary dev loop; manual DAW loads (Plan 03) are the spot-check, not the reverse.
Output: tools/ scripts committed; validation-logs/ evidence of green auval + pluginval runs; any validator-driven source fixes applied.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-plugin-foundation/01-RESEARCH.md
@.planning/phases/01-plugin-foundation/01-VALIDATION.md
@.planning/phases/01-plugin-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Contracts established by Plan 01 — use directly, no exploration needed. -->

Plugin identity (from CMakeLists.txt juce_add_plugin):
- AU type/subtype/manufacturer: aufx / Cha1 / Chai  → auval invocation is `auval -v aufx Cha1 Chai`
- Installed bundles (COPY_PLUGIN_AFTER_BUILD TRUE):
  - $HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3
  - $HOME/Library/Audio/Plug-Ins/Components/ChordAI.component
- Standalone app: under build/ChordAI_artefacts/ (locate with `find build -type d -name 'ChordAI.app'`)
- State: public `juce::AudioProcessorValueTreeState apvts` in ChordAIAudioProcessor; empty ParameterLayout; getState/setState via copyState/replaceState XML
- Rebuild command: `cmake --build build`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tools/fetch-pluginval.sh and fetch the binary</name>
  <files>tools/fetch-pluginval.sh</files>
  <action>
    Create tools/fetch-pluginval.sh (chmod +x), bash with `set -euo pipefail`:
    - `cd "$(dirname "$0")"` so it always operates inside tools/
    - Idempotent: if `pluginval.app/Contents/MacOS/pluginval` already exists and is executable, print "pluginval already present" and exit 0
    - Otherwise: `curl -L "https://github.com/Tracktion/pluginval/releases/latest/download/pluginval_macOS.zip" -o pluginval.zip`, `unzip -o pluginval.zip`, `rm pluginval.zip`
    - Fail loudly if the binary is missing after unzip
    The pluginval binary is NOT committed (tools/pluginval.app/ and tools/pluginval.zip are gitignored from Plan 01) — only the script is source-controlled. pluginval is GPLv3 but runs out-of-process against the built plugin, so it does not taint the product license.
    Run the script to actually download pluginval.
  </action>
  <verify>
    <automated>bash tools/fetch-pluginval.sh && tools/pluginval.app/Contents/MacOS/pluginval --version</automated>
  </verify>
  <done>Script exists, is idempotent, and a working pluginval binary sits at tools/pluginval.app/Contents/MacOS/pluginval (gitignored).</done>
</task>

<task type="auto">
  <name>Task 2: Create tools/smoke-test-standalone.sh and run it</name>
  <files>tools/smoke-test-standalone.sh</files>
  <action>
    Create tools/smoke-test-standalone.sh (chmod +x), bash with `set -euo pipefail`, run from repo root:
    1. Locate the app: `APP=$(find build -type d -name 'ChordAI.app' -print -quit)`; fail with a clear message if not found ("run cmake --build build first")
    2. Record a timestamp marker file (mktemp + touch) for crash-report comparison
    3. Launch the binary directly in background: `"$APP/Contents/MacOS/ChordAI" & PID=$!`
    4. `sleep 5`, then `kill -0 "$PID"` — process must still be alive (if it exited, FAIL)
    5. `kill "$PID"` and wait to clean up
    6. Check `~/Library/Logs/DiagnosticReports` for any file matching `ChordAI*` newer than the marker (`find ... -newer "$MARKER"`); any hit = FAIL
    7. Print PASS/FAIL and exit 0/1 accordingly
    Note in a comment: this proves "launches and stays alive, no crash" only; the visual "window renders correctly" check happens once in the Plan 03 checkpoint. Run the script against the Plan 01 build.
  </action>
  <verify>
    <automated>bash tools/smoke-test-standalone.sh</automated>
  </verify>
  <done>Script exits 0: Standalone launched from build artefacts, survived 5 s, was cleanly killed, and produced no new crash report.</done>
</task>

<task type="auto">
  <name>Task 3: Run full validation suite (auval + pluginval strictness 5) and fix findings</name>
  <files>Source/PluginProcessor.cpp, Source/PluginProcessor.h</files>
  <action>
    Run the full validation gate against the installed bundles:
    1. AU registration sanity: `auval -a | grep -i chordai` — if the AU is not listed, refresh the component registrar (`killall -9 AudioComponentRegistrar 2>/dev/null; sleep 2`) and retry; a rebuild re-copies the component if needed.
    2. `auval -v aufx Cha1 Chai` — must end with "PASS". Use `-de` variant while debugging failures (stops at first error).
    3. `mkdir -p validation-logs`, then run pluginval per format:
       `tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --output-dir validation-logs "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3"`
       `tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --output-dir validation-logs "$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component"`
       Strictness 5 includes the state save/restore round-trip test (success criterion 5) by default — no extra flag needed.
    4. Contingencies, applied ONLY if a validator actually complains (research Open Questions 3):
       - Zero-parameter warning/failure from the empty ParameterLayout → add ONE placeholder parameter to createParameterLayout() (e.g., `AudioParameterBool ("bypass", "Bypass", false)`), rebuild (`cmake --build build`), rerun the suite.
       - Bus/channel-config failure → adjust BusesProperties/isBusesLayoutSupported minimally, rebuild, rerun.
       Keep processBlock inert regardless of any fix — zero allocation/locks/I-O.
    5. Final RT-safety audit (pluginval's RealtimeCheck cannot detect mutex misuse): grep Source/PluginProcessor.cpp for `std::mutex`, `CriticalSection`, `new `, `malloc`, `resize`, `fopen`, `juce::String` in processBlock and its callees — must be clean.
    All three checks green = the automatable portion of PLT-01 criteria 2-5 is proven.
  </action>
  <verify>
    <automated>auval -v aufx Cha1 Chai && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --output-dir validation-logs "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3" && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --output-dir validation-logs "$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component"</automated>
  </verify>
  <done>auval reports PASS; pluginval strictness 5 passes on both VST3 and AU (state round-trip included); RT grep audit clean; any contingency fixes committed with rationale.</done>
</task>

</tasks>

<verification>
- `bash tools/fetch-pluginval.sh` idempotent, produces working pluginval
- `bash tools/smoke-test-standalone.sh` exits 0
- `auval -v aufx Cha1 Chai` → PASS
- pluginval strictness 5 green on VST3 and AU, logs in validation-logs/
- No mutex/alloc/I-O in processBlock (grep audit)
</verification>

<success_criteria>
- Full automated validation suite (build + auval + pluginval + smoke) is scriptable and green — the per-wave gate defined in 01-VALIDATION.md now exists and passes
- State round-trip (getStateInformation/setStateInformation) proven by pluginval's strictness-5 suite
- Only remaining PLT-01 evidence is the manual DAW load matrix → Plan 03 checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-foundation/01-02-SUMMARY.md`
</output>
