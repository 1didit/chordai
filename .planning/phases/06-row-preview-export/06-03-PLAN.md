---
phase: 06-row-preview-export
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - Source/UI/MidiRowLayout.h
  - Source/UI/MidiRowView.h
  - Source/UI/MidiRowView.cpp
  - Source/UI/MidiSetsPanel.h
  - Source/UI/MidiSetsPanel.cpp
  - Source/PluginEditor.cpp
  - Tests/MidiSetsPanelLayoutTests.cpp
autonomous: true
requirements: [PRV-01, EXP-01, EXP-02]

must_haves:
  truths:
    - "Clicking a row's play icon auditions that row; clicking again (now a stop glyph) stops it; only one row plays at a time"
    - "Dragging anywhere on a row body starts an OS file drag carrying a freshly written temp .mid — the file exists on disk BEFORE performExternalDragDropOfFiles is called"
    - "The drag temp file is NEVER deleted in a drag completion callback — cleanup of previous temp files happens at the start of the next drag (Ableton async-read race fix)"
    - "Clicking a row's save icon opens a native async save dialog pre-filled with suggestedFileName, defaulting to ~/Documents/ChordAI MIDI/ and remembering the last-used directory for the session"
    - "Region-change regeneration (MidiSetsPanel::setRows) stops any playing audition before destroying row views"
    - "RegionSelectorOverlay is untouched; the pixel-art aesthetic (flat rects, existing palette) is preserved"
  artifacts:
    - path: "Source/UI/MidiRowLayout.h"
      provides: "Pure hit-zone geometry: playIconRect / saveIconRect from gutter bounds (unit-testable without a Component)"
      exports: ["playIconRect", "saveIconRect"]
    - path: "Source/UI/MidiRowView.cpp"
      provides: "mouseDown/mouseDrag/mouseUp gesture split, icon painting, drag-out flow, save flow"
      contains: "performExternalDragDropOfFiles"
    - path: "Source/UI/MidiSetsPanel.cpp"
      provides: "stopAudition-on-setRows + hook forwarding into every rebuilt MidiRowView + playing-state repaint timer"
      contains: "onStopAudition"
    - path: "Source/PluginEditor.cpp"
      provides: "Hooks wired to ChordAIAudioProcessor: startAudition/stopAudition/isAuditionPlaying/getAuditionRowId/getAnalysisResult"
  key_links:
    - from: "Source/UI/MidiRowView.cpp mouseDrag"
      to: "MidiFileWriter::writeToFile"
      via: "temp .mid written to tempDirectory/ChordAI/ before the drag call"
      pattern: "writeToFile"
    - from: "Source/UI/MidiRowView.cpp"
      to: "juce::DragAndDropContainer::performExternalDragDropOfFiles"
      via: "static call, canMoveFiles=false, nullptr completion callback"
      pattern: "performExternalDragDropOfFiles.*false"
    - from: "Source/UI/MidiSetsPanel.cpp setRows"
      to: "ChordAIAudioProcessor::stopAudition"
      via: "onStopAudition std::function hook, called unconditionally at the top of setRows"
      pattern: "onStopAudition"
    - from: "Source/PluginEditor.cpp"
      to: "processor.startAudition / processor.getAuditionRowId"
      via: "lambda hooks assigned in the editor ctor"
      pattern: "startAudition"
---

<objective>
Make the 5 MIDI rows interactive — the user-demanded MIDI-pack workflow: play/stop audition icons and a save icon in each row's 92px label gutter, and drag-anywhere-on-the-row OS file drag that drops a `.mid` straight into the DAW piano roll. All export goes through 06-01's `MidiFileWriter`; all audition goes through 06-02's processor API; play state lives on the processor because every `MidiRowView` is destroyed on regeneration (06-RESEARCH.md Pitfall 3).

Purpose: EXP-01 (drag-out), EXP-02 (save dialog), PRV-01 (play/stop interaction) — the phase's user-facing surface.
Output: interactive MidiRowView + MidiSetsPanel/PluginEditor wiring; full suite + pluginval green.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-row-preview-export/06-RESEARCH.md
@.planning/phases/02-audio-import-waveform/02-CONTEXT.md
@Source/UI/MidiRowView.h
@Source/UI/MidiRowView.cpp
@Source/UI/MidiRowLayout.h
@Source/UI/MidiSetsPanel.h
@Source/UI/MidiSetsPanel.cpp
@Source/PluginEditor.h
@Source/PluginEditor.cpp
@Source/MidiGen/MidiFileWriter.h
@Tests/MidiSetsPanelLayoutTests.cpp

All commands run from /Users/test/Documents/prjcts/chordai. Builds: Bash timeout 600000 ms.
Git commits: plain conventional messages, NO co-author/attribution trailers.
Non-ASCII string literals (if any) MUST go through juce::String(juce::CharPointer_UTF8(...)) — plain literals assert at pluginval strictness 5 (Phase 02-02 finding). Prefer ASCII-only strings in this plan.

<interfaces>
<!-- Contracts from prior plans — use directly, do not re-explore. -->

From 06-01, Source/MidiGen/MidiFileWriter.h:
```cpp
namespace MidiFileWriter
{
    constexpr int kTicksPerQuarterNote = 960;
    juce::MidiFile buildMidiFile (const MidiSetRow& row, double bpm);
    bool writeToFile (const MidiSetRow& row, double bpm, const juce::File& destination); // atomic replace
    juce::String suggestedFileName (const MidiSetRow& row, const KeyResult& key, double bpm); // "pop-trap_G#m_128bpm.mid"
}
```

From 06-02, Source/PluginProcessor.h additions:
```cpp
void startAudition (const MidiSetRow& row);   // message thread; replaces current audition
void stopAudition();
bool isAuditionPlaying() const;
juce::String getAuditionRowId() const;         // AND with isAuditionPlaying() — stale after auto-stop by design
std::shared_ptr<const AnalysisResult> getAnalysisResult() const; // ->bpm, ->key (KeyResult); may be nullptr
```

Current MidiRowView (Source/UI/MidiRowView.h/.cpp): ctor calls `setInterceptsMouseClicks(false, false)` ("Phase 6 flips this"); paint() uses kGutterWidth=92, kAccentBarWidth=2, accentForStyle(row.style), label at 10pt in the gutter, notes as flat velocity-alpha rects. Rows are 140px/5 = 28px tall. `getRow()` already exists as "the Phase 6 drag-out/audition hook".

Current MidiSetsPanel::setRows: `rows = move; rowViews.clear(); ...` rebuilds every MidiRowView from scratch on EVERY call (incl. every region-change regeneration) — this is why play state must not live on views.

PluginEditor: owns `processor` (ChordAIAudioProcessor&) and `midiSetsPanel`; calls `midiSetsPanel.setRows(processor.getMidiSetRows())` from changeListenerCallback AND handleLoadComplete.

JUCE (verified in 06-RESEARCH.md): `juce::DragAndDropContainer::performExternalDragDropOfFiles({paths}, canMoveFiles, sourceComponent, callback)` is STATIC — no DragAndDropContainer ancestor needed; `e.mouseWasDraggedSinceMouseDown()` is the sanctioned drag threshold; `juce::FileChooser::launchAsync(flags, callback)` with `FileBrowserComponent::saveMode | canSelectFiles | warnAboutOverwriting` — the FileChooser object MUST outlive the callback (unique_ptr member); `juce::File::getSpecialLocation(juce::File::tempDirectory / userDocumentsDirectory)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Icon geometry + MidiRowView interaction skeleton (hooks, painting, click dispatch)</name>
  <files>Source/UI/MidiRowLayout.h, Source/UI/MidiRowView.h, Source/UI/MidiRowView.cpp, Tests/MidiSetsPanelLayoutTests.cpp</files>
  <behavior>
    Tests (append to Tests/MidiSetsPanelLayoutTests.cpp, existing "[midisetspanellayout]" tag — pure functions, no Component):
    - playIconRect/saveIconRect for a 92x28 gutter: both fully inside the gutter, non-overlapping, each at least 10x10 px (usable hit target in a 28px row), right-aligned so the label keeps >= 55px of text width.
    - Stability: rects for 92x28 equal rects for a second call (pure/deterministic); degenerate 92x0 gutter returns empty-safe rects (no negative sizes).
  </behavior>
  <action>
    RED: add `inline juce::Rectangle<int> playIconRect (juce::Rectangle<int> gutterBounds)` and `saveIconRect(...)` declarations to Source/UI/MidiRowLayout.h (same pure-free-function extraction rationale the file already documents) + failing geometry tests. Commit `test(06-03): add failing row icon hit-zone geometry tests`.

    GREEN: implement — recommended layout (executor discretion within the tested invariants): two 12x12 zones stacked vertically at the gutter's right edge (play on top, save below, 2px gap, 3px right margin, vertically centered as a pair). Then MidiRowView:
    - Ctor: `setInterceptsMouseClicks(true, false)` (flip the documented Phase 6 switch).
    - New public std::function hooks (set by MidiSetsPanel before/right after setRow): `std::function<double()> getBpmForExport; std::function<KeyResult()> getKeyForExport; std::function<void(const MidiSetRow&)> onAuditionToggle; std::function<bool(const juce::String& rowId)> isRowPlaying;` — MidiRowView never reaches into PluginProcessor directly (established decoupling, per 06-RESEARCH.md User Constraints).
    - paint(): after the existing label draw, shrink the label's text rect on the right by the icon zones' width so text never underlaps icons; draw the icons pixel-art style — flat single-colour fills only, no gradients, existing palette: idle glyph colour 0xff5a5a6e, playing/active accent = accentForStyle(row.style). Play = stepped pixel triangle (3-4 fillRect columns); when `isRowPlaying && isRowPlaying(row.id)` draw a solid stop square instead; save = small down-arrow-into-tray built from 3 fillRects. Exact glyph pixels are executor discretion — flat rects + palette are the locked constraints (02-CONTEXT.md pixel-art identity).
    - mouseDown: record `dragStarted = false` (member); no action yet.
    - mouseUp: if `!dragStarted` — hit-test playIconRect -> `onAuditionToggle(row)` + repaint(); saveIconRect -> `saveRow()` (Task 2 stub for now); elsewhere -> nothing.
    - mouseDrag: gesture guard only this task (`if (dragStarted || !e.mouseWasDraggedSinceMouseDown()) return; dragStarted = true;`) — the actual drag-out body lands in Task 2.
    Commit `feat(06-03): MidiRowView interaction skeleton — icons, hooks, click dispatch`.
  </action>
  <verify>
    <automated>cmake --build build --target ChordAITests && ./build/ChordAITests_artefacts/Debug/ChordAITests "[midisetspanellayout]"</automated>
  </verify>
  <done>Geometry tests green; MidiRowView paints play/stop + save glyphs in the gutter, accepts mouse, dispatches icon clicks through hooks; label never underlaps icons.</done>
</task>

<task type="auto">
  <name>Task 2: Drag-out (EXP-01) + save dialog (EXP-02) flows in MidiRowView</name>
  <files>Source/UI/MidiRowView.h, Source/UI/MidiRowView.cpp</files>
  <action>
    Drag-out (06-RESEARCH.md Pattern 2 + Pitfall 1 — copy the verified shape):
    - File-local helper `writeDragTempFile(const MidiSetRow& row, const KeyResult& key, double bpm) -> juce::File`: subfolder = `juce::File::getSpecialLocation(juce::File::tempDirectory).getChildFile("ChordAI")`; FIRST perform the deferred cleanup — delete every existing *.mid in that subfolder (this is the previous drag's file; deleting it now, not in a completion callback, is THE Ableton fix); `createDirectory()`; target = subfolder child `MidiFileWriter::suggestedFileName(row, key, bpm)`; `MidiFileWriter::writeToFile(row, bpm, target)`; return target (invalid File on write failure).
    - In mouseDrag, after the once-per-gesture guard from Task 1: resolve `bpm = getBpmForExport ? getBpmForExport() : 120.0` and `key = getKeyForExport ? getKeyForExport() : KeyResult{}`; write the temp file; if `tempFile.existsAsFile()` call `juce::DragAndDropContainer::performExternalDragDropOfFiles({ tempFile.getFullPathName() }, /*canMoveFiles*/ false, this, /*callback*/ nullptr)`. CRITICAL, verbatim from research: canMoveFiles MUST be false (DAW copies bytes, never takes ownership) and the callback MUST be nullptr — ANY cleanup callback that deletes the file races Ableton's async read ("could not be opened"). Leave the file on disk; next drag's cleanup or OS temp hygiene collects it. Add an in-code comment citing 06-RESEARCH.md Pitfall 1 so this is never "optimized" away.
    - Drag works from anywhere on the row, icons included (a real drag gesture overrides a click; the click only fires in mouseUp when no drag happened) — this satisfies "drag anywhere on row body".

    Save (06-RESEARCH.md Pattern 4 / Code Examples — copy the verified shape):
    - `saveRow()`: function-local `static juce::File lastUsedDirectory = userDocumentsDirectory.getChildFile("ChordAI MIDI");` (session memory; static-local is the sanctioned v1 choice). `lastUsedDirectory.createDirectory()`; initial file = lastUsedDirectory child of suggestedFileName. Member `std::unique_ptr<juce::FileChooser> fileChooser` (MUST be a member — the header's own doc requires the chooser to outlive launchAsync's callback); `fileChooser = std::make_unique<juce::FileChooser>("Save MIDI row", initial, "*.mid");` then `launchAsync(saveMode | canSelectFiles | warnAboutOverwriting, callback)`.
    - Lifetime safety (row views are destroyed by every setRows while a dialog may be open): the callback captures the row BY VALUE and bpm by value, and updates lastUsedDirectory through the function-local static — it must NOT capture `this`. If the view dies, the chooser dies with it and the callback simply never fires; nothing dangles. On result: empty File -> cancelled, return; else update lastUsedDirectory = chosen.getParentDirectory(), `MidiFileWriter::writeToFile(rowCopy, bpm, chosen)`.
    Commit `feat(06-03): row drag-out with deferred temp cleanup + async .mid save dialog`.
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure && ! grep -rn "deleteFile" Source/UI/MidiRowView.cpp | grep -i "callback"</automated>
  </verify>
  <done>Build + full suite green; performExternalDragDropOfFiles called with canMoveFiles=false and nullptr callback AFTER the temp file exists; deferred cleanup at drag start; save dialog writes via the shared MidiFileWriter path with last-dir memory.</done>
</task>

<task type="auto">
  <name>Task 3: MidiSetsPanel forwarding + stop-on-regenerate + PluginEditor wiring</name>
  <files>Source/UI/MidiSetsPanel.h, Source/UI/MidiSetsPanel.cpp, Source/PluginEditor.cpp</files>
  <action>
    MidiSetsPanel (Pitfall 3 — play state must survive view destruction, and stale audio must never outlive its row):
    - New public std::function hooks (assigned once by the editor): `getBpmForExport`, `getKeyForExport`, `onAuditionToggle`, `isRowPlaying`, `onStopAudition`.
    - setRows(): FIRST LINE — `if (onStopAudition) onStopAudition();` unconditionally (a regenerate always invalidates whatever was playing; the old MidiSetRow no longer exists). Then the existing rebuild; for each new MidiRowView, forward the four row-level hooks. The panel's own onAuditionToggle wrapper forwards to the editor's hook AND starts the repaint timer (below).
    - Playing-state repaint: make MidiSetsPanel a juce::Timer. `timerCallback()` at 10 Hz: repaint all row views (icon glyphs read play state live from isRowPlaying — no cached booleans on views); if nothing is playing anymore (auto-stop at buffer end flips the processor atomic on the audio thread), stopTimer() after one final repaint. Start the timer inside the onAuditionToggle wrapper. This is the only new Timer and it runs only while auditioning — consistent with the existing message-thread animation discipline (02-CONTEXT.md).

    PluginEditor.cpp (ctor, after addAndMakeVisible(midiSetsPanel); no PluginEditor.h changes needed):
    - `midiSetsPanel.onStopAudition = [this] { processor.stopAudition(); };`
    - `midiSetsPanel.getBpmForExport = [this] { auto r = processor.getAnalysisResult(); return (r != nullptr && r->bpm > 0.0) ? r->bpm : 120.0; };`
    - `midiSetsPanel.getKeyForExport = [this] { auto r = processor.getAnalysisResult(); return r != nullptr ? r->key : KeyResult{}; };`
    - `midiSetsPanel.onAuditionToggle = [this] (const MidiSetRow& row) { if (processor.isAuditionPlaying() && processor.getAuditionRowId() == row.id) processor.stopAudition(); else processor.startAudition(row); };`
    - `midiSetsPanel.isRowPlaying = [this] (const juce::String& id) { return processor.isAuditionPlaying() && processor.getAuditionRowId() == id; };`
    Capturing `this` is safe here: midiSetsPanel is a member of the editor — hooks cannot outlive it. Assign hooks BEFORE the ctor's editor-reopen restore branch (which may call setRows immediately).
    RegionSelectorOverlay and every other component: untouched.
    Commit `feat(06-03): wire audition/export hooks editor->panel->rows; stop audition on regeneration`.
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3" && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component"</automated>
  </verify>
  <done>Full suite green; pluginval strictness 5 green on VST3 + AU (per-wave gate — pluginval's Editor Automation pass exercises the new mouse/paint paths); setRows stops audition before destroying views; all hooks wired end-to-end.</done>
</task>

</tasks>

<verification>
- Full ctest suite green (baseline + all Phase 6 tags)
- pluginval strictness 5 green on both formats (editor interaction paths exercised)
- Code review greps: `performExternalDragDropOfFiles` called with `false, this, nullptr`; `onStopAudition` is setRows' first statement; no `this` captured in the FileChooser callback; `setInterceptsMouseClicks (true` in MidiRowView ctor
- RegionSelectorOverlay.h/.cpp show zero diff
</verification>

<success_criteria>
- EXP-01: drag from any row produces a valid temp .mid (written before the drag call, Ableton-safe deferred cleanup) handed to the OS drag
- EXP-02: save icon opens a native async save dialog, suggested MIDI-pack name, ~/Documents/ChordAI MIDI/ default + session last-dir memory, shared MidiFileWriter path
- PRV-01 interaction: play/stop per row against processor-held state; regeneration cleanly stops audition; icons reflect auto-stop within ~100ms
- Human-facing behavior verified in 06-04's checkpoint (OS drag / native dialog / sound are not unit-testable — 06-VALIDATION.md manual-only rows)
</success_criteria>

<output>
After completion, create `.planning/phases/06-row-preview-export/06-03-SUMMARY.md`
</output>
