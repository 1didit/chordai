---
phase: 06-row-preview-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/MidiGen/MidiFileWriter.h
  - Source/MidiGen/MidiFileWriter.cpp
  - Tests/MidiFileWriterTests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [EXP-03]

must_haves:
  truths:
    - "A MidiSetRow + detected bpm converts to a format-1 SMF (TPQN 960) whose notes round-trip within 1 tick of startBeats*960 / (startBeats+lengthBeats)*960"
    - "The written file's tempo meta-event matches AnalysisResult.bpm (60/bpm seconds-per-quarter within 1e-4), with bpm<=0 falling back to 120"
    - "The written file carries a 4/4 time-signature meta-event on track 0, separate from the note track"
    - "A NoteEvent with lengthBeats <= 0 is never written to the file"
    - "suggestedFileName produces a filesystem-safe MIDI-pack-style name: rowId_Key_NNNbpm.mid"
    - "Writing twice to the same destination path replaces the file (no append corruption)"
  artifacts:
    - path: "Source/MidiGen/MidiFileWriter.h"
      provides: "buildMidiFile / writeToFile / suggestedFileName declarations + kTicksPerQuarterNote = 960"
      exports: ["MidiFileWriter::buildMidiFile", "MidiFileWriter::writeToFile", "MidiFileWriter::suggestedFileName", "MidiFileWriter::kTicksPerQuarterNote"]
    - path: "Source/MidiGen/MidiFileWriter.cpp"
      provides: "Implementation: beat->tick conversion, meta track, zero-length guard, TemporaryFile atomic replace"
      min_lines: 60
    - path: "Tests/MidiFileWriterTests.cpp"
      provides: "Round-trip, tempo/fallback, time-sig, zero-length-guard, overwrite, naming tests"
      min_lines: 80
  key_links:
    - from: "Tests/MidiFileWriterTests.cpp"
      to: "Source/MidiGen/MidiFileWriter.h"
      via: "#include + Catch2 [midifilewriter] tag"
      pattern: "midifilewriter"
    - from: "CMakeLists.txt CHORDAI_MIDIGEN_SOURCES"
      to: "Source/MidiGen/MidiFileWriter.cpp"
      via: "source list consumed by both ChordAI and ChordAITests targets"
      pattern: "MidiFileWriter\\.cpp"
---

<objective>
Build the shared MIDI-file core of Phase 6: `MidiFileWriter` converts a beat-domain `MidiSetRow` + detected bpm into a format-1 Standard MIDI File (TPQN 960, tempo + 4/4 time-sig meta track), writes it to disk atomically, and produces MIDI-pack-style suggested file names. Both export paths (EXP-01 drag-out and EXP-02 save dialog, wired in plan 06-03) call this single code path — divergence here is exactly how EXP-03's "carries the detected tempo" guarantee could silently break in one path and not the other.

Purpose: EXP-03 (exported MIDI is bar-aligned and carries the detected tempo) — proven by round-trip unit tests, not by manual DAW import alone.
Output: `Source/MidiGen/MidiFileWriter.h/.cpp`, `Tests/MidiFileWriterTests.cpp`, CMake wiring, `[midifilewriter]` suite green.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-row-preview-export/06-RESEARCH.md
@.planning/phases/06-row-preview-export/06-VALIDATION.md
@Source/MidiGen/MidiSetRow.h
@Source/MidiGen/NoteEvent.h
@Source/Analysis/AnalysisResult.h
@Source/UI/ChordNameFormatter.h
@CMakeLists.txt

All commands run from /Users/test/Documents/prjcts/chordai. Builds: Bash timeout 600000 ms.
Git commits: plain conventional messages, NO co-author/attribution trailers.

<interfaces>
<!-- Existing contracts the executor uses directly — no codebase exploration needed. -->

From Source/MidiGen/NoteEvent.h (pure value type, timing in BEATS — 1.0 == one quarter note at AnalysisResult.bpm):
```cpp
struct NoteEvent
{
    double startBeats = 0.0;
    double lengthBeats = 0.0;
    int pitch = 60;          // MIDI note number 0-127
    float velocity = 0.8f;   // 0.0-1.0, matches juce::MidiMessage::noteOn's float overload
};
```

From Source/MidiGen/MidiSetRow.h:
```cpp
enum class RowStyle { DetectedAsIs, PopHipHopTrap, RnbNeoSoul, ElectronicHouse, Bass };
struct MidiSetRow
{
    juce::String id;       // stable filesystem-safe key: "as-is", "pop-trap", "rnb-neosoul", "house", "bass"
    juce::String label;    // display label (contains '/', '&', spaces — NOT filesystem-safe, do not use in filenames)
    RowStyle style = RowStyle::DetectedAsIs;
    std::vector<NoteEvent> notes;
};
```

From Source/Analysis/AnalysisResult.h (FROZEN CONTRACT):
```cpp
struct KeyResult
{
    int tonicPitchClass = 0;   // 0=C..11=B
    bool isMajor = true;
    float confidence = 0.0f;
};
```

From Source/UI/ChordNameFormatter.h — the 12-note-name convention to duplicate verbatim (research's explicit guidance: duplicate the 12-entry array, do not invent a second table):
```cpp
static constexpr const char* kNoteNames[12] =
    { "C", "C#", "D", "D#", "E", "F", "F#", "G", "G#", "A", "A#", "B" };
```

JUCE 8.0.14 APIs (verified against vendored external/JUCE source in 06-RESEARCH.md — all in juce_audio_basics/juce_core, already linked by both targets):
- `juce::MidiFile::setTicksPerQuarterNote(int)` (MUST be called before addTrack), `addTrack(const MidiMessageSequence&)`, `writeTo(OutputStream&, int midiFileType)`, `readFrom(InputStream&)`, `getTimeFormat()`
- `juce::MidiMessage::noteOn(int channel, int pitch, float velocity)`, `noteOff(int channel, int pitch, float velocity)`, `tempoMetaEvent(int microsecondsPerQuarterNote)`, `timeSignatureMetaEvent(int num, int den)`, `endOfTrack()`, `isTempoMetaEvent()`, `getTempoSecondsPerQuarterNote()`, `isTimeSignatureMetaEvent()`, `getTimeSignatureInfo(int&, int&)`, `getFloatVelocity()`
- `juce::MidiMessageSequence::addEvent(msg, timeStamp)`, `updateMatchedPairs()`, `sort()`, `getEndTime()`
- `juce::TemporaryFile(destination)` + `overwriteTargetFileWithTemporary()` (atomic replace — Pitfall 4: FileOutputStream appends by default, never write the destination directly)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MidiFileWriter core — buildMidiFile + writeToFile with round-trip tests, CMake wiring</name>
  <files>Source/MidiGen/MidiFileWriter.h, Source/MidiGen/MidiFileWriter.cpp, Tests/MidiFileWriterTests.cpp, CMakeLists.txt</files>
  <behavior>
    Tests first (Tests/MidiFileWriterTests.cpp, TEST_CASE names "MidiFileWriterTests.*", tag "[midifilewriter]"):
    - Round-trip: build a MidiSetRow with 3+ notes at non-trivial positions (e.g. startBeats 0.0/1.5/4.0, lengths 1.0/0.25/2.0, distinct pitches, velocity 0.8f), bpm = 128.2 (deliberately non-integer). buildMidiFile -> writeTo a juce::MemoryOutputStream -> MidiFile::readFrom(MemoryInputStream). Assert: getTimeFormat() == 960; getNumTracks() == 2; every note-on/note-off tick on track 1 within ±1 tick of startBeats*960 and (startBeats+lengthBeats)*960; pitches match exactly; getFloatVelocity() within 1.0f/127 of the source velocity (7-bit quantization).
    - Tempo meta: track 0 contains a tempo meta-event with getTempoSecondsPerQuarterNote() within 1e-4 of 60.0/128.2. Bar alignment falls out of the beat domain: a note at startBeats 4.0 lands at tick 3840 = exactly bar 2 in 4/4 at TPQN 960 — assert it.
    - bpm fallback: buildMidiFile(row, 0.0) and (row, -5.0) -> tempo meta reads 60.0/120.0 (research: a tempo meta-event cannot represent zero/negative tempo; guard here, not upstream).
    - Time signature: track 0 contains a 4/4 timeSignatureMetaEvent (matches AnalysisResult.barStartBeatIndices' existing 4/4 assumption).
    - Zero-length guard (Pitfall 5): a row containing a note with lengthBeats == 0.0 and one with lengthBeats < 0 -> neither produces any note-on/note-off in the output; well-formed notes in the same row still round-trip.
    - writeToFile: writes to a fresh path under juce::File::getSpecialLocation(tempDirectory) test subfolder -> returns true, file exists, size > 0, MidiFile::readFrom(FileInputStream) succeeds with same track/event counts.
    - Overwrite regression (Pitfall 4): writeToFile twice to the SAME path -> second call returns true and the re-read file has the same event count as a single write (no appended/duplicated bytes).
    - Empty row: row.notes.empty() -> buildMidiFile still valid (2 tracks, meta events present, note track has only endOfTrack); writeToFile succeeds.
  </behavior>
  <action>
    RED: add Tests/MidiFileWriterTests.cpp to `target_sources(ChordAITests ...)` in CMakeLists.txt and Source/MidiGen/MidiFileWriter.cpp to `CHORDAI_MIDIGEN_SOURCES` (the list is consumed by BOTH ChordAI and ChordAITests target_sources blocks — one edit covers both). Write the header contract + a stub .cpp so the tests compile and FAIL. Commit `test(06-01): add failing MidiFileWriter round-trip tests`.

    GREEN: implement per 06-RESEARCH.md Pattern 1 — copy its verified shape:

    Header (`Source/MidiGen/MidiFileWriter.h`): `#pragma once`, `#include <JuceHeader.h>`, `#include "MidiSetRow.h"`, `#include "../Analysis/AnalysisResult.h"` (KeyResult lives there — the MidiGen folder-boundary rule explicitly permits AnalysisResult.h). Namespace `MidiFileWriter` with:
    - `constexpr int kTicksPerQuarterNote = 960;`
    - `juce::MidiFile buildMidiFile (const MidiSetRow& row, double bpm);`
    - `bool writeToFile (const MidiSetRow& row, double bpm, const juce::File& destination);`
    - `juce::String suggestedFileName (const MidiSetRow& row, const KeyResult& key, double bpm);` (declared now, implemented in Task 2 — stub returning row.id + ".mid" is fine for this task)

    buildMidiFile: `safeBpm = bpm > 0.0 ? bpm : 120.0`. `file.setTicksPerQuarterNote(960)` BEFORE addTrack. Track 0 (meta): `tempoMetaEvent(juce::roundToInt(60000000.0 / safeBpm))` at tick 0, `timeSignatureMetaEvent(4, 4)` at tick 0, `endOfTrack()`. Track 1 (notes): for each NoteEvent, skip if `lengthBeats <= 0.0`; else `noteOn(1, pitch, velocity)` at `startBeats * 960.0`, `noteOff(1, pitch, 0.0f)` at `(startBeats + lengthBeats) * 960.0`; then `updateMatchedPairs()`, `sort()`, append `endOfTrack()` at `getEndTime()`.

    writeToFile: `destination.getParentDirectory().createDirectory()` guard, then `juce::TemporaryFile temp(destination)` + `FileOutputStream` on `temp.getFile()` + `midiFile.writeTo(stream, 1)` + `temp.overwriteTargetFileWithTemporary()` — NEVER a direct FileOutputStream on destination (append-by-default corruption, Pitfall 4).

    Do NOT re-derive tick positions via seconds/BPM anywhere — beat-domain multiply only (anti-pattern in 06-RESEARCH.md; this is what makes EXP-03's bar alignment structural, not incidental). Commit `feat(06-01): implement MidiFileWriter (format 1, TPQN 960, tempo + 4/4 meta)`.
  </action>
  <verify>
    <automated>cmake --build build --target ChordAITests && ./build/ChordAITests_artefacts/Debug/ChordAITests "[midifilewriter]"</automated>
  </verify>
  <done>All [midifilewriter] round-trip/tempo/fallback/time-sig/zero-length/overwrite tests green; MidiFileWriter.cpp compiled into both targets via CHORDAI_MIDIGEN_SOURCES.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: suggestedFileName — MIDI-pack-style, filesystem-safe naming</name>
  <files>Source/MidiGen/MidiFileWriter.cpp, Tests/MidiFileWriterTests.cpp</files>
  <behavior>
    Tests (same file, same [midifilewriter] tag):
    - Row id "pop-trap", key {tonicPitchClass: 8 (G#), isMajor: false}, bpm 128.2 -> "pop-trap_G#m_128bpm.mid" (bpm rounded to int, minor suffix "m", major = no suffix).
    - Row id "as-is", key {0 (C), isMajor: true}, bpm 90.0 -> "as-is_C_90bpm.mid".
    - bpm <= 0 -> "..._120bpm.mid" (same fallback as buildMidiFile).
    - Filesystem safety: for every RowStyle's id x a sample of keys, the result contains none of `/ \ : * ? " < > |` and no whitespace (row.id is already a safe slug; '#' from note names is legal on macOS and Windows).
    - Uniqueness: all 5 row ids with the same key/bpm produce 5 distinct names.
  </behavior>
  <action>
    RED: write the naming tests against the Task 1 stub — they must fail. GREEN: implement in MidiFileWriter.cpp per 06-RESEARCH.md Code Examples: duplicate ChordNameFormatter.h's 12-entry `kNoteNames` array verbatim as a file-local constant (research's explicit call: duplicate, don't refactor the shared header this phase), then `row.id + "_" + noteName + (key.isMajor ? "" : "m") + "_" + juce::String(juce::roundToInt(bpm > 0.0 ? bpm : 120.0)) + "bpm.mid"`. Use row.id, never row.label (label contains '/', '&', spaces). Commits: `test(06-01): add failing suggestedFileName tests` then `feat(06-01): implement MIDI-pack-style suggestedFileName`.
  </action>
  <verify>
    <automated>cmake --build build --target ChordAITests && ./build/ChordAITests_artefacts/Debug/ChordAITests "[midifilewriter]" && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <done>Naming tests green; full suite green (109/109 baseline + new tests, zero regressions).</done>
</task>

</tasks>

<verification>
- `./build/ChordAITests_artefacts/Debug/ChordAITests "[midifilewriter]"` — all new tests green
- `ctest --test-dir build --output-on-failure` — full suite green (no regressions on the 109-test baseline)
- `grep MidiFileWriter.cpp CMakeLists.txt` — wired via CHORDAI_MIDIGEN_SOURCES (both targets)
- No plugin-target behavior change this plan (pure new module) — pluginval gate lands with the first processBlock-touching wave (06-02)
</verification>

<success_criteria>
- EXP-03 core evidenced by unit tests: exported MIDI is bar-aligned (beat-domain * 960 tick math, bar-2 tick assertion) and carries the detected tempo (tempo meta round-trip incl. non-integer bpm + bpm<=0 fallback)
- Single shared writer both later export paths (06-03 drag + save) will call — no second conversion path exists
- suggestedFileName ready for both the drag temp file and the save dialog's initial name
</success_criteria>

<output>
After completion, create `.planning/phases/06-row-preview-export/06-01-SUMMARY.md`
</output>
