---
phase: 06-row-preview-export
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [PRV-01, EXP-01, EXP-02, EXP-03]

must_haves:
  truths:
    - "Dragging a MIDI row from the plugin into FL Studio's piano roll/channel rack imports the notes correctly (right pitches, right bar positions, project picks up the row's tempo from the file)"
    - "Each of the 5 rows auditions with an audible piano-ish tone; play/stop works; only one row plays at a time; playback stops on region-change regeneration"
    - "The save icon's native dialog saves a .mid that opens correctly in the DAW, into ~/Documents/ChordAI MIDI/ by default"
    - "Full suite and pluginval strictness 5 (VST3+AU) are green on a fresh build at phase close"
    - "PRV-01, EXP-01, EXP-02, EXP-03 are checked off in REQUIREMENTS.md after user approval"
  artifacts:
    - path: "build/ChordAI_artefacts/Standalone/ChordAI.app"
      provides: "Runnable Standalone build for the human checkpoint"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PRV-01/EXP-01/EXP-02/EXP-03 marked complete + traceability rows updated"
      contains: "[x] **EXP-01**"
  key_links:
    - from: "Plugin MidiRowView drag"
      to: "FL Studio piano roll"
      via: "human-observed: OS drag imports notes + tempo (not automatable)"
      pattern: "n/a (manual verification)"
---

<objective>
Close Phase 6 with the full gate (fresh build, full suite, pluginval strictness 5 VST3+AU) and the human checkpoint on the user's own DAW: drag each MIDI row into FL Studio's piano roll (the user's exact demanded MIDI-pack workflow), audition every row, and exercise the native save dialog. OS-level drag-and-drop, native dialogs, and sound character have no automated tooling (06-VALIDATION.md's three justified manual-only rows) — this checkpoint is the final evidence for PRV-01/EXP-01/EXP-02/EXP-03.

Purpose: phase-gate + human evidence before /gsd:verify-work; the 03-06/05-06 precedent (real-track human checks catch what tests can't).
Output: verified phase, four requirements checked off, user approval on the export contract.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-row-preview-export/06-03-SUMMARY.md
@.planning/phases/06-row-preview-export/06-VALIDATION.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

All commands run from /Users/test/Documents/prjcts/chordai. Builds: Bash timeout 600000 ms.
Git commits: plain conventional messages, NO co-author/attribution trailers.
User's DAW for EXP-01 is FL Studio (per STATE.md demand signal); Ableton/Logic spot-checks are optional here — the full per-DAW matrix is Phase 7 (PLT-03). The user's real test track (TOCK.mp3, 75s, 128 BPM / G# minor from prior checkpoints) is the preferred input for continuity.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase gate — fresh full build, full suite, pluginval, Standalone launch-ready</name>
  <files>(none — verification only)</files>
  <action>
    Run the complete phase gate:
    1. `cmake --build build` — all targets including Standalone (COPY_PLUGIN_AFTER_BUILD installs VST3/AU for pluginval).
    2. Full ctest suite — every Phase 6 tag ([midifilewriter], [auditionrenderer], [processoraudition], [midisetspanellayout]) plus the entire pre-existing baseline green.
    3. pluginval strictness 5 for VST3 and AU — the audition read path lives in processBlock; the RT check is the point, not a formality.
    4. Standalone exists and smoke-launches: `ls build/ChordAI_artefacts/Standalone/ChordAI.app` + `tools/smoke-test-standalone.sh`.
    If anything is red: STOP and fix forward (or report honestly) before presenting the checkpoint — never hand the user a broken build to verify.
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3" && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component" && ls build/ChordAI_artefacts/Standalone/ChordAI.app</automated>
  </verify>
  <done>Full suite green, pluginval strictness 5 green (both formats), Standalone built and launchable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human checkpoint — drag into FL Studio, audition every row, save dialog</name>
  <what-built>Phase 6 complete row preview & export: every generated MIDI row now has play/stop audition (built-in deterministic piano-ish tone, RT-safe playback), drag-out as a .mid file straight into the DAW (Ableton-safe deferred temp cleanup), and a native save dialog with MIDI-pack-style file names (row_Key_BPMbpm.mid) defaulting to ~/Documents/ChordAI MIDI/.</what-built>
  <how-to-verify>
    Part A — Audition (Standalone):
    1. `open build/ChordAI_artefacts/Standalone/ChordAI.app`, drop TOCK.mp3, wait for analysis + rows.
    2. Click each row's play icon (top icon in the label gutter): a piano-ish rendition of that row plays; the icon becomes a stop square. Confirm each of the 5 rows sounds — and sounds like its visual pattern (bass = low sparse line, house = short off-beat stabs, R&B = fuller chords).
    3. Click play on row A, then play on row B while A is playing — A stops, B plays (one at a time). Click stop mid-play — silence immediately. Let one row play to the end — icon returns to play by itself.
    4. Mid-audition, drag the analysis region selector — playback stops cleanly (no stale audio, no glitch), rows regenerate.

    Part B — Drag-out into FL Studio (EXP-01, the core demand):
    5. Open FL Studio with an empty project. From the plugin (Standalone window is fine), press-and-drag from a row's note area into FL Studio's channel rack / piano roll.
    6. Confirm: notes land in the piano roll with correct pitches and bar-aligned positions matching the row's visual pattern; the imported clip's tempo matches the detected BPM (~128 for TOCK). Repeat for at least 3 of the 5 rows including Bass.
    7. Drag the same row a second time (temp-file cleanup/rewrite path) — still imports correctly.

    Part C — Save dialog (EXP-02):
    8. Click a row's save icon (bottom icon in the gutter): native macOS save dialog opens, pre-filled name like "pop-trap_G#m_128bpm.mid", default folder ~/Documents/ChordAI MIDI/. Save, then drag/import the saved file into FL Studio — same correct notes and tempo.
    9. Save a second row — the dialog remembers the folder you last chose.

    Optional spot-check (full matrix is Phase 7): repeat one drag in Ableton Live and/or Logic Pro if convenient.
  </how-to-verify>
  <files>(none — manual verification)</files>
  <action>Present the how-to-verify steps to the user verbatim, then STOP and wait for the resume signal. Do not proceed, commit, or write the SUMMARY until the user responds. If the user reports a defect, fix it in a surgical commit (03-06/02-04/05-06 precedent: checkpoint defects are fixed live within the checkpoint plan), re-run the Task 1 gate, and re-present the affected checks.</action>
  <verify>Human confirmation of Parts A-C (manual-only: OS drag-and-drop, native dialog, and audition sound quality cannot be automated — 06-VALIDATION.md Manual-Only Verifications)</verify>
  <done>User replied "approved" (or all reported defects fixed and re-approved); PRV-01/EXP-01/EXP-02/EXP-03 evidenced end-to-end on the user's real DAW and track.</done>
  <resume-signal>Type "approved" if Parts A-C pass, or describe exactly what failed (which part, which step number, which row)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Mark PRV-01 / EXP-01 / EXP-02 / EXP-03 complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    After (and only after) user approval: in .planning/REQUIREMENTS.md, flip the four checkboxes to `[x]` (PRV-01, EXP-01, EXP-02, EXP-03) and update their four Traceability rows from "Pending" to "Complete". Commit `docs(06): mark PRV-01/EXP-01/EXP-02/EXP-03 complete after FL Studio checkpoint`.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*PRV-01\|\[x\] \*\*EXP-01\|\[x\] \*\*EXP-02\|\[x\] \*\*EXP-03" .planning/REQUIREMENTS.md | grep -q 4 && echo OK</automated>
  </verify>
  <done>All four Phase 6 requirements checked off with traceability rows updated; committed.</done>
</task>

</tasks>

<verification>
- Task 1 gate green in full (suite + pluginval both formats + Standalone)
- User approval received on Parts A-C, or defects fixed and re-verified
- REQUIREMENTS.md shows all four Phase 6 requirements complete
</verification>

<success_criteria>
- PRV-01: every row auditions with the built-in tone, play/stop/one-at-a-time/auto-stop/regeneration-stop all human-confirmed
- EXP-01: rows drag into FL Studio's piano roll with correct notes, human-confirmed (incl. repeat drag)
- EXP-02: native save dialog produces a valid .mid in the chosen folder with last-dir memory, human-confirmed
- EXP-03: imported/saved MIDI is bar-aligned and carries detected tempo, human-confirmed in the DAW (on top of 06-01's unit-test evidence)
- Phase 6 ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/06-row-preview-export/06-04-SUMMARY.md`
</output>
