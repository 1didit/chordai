---
phase: 04-analysis-ui-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/Analysis/AnalysisPipeline.h
  - Source/Analysis/AnalysisPipeline.cpp
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
  - Tests/AnalysisPipelineTests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [ANL-04]

must_haves:
  truths:
    - "Loading a file auto-triggers background analysis and a result is published to the message thread without any UI interaction"
    - "Two region changes in quick succession publish ONLY the last region's result — a superseded job's late completion is silently discarded"
    - "A no-op region change (same clamped region) does not re-trigger analysis"
    - "analyse() never runs on the message thread — it runs inside a ThreadPoolJob on a dedicated size-1 analysisPool"
    - "The last good AnalysisResult stays published during a re-analysis; it is cleared (nullptr) only when a NEW file load completes"
    - "Full existing suite stays green and pluginval strictness 5 stays green"
  artifacts:
    - path: "Source/Analysis/AnalysisPipeline.h"
      provides: "ThreadPoolJob wrapping ClassicDspChordAnalyzer::analyse with CancelToken adapter and generation-tagged callbacks"
      contains: "shouldExit"
    - path: "Source/PluginProcessor.h"
      provides: "analysisPool, triggerAnalysis(), getAnalysisResult(), isAnalyzing(), getAnalysisProgress(), analysisBroadcaster"
      contains: "triggerAnalysis"
    - path: "Tests/AnalysisPipelineTests.cpp"
      provides: "AutoAnalyzeOnLoad, CancelAndRestart, NoOpRegionDoesNotRetrigger, ClearedOnNewLoad tests via PluginProcessor public API"
      min_lines: 80
    - path: "CMakeLists.txt"
      provides: "AnalysisPipeline.cpp in both targets; PluginProcessor/PluginEditor/UI sources + JucePlugin_Name define in ChordAITests"
      contains: "AnalysisPipeline.cpp"
  key_links:
    - from: "Source/PluginProcessor.cpp loadAudioFile completion callback"
      to: "triggerAnalysis()"
      via: "clear stale analysisResult (atomic_store nullptr) then trigger, on successful load only"
      pattern: "triggerAnalysis"
    - from: "Source/PluginProcessor.cpp setSelectedRegion"
      to: "triggerAnalysis()"
      via: "equality guard: only when the clamped region actually changed"
      pattern: "clamped == selectedRegion"
    - from: "Source/Analysis/AnalysisPipeline.cpp runJob"
      to: "message thread"
      via: "juce::MessageManager::callAsync for both progress and completion"
      pattern: "callAsync"
    - from: "progress/completion callbacks in triggerAnalysis"
      to: "analysisGeneration"
      via: "captured generation compared against analysisGeneration.load(); mismatch = discard"
      pattern: "analysisGeneration"
---

<objective>
Wrap the frozen, already-verified synchronous ChordAnalyzer engine in a cancellable, restartable background pipeline: a new AnalysisPipeline ThreadPoolJob plus generation-guarded trigger/publish plumbing on ChordAIAudioProcessor, auto-triggered on load complete and on real region changes.

Purpose: ANL-04's backbone — "analysis runs on a background thread, UI stays responsive." This is also the exact cancel-and-restart mechanism Phase 5's GEN-04 (regenerate on region change) will reuse.
Output: AnalysisPipeline.h/.cpp, extended PluginProcessor with triggerAnalysis()/getAnalysisResult()/isAnalyzing()/getAnalysisProgress()/analysisBroadcaster, AnalysisPipelineTests green, full regression + pluginval green.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-analysis-ui-integration/04-RESEARCH.md
@.planning/phases/04-analysis-ui-integration/04-VALIDATION.md
@Source/Analysis/ChordAnalyzer.h
@Source/Analysis/AnalysisResult.h
@Source/Analysis/ClassicDspChordAnalyzer.h
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/Import/AudioFileLoader.h
@Tests/WaveformRegionTests.cpp
@CMakeLists.txt

<interfaces>
<!-- Frozen contracts and existing patterns the executor uses directly — no exploration needed. -->

From Source/Analysis/ChordAnalyzer.h (FROZEN — do not modify):
```cpp
struct CancelToken { virtual bool shouldCancel() const = 0; };
using ProgressCallback = std::function<void (double fractionComplete, const juce::String& stage)>;
virtual AnalysisResult analyse (const juce::AudioBuffer<float>& audio, double sampleRate,
                                juce::Range<double> regionSeconds,
                                const ProgressCallback& onProgress,
                                const CancelToken& cancelToken) = 0;
// Doc comment names the Phase 4 adapter verbatim:
//   bool shouldCancel() const override { return job.shouldExit(); }
// ProgressCallback fires only ~5x/run ("decoding" 0.10, "beat" 0.35, "chroma" 0.70, "key" 0.80, "chords" 1.0)
// analyse() returns chords.empty() + wasCancelled=true if cancelled; never throws.
```

From Source/Analysis/AnalysisResult.h (FROZEN — do not modify):
```cpp
struct AnalysisResult {
    double sampleRate; juce::Range<double> analyzedRegionSeconds;
    double bpm; std::vector<double> beatTimesSeconds; std::vector<int> barStartBeatIndices;
    KeyResult key; std::vector<ChordSegment> chords; bool wasCancelled;
};
```

From Source/PluginProcessor.h (existing members to mirror):
```cpp
juce::ThreadPool loaderPool { 1 };                       // existing decode pool — do NOT reuse for analysis
std::shared_ptr<const LoadedAudio> loadedAudio;          // access ONLY via std::atomic_load/atomic_store
juce::Range<double> selectedRegion;                      // message-thread only
std::shared_ptr<int> aliveToken = std::make_shared<int>(0); // weak_ptr guard for async callbacks
juce::ChangeBroadcaster loadBroadcaster;
```

From Source/Import/LoadedAudio.h (via AudioFileLoader.h): LoadedAudio has `buffer` (juce::AudioBuffer<float>), `sampleRate`, `lengthSeconds`, `sourceFile`.

Message-pump test technique (Tests/WaveformRegionTests.cpp, ThumbnailPopulates — reuse verbatim):
```cpp
juce::ScopedJuceInitialiser_GUI guiInit;
auto deadline = juce::Time::getMillisecondCounter() + 5000;
while (!condition && juce::Time::getMillisecondCounter() < deadline)
    juce::MessageManager::getInstance()->runDispatchLoopUntil (50);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — test scaffold, API surface stubs, CMake wiring (RED)</name>
  <files>Tests/AnalysisPipelineTests.cpp, Source/Analysis/AnalysisPipeline.h, Source/Analysis/AnalysisPipeline.cpp, Source/PluginProcessor.h, CMakeLists.txt</files>
  <behavior>
    Tests drive ChordAIAudioProcessor's PUBLIC API only, with ScopedJuceInitialiser_GUI + the runDispatchLoopUntil pump technique from WaveformRegionTests.ThumbnailPopulates. Fixture: write a local helper that renders a ~3s 220 Hz sine to a temp WAV via juce::WavAudioFormat (pattern exists in WaveformRegionTests.cpp's writeWavFixture; a tone — not silence — makes analysis take a non-trivial, observable amount of time and produce non-empty results).
    - Test "AnalysisPipelineTests.AutoAnalyzeOnLoad": loadAudioFile(fixture); pump until getLoadedAudio()!=nullptr (5s deadline); pump until getAnalysisResult()!=nullptr AND !isAnalyzing() (60s deadline); REQUIRE result->analyzedRegionSeconds spans ~the whole file (start ≈ 0.0, end ≈ lengthSeconds within 0.01).
    - Test "AnalysisPipelineTests.CancelAndRestart": after AutoAnalyzeOnLoad-style completion, call setSelectedRegion({0.25L, 0.9L}) then IMMEDIATELY setSelectedRegion({0.1L, 0.6L}) (L = lengthSeconds) with no pump in between; pump until !isAnalyzing() (60s deadline); REQUIRE getAnalysisResult()->analyzedRegionSeconds ≈ {0.1L, 0.6L} — the generation guard must ensure only the LAST region's result is published even though the first job may finish late (cancellation is cooperative).
    - Test "AnalysisPipelineTests.NoOpRegionDoesNotRetrigger": after completion, capture `auto before = proc.getAnalysisResult();` then setSelectedRegion(proc.getSelectedRegion()); pump ~300ms; REQUIRE !isAnalyzing() AND getAnalysisResult() == before (shared_ptr pointer identity — nothing republished).
    - Test "AnalysisPipelineTests.ClearedOnNewLoad": after completion, capture firstAudio = getLoadedAudio(); loadAudioFile(fixture) again; pump until getLoadedAudio() != firstAudio (pointer changes on reload); REQUIRE getAnalysisResult() == nullptr at that instant (stale result cleared before the new ~1s+ analysis can complete). If empirically flaky, keep the pointer-change wait but assert instead that the FINAL result (after !isAnalyzing()) has analyzedRegionSeconds == whole file, and document why.
  </behavior>
  <action>
    1. Create Source/Analysis/AnalysisPipeline.h/.cpp as a compilable SKELETON only (class declared per Task 2's shape; runJob() body may return jobHasFinished without doing work yet).
    2. Add to Source/PluginProcessor.h the new public API as declarations with STUB bodies in a minimal form so the test file compiles: `void triggerAnalysis();` (empty stub), `std::shared_ptr<const AnalysisResult> getAnalysisResult() const;` (returns nullptr), `bool isAnalyzing() const;` (returns false), `double getAnalysisProgress() const;` (returns 0.0), `juce::ChangeBroadcaster analysisBroadcaster;`. Include "Analysis/AnalysisResult.h". Private members land in Task 2.
    3. Write Tests/AnalysisPipelineTests.cpp per <behavior>.
    4. CMakeLists.txt: append `Source/Analysis/AnalysisPipeline.cpp` to target_sources(ChordAI ...). Append to target_sources(ChordAITests ...): `Tests/AnalysisPipelineTests.cpp`, `Source/Analysis/AnalysisPipeline.cpp`, `Source/PluginProcessor.cpp`, `Source/PluginEditor.cpp`, `Source/UI/ConveyorBeltComponent.cpp`, `Source/UI/MidiSetsPlaceholder.cpp`, `Source/UI/WaveformView.cpp`, `Source/UI/RegionSelectorOverlay.cpp` (PluginProcessor.cpp references createEditor → the editor + all UI components must link). Add `JucePlugin_Name="ChordAI"` to target_compile_definitions(ChordAITests ...) — the only JucePlugin_* macro used in Source/ (verified by grep: PluginProcessor.h:19 getName()). If link errors surface for gui symbols, add `juce::juce_gui_extra` to target_link_libraries(ChordAITests ...).
    5. Reconfigure + build: `cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug && cmake --build build` (Bash timeout 600000). Build MUST succeed; the four new tests MUST FAIL (RED) against the stubs — AutoAnalyzeOnLoad times out with getAnalysisResult()==nullptr.
    Do NOT touch ChordAnalyzer.h or AnalysisResult.h (frozen, byte-identical since 03-01).
    Commit: `test(04-01): add failing AnalysisPipeline tests + CMake test-target wiring`
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build -R "ChordAITests.AnalysisPipelineTests" --output-on-failure; test $? -ne 0</automated>
  </verify>
  <done>Build compiles cleanly with PluginProcessor/PluginEditor/UI sources in the test target; all 4 AnalysisPipelineTests exist and fail (RED); frozen headers untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AnalysisPipeline + generation-guarded triggerAnalysis (GREEN)</name>
  <files>Source/Analysis/AnalysisPipeline.h, Source/Analysis/AnalysisPipeline.cpp, Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <action>
    Implement per 04-RESEARCH.md Patterns 1-3 (code sketches there are pre-verified against this repo — follow them closely):

    AnalysisPipeline (Source/Analysis/AnalysisPipeline.h/.cpp): juce::ThreadPoolJob ("ChordAnalysis") holding std::shared_ptr<const LoadedAudio> audio, juce::Range<double> region, uint64_t generation, ProgressCallback onProgress(generation, fraction, stage), CompletionCallback onDone(generation, shared_ptr<const AnalysisResult>). runJob(): local `struct Adapter : ChordAnalyzer::CancelToken { ThreadPoolJob& job; bool shouldCancel() const override { return job.shouldExit(); } }`; local ClassicDspChordAnalyzer; wrap the engine ProgressCallback to forward via MessageManager::callAsync (capture cb+gen+fraction+stage by value); call analyse(audio->buffer, audio->sampleRate, region, progress, cancelToken); make_shared<const AnalysisResult> of the return; callAsync the completion; return jobHasFinished. No references into processor state — only captured snapshots (mirrors AudioFileLoadJob's discipline). Include "ClassicDspChordAnalyzer.h" and "../Import/LoadedAudio.h".

    PluginProcessor.h private additions: `juce::ThreadPool analysisPool { 1 };` — declare it AFTER loaderPool with a comment mirroring loaderPool's destruction-order note (ThreadPool dtor waits for/cancels in-flight jobs; the analysis job only reads its own captured snapshots, but keep pools grouped and documented); `std::shared_ptr<const AnalysisResult> analysisResult;` (access ONLY via std::atomic_load/atomic_store — same Apple libc++ comment as loadedAudio); `std::atomic<uint64_t> analysisGeneration { 0 };`; `bool analyzingFlag = false;` and `double analysisProgress = 0.0;` (message-thread only).

    triggerAnalysis() (message-thread only): bail if getLoadedAudio()==nullptr; `analysisPool.removeAllJobs (true, 0);` (non-blocking cancel signal — never track a raw ThreadPoolJob*); `const uint64_t generation = ++analysisGeneration;`; set analyzingFlag=true, analysisProgress=0.0 SYNCHRONOUSLY (no busy-state gap — Pitfall 3); analysisBroadcaster.sendChangeMessage(); capture std::weak_ptr<int> weakAlive(aliveToken); onProgress: bail if weakAlive.expired() OR gen != analysisGeneration.load(); else analysisProgress=fraction, sendChangeMessage. onDone: bail on same guard (superseded job must NOT touch analysisResult — keep last good result visible); else atomic_store(&analysisResult, result), analyzingFlag=false, analysisProgress=1.0, sendChangeMessage. `analysisPool.addJob(new AnalysisPipeline(audio, selectedRegion, generation, onProgress, onDone), true);`

    getAnalysisResult(): return std::atomic_load(&analysisResult). isAnalyzing()/getAnalysisProgress(): return the message-thread fields.

    Trigger point 1 — loadAudioFile completion callback, success branch only: after setting selectedRegion/RegionState::write, add `std::atomic_store (&analysisResult, std::shared_ptr<const AnalysisResult>());` (new song must never show the old song's chords) BEFORE loadBroadcaster.sendChangeMessage(); then call triggerAnalysis() after the broadcast.

    Trigger point 2 — setSelectedRegion: compute clamped into a local; `if (clamped == selectedRegion) return;` (no-op guard, Pitfall 5 — RegionSelectorOverlay::setTotalLength re-fires onRegionChanged with the whole-file default on every editor reopen); then assign, RegionState::write, triggerAnalysis(). Do NOT add any other trigger call sites — all "when does analysis run" logic stays in the processor, never the Editor.

    Run the new tests until green, then the FULL suite (64 existing + new) — zero regressions.
    Commit: `feat(04-01): background AnalysisPipeline with generation-guarded cancel-and-restart`
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build -R "ChordAITests.AnalysisPipelineTests" --output-on-failure && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <done>All 4 AnalysisPipelineTests green; full suite green (no regression among the existing 64); ChordAnalyzer.h/AnalysisResult.h byte-identical (git diff shows no changes to them).</done>
</task>

<task type="auto">
  <name>Task 3: Wave regression gate — pluginval strictness 5 (VST3 + AU)</name>
  <files>(none — verification only)</files>
  <action>
    Build the plugin targets so COPY_PLUGIN_AFTER_BUILD installs fresh binaries: `cmake --build build` (Bash timeout 600000). Then run pluginval strictness 5 against both installed formats:
    `tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3"`
    `tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component"`
    Both must end "ALL TESTS PASSED". pluginval's Editor Automation pass opens/closes the editor repeatedly — this specifically exercises the editor-reopen path against the new analysis state (Pitfall 5's no-op guard). If either run fails, fix in this plan's files before proceeding.
  </action>
  <verify>
    <automated>tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3" && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component"</automated>
  </verify>
  <done>pluginval strictness 5 green for VST3 and AU with the new pipeline wired in.</done>
</task>

</tasks>

<verification>
- `ctest --test-dir build --output-on-failure` — full suite green (existing 64 + 4 new)
- pluginval strictness 5 green (VST3 + AU)
- `git diff --stat HEAD~2 -- Source/Analysis/ChordAnalyzer.h Source/Analysis/AnalysisResult.h` shows zero changes (frozen contracts untouched)
- grep confirms key links: `grep -n "triggerAnalysis" Source/PluginProcessor.cpp` (2 call sites: load-complete callback + setSelectedRegion), `grep -n "removeAllJobs" Source/PluginProcessor.cpp`, `grep -n "callAsync" Source/Analysis/AnalysisPipeline.cpp`
</verification>

<success_criteria>
- Background analysis auto-runs on load and on real region changes; message thread never executes analyse()
- Generation guard proven by test: rapid double region change publishes only the last region's result
- No-op region changes (editor reopen) provably do not re-trigger
- Last good result persists through re-analysis; cleared on new file load
- Full suite + pluginval green
</success_criteria>

<output>
After completion, create `.planning/phases/04-analysis-ui-integration/04-01-SUMMARY.md`
</output>
