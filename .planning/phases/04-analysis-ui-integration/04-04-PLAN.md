---
phase: 04-analysis-ui-integration
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified: []
autonomous: false
requirements: [ANL-04, ANL-05]

must_haves:
  truths:
    - "A one-time Release build exists and its full test suite is green (the real optimization level that ships)"
    - "A real ~3-minute track analyzes in seconds (not minutes) on the Release build — the actual ANL-04 UX claim, not the Debug proxy number"
    - "UI stays responsive (region drag, window resize) while a 3+ minute track analyzes in Standalone"
    - "Progress is visibly shown on the conveyor for the duration of analysis, and a chunk falls at completion"
    - "Chord timeline is legible and horizontally aligned with the waveform on a real track"
  artifacts:
    - path: "build-release/ChordAITests_artefacts/Release/ChordAITests"
      provides: "Release-optimized test binary for the timing measurement (build output, not committed)"
  key_links:
    - from: "Release timing measurement"
      to: "ANL-04 'a 3-minute song analyzes in seconds'"
      via: "RealTrackHarness elapsed-seconds printout on a real track (Debug's ~35s/180s proxy is NOT the number — Pitfall 4)"
      pattern: "RealTrackHarness"
---

<objective>
Close Phase 4 with the two things only a human + a Release build can prove: the real "seconds not minutes" timing measurement, and the felt UI experience (responsiveness, progress visibility, timeline legibility) in Standalone on a real track.

Purpose: ANL-04's success criteria 1-3 and ANL-05's criterion 4 are UX claims — the Debug PerformanceBudget test is only a regression proxy (its own comment says Release meets the ~5-10s budget; Debug measures ~35s for 180s).
Output: Recorded Release timing number + human-verified checkpoint evidence; phase ready for /gsd:verify-work.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-analysis-ui-integration/04-RESEARCH.md
@.planning/phases/04-analysis-ui-integration/04-VALIDATION.md
@.planning/phases/04-analysis-ui-integration/04-01-SUMMARY.md
@.planning/phases/04-analysis-ui-integration/04-02-SUMMARY.md
@.planning/phases/04-analysis-ui-integration/04-03-SUMMARY.md

<interfaces>
Known paths (verified against the existing Debug build's layout):
- Release test binary will land at: build-release/ChordAITests_artefacts/Release/ChordAITests
- Release Standalone app will land at: build-release/ChordAI_artefacts/Release/Standalone/ChordAI.app
- RealTrackHarness (Tests/ClassicDspChordAnalyzerTests.cpp, hidden tag "[.realtrack]") reads env var CHORDAI_REAL_TRACK=/path/to/audio, prints BPM/key/chords AND "Analysis time: N s" to stdout, and SUCCEEDs with a hint if the env var is unset.
- CAUTION: COPY_PLUGIN_AFTER_BUILD TRUE means the Release build will replace the installed VST3/AU with Release binaries — fine (that's what ships), just expected behavior, not an error.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: One-time Release configure + build + Release suite run</name>
  <files>(build-release/ — build output only, not committed; ensure it is git-ignored)</files>
  <action>
    1. Check .gitignore covers `build-release/` (the pattern may already be `build*/` — verify with `git check-ignore build-release`); if not, add it and include in the commit.
    2. Configure + build (Bash timeout 600000): `cmake -B build-release -G Ninja -DCMAKE_BUILD_TYPE=Release && cmake --build build-release`.
    3. Run the full suite on the Release build: `ctest --test-dir build-release --output-on-failure` — all tests must pass at -O2/-O3 (catches any optimization-sensitive behavior; PerformanceBudget will be dramatically faster than Debug).
    4. This is a ONE-TIME manual-checkpoint prerequisite per 04-RESEARCH Open Question 4 — do NOT add Release CI scripts/targets (formalizing Release CI is deferred to Phase 7).
    If the user has previously used a real track for checkpoints (Phase 3 used the user's 75s trap beat / TOCK.mp3), do NOT guess its path — the human runs the timed harness in Task 2.
  </action>
  <verify>
    <automated>ctest --test-dir build-release --output-on-failure</automated>
  </verify>
  <done>build-release/ exists, full suite green in Release, Release Standalone + test binary present at the documented artefact paths.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human checkpoint — Release timing + Standalone UX verification</name>
  <what-built>Phase 4 complete stack: background AnalysisPipeline with cancel-and-restart (04-01), chord timeline band over the waveform (04-02), conveyor progress fill + chunk-fall-on-complete (04-03), all on a fresh Release build (Task 1).</what-built>
  <how-to-verify>
1. Release timing (ANL-04 "seconds not minutes"). From /Users/test/Documents/prjcts/chordai run:
   `CHORDAI_REAL_TRACK="/path/to/your/~3min/track.mp3" ./build-release/ChordAITests_artefacts/Release/ChordAITests "[.realtrack]" -s`
   Read the printed "Analysis time: N s" — expected single-digit-to-low-double-digit seconds for a ~3-minute track (Debug was ~35s for 180s; Release research budget is ~5-10s). Also sanity-check the printed BPM/key/chords look plausible.
2. Launch the Release Standalone: `open build-release/ChordAI_artefacts/Release/Standalone/ChordAI.app`
3. Drop a 3+ minute track onto the window. During analysis:
   - Conveyor shows a gold progress fill and the belt visibly speeds up (ANL-04 "progress is shown")
   - Drag the region selector and resize the window WHILE analysis runs — no freeze, no beachball (ANL-04 "UI stays responsive")
4. When analysis completes:
   - A piano-roll chunk falls off the belt's right end (and it did NOT fall earlier at load time)
   - Named chords (e.g. C, Am, G7, N.C.) appear as coloured blocks on the band above the waveform, aligned with the audio (ANL-05; note: v1 vocabulary is root-position maj/min/dom7/N.C. only — Cmaj7/F/A-style names are NOT expected, per the frozen Phase 3 contract)
5. Drag a smaller region on the waveform → progress re-runs, previous chords stay visible during re-analysis, then blocks update and appear only inside the region
6. Report the measured Release analysis time so it can be recorded in the SUMMARY
  </how-to-verify>
  <action>Present the verification steps above to the user, wait for the timing measurement and the Standalone UX walkthrough, and record the outcome (approved + measured seconds / concrete issue report per step). No code changes in this task — it gates phase completion on the four manual-only rows of 04-VALIDATION.md that no automated test can cover (Release timing, felt responsiveness, progress visibility, timeline legibility).</action>
  <verify>User responds with "approved" plus the measured Release analysis time, or a concrete issue report</verify>
  <done>User confirms: real ~3-min track analyzes in seconds on Release; UI stayed responsive during analysis; progress fill + belt speed-up visible; chunk fell on completion (not on load); chord timeline legible and aligned. Measured time recorded.</done>
  <resume-signal>Type "approved" with the measured Release time, or describe issues (which step, what you saw)</resume-signal>
</task>

</tasks>

<verification>
- Release full suite green (Task 1 automated)
- Human-verified: timing in seconds, responsive UI, visible progress, chunk on completion, legible aligned timeline
- Both requirement IDs evidenced: ANL-04 (background + responsive + progress + fast), ANL-05 (named chords on timeline over waveform)
</verification>

<success_criteria>
- Measured Release analysis time for a real ~3-minute track recorded, within "seconds not minutes"
- All four manual-only behaviors from 04-VALIDATION.md's Manual-Only Verifications table confirmed by the user
- Phase 4 ready for /gsd:verify-work; REQUIREMENTS.md ANL-04/ANL-05 can be marked complete after verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-analysis-ui-integration/04-04-SUMMARY.md` (include the measured Release timing number)
</output>
