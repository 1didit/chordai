---
phase: 05-midi-conveyor-generation
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified: []
autonomous: false
requirements: [GEN-01, GEN-02, GEN-03, GEN-04]

must_haves:
  truths:
    - "On a real track in Standalone, 5 labeled MIDI rows are visible and legible in the bottom band after analysis completes"
    - "The 3 style rows read as visually/structurally distinct note patterns on the same source track (audible distinctness is Phase 6's audition)"
    - "The bass row visibly follows the progression as a low, sparse line distinct from the chord rows"
    - "Dragging the analysis region re-runs analysis and the rows visibly regenerate together with the chord timeline"
    - "Full suite and pluginval strictness 5 are green at phase close"
  artifacts:
    - path: "build/ChordAI_artefacts/Standalone/ChordAI.app"
      provides: "Runnable Standalone build for the human checkpoint"
  key_links:
    - from: "Standalone editor bottom band"
      to: "PluginProcessor::getMidiSetRows"
      via: "human-observed: rows appear when analysis completes and change when the region changes"
      pattern: "n/a (manual verification)"
---

<objective>
Close Phase 5 with the full-suite/pluginval phase gate and the human real-track checkpoint: 5 rows visible, legible, style-distinct, regenerating on region drag in Standalone.

Purpose: pixel-art visual legibility and cross-style visual distinctness have no automated tooling in this project (Phase 4's manual chord-timeline-legibility precedent, and 03-06's proof that real-track human checks catch what synthetic fixtures can't) — this checkpoint is the phase's final evidence for GEN-01..04 before /gsd:verify-work.
Output: verified phase; user approval on the visual contract.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-midi-conveyor-generation/05-05-SUMMARY.md
@.planning/STATE.md

All commands run from /Users/test/Documents/prjcts/chordai. Builds: Bash timeout 600000 ms.
No commits expected from this plan (verification only); SUMMARY.md only.
The user's own real test track (TOCK.mp3, 75s, known 128 BPM / G# minor from Phase 3/4 checkpoints) is the preferred test input — ask the user to use the same file for continuity.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase gate — fresh full build, full suite, pluginval, Standalone launch-ready</name>
  <files>(none — verification only)</files>
  <action>
    Run the complete phase gate from a clean incremental build:
    1. `cmake --build build` — all targets including Standalone.
    2. Full ctest suite — every Phase 5 test plus all pre-existing tests green.
    3. pluginval strictness 5 for VST3 and AU.
    4. Confirm the Standalone app exists and launches: `ls build/ChordAI_artefacts/Standalone/ChordAI.app` then `tools/smoke-test-standalone.sh` (established smoke script).
    If anything is red: STOP and fix forward (or report honestly) before presenting the checkpoint — never hand the user a broken build to verify.
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/VST3/ChordAI.vst3" && tools/pluginval.app/Contents/MacOS/pluginval --strictness-level 5 --validate-in-process --validate "$HOME/Library/Audio/Plug-Ins/Components/ChordAI.component" && ls build/ChordAI_artefacts/Standalone/ChordAI.app</automated>
  </verify>
  <done>Full suite green, pluginval strictness 5 green (both formats), Standalone built and launchable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human checkpoint — 5 rows visible, legible, distinct; regenerate on region drag</name>
  <what-built>Phase 5 complete MIDI conveyor generation: one analysis pass now fans out into 5 deterministic MIDI rows (Detected as-is, Pop/Trap dark triads, R&B/Neo-Soul extended voicings with voice leading, Electronic/House off-beat stabs, Bass root line), rendered as labeled mini piano-roll strips in the bottom 140px band, regenerating automatically on region change via the existing analysis pipeline.</what-built>
  <how-to-verify>
    1. Launch: `open build/ChordAI_artefacts/Standalone/ChordAI.app`
    2. Drag TOCK.mp3 (the same 75s track from the Phase 3/4 checkpoints) onto the window; wait for analysis (~3s, belt speeds up then chunk falls).
    3. Bottom band: confirm 5 labeled rows appear — Detected / Pop / Trap / R&B / Neo-Soul / Electronic / House / Bass — each with visible note rectangles.
    4. Legibility: labels readable at default window size; notes visible (not sub-pixel smears) across the full track length; window resize keeps rows intact.
    5. Style distinctness (visual, content-wise — audition is Phase 6): Pop/Trap = blocky sustained chords low-mid; R&B = taller stacks (more notes per chord), smooth register drift between chords; House = short repeating off-beat ticks high up; Bass = single low sparse line. Each should look clearly different from the others.
    6. Musical sanity: row patterns change where the chord timeline above shows chord changes; silent/N.C. passages show gaps in ALL rows.
    7. Regeneration (GEN-04): drag the waveform region selector to roughly the first half of the track → analysis re-runs → all 5 rows redraw together with the chord timeline (no stale rows, no rows updating out of sync). Drag back to full — rows return.
    8. Fresh-load clear: drop a different audio file → old rows disappear immediately, new rows appear only when the new analysis completes.
  </how-to-verify>
  <files>(none — manual verification in Standalone)</files>
  <action>Present the how-to-verify steps to the user verbatim, then STOP and wait for the resume signal. Do not proceed, commit, or write the SUMMARY until the user responds. If the user reports a defect, fix it in a surgical commit (03-06/02-04 precedent: checkpoint defects are fixed live within the checkpoint plan), re-run the Task 1 gate, and re-present the affected checks.</action>
  <verify>Human confirmation of all 8 checks (manual-only: pixel-art legibility and cross-style visual distinctness have no automated tooling — 05-RESEARCH.md Validation Architecture's two justified manual-only rows; automated coverage for the same behaviors lives in plans 05-02..05-05)</verify>
  <done>User replied "approved" (or all reported defects fixed and re-approved); GEN-01..04 visually evidenced on a real track.</done>
  <resume-signal>Type "approved" if all 8 checks pass, or describe exactly what looks wrong (which row, which check number)</resume-signal>
</task>

</tasks>

<verification>
- Task 1 gate green in full (suite + pluginval + Standalone)
- User approval received on all 8 checkpoint items, or defects fixed and re-verified
</verification>

<success_criteria>
- GEN-01: 5 simultaneous rows human-confirmed visible after one analysis pass
- GEN-02: 3 style rows human-confirmed visually/structurally distinct on a real track
- GEN-03: bass row human-confirmed following the progression with distinct rhythm/register
- GEN-04: rows human-confirmed regenerating on region change, in sync with the chord timeline
- Phase 5 ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/05-midi-conveyor-generation/05-06-SUMMARY.md`
</output>
