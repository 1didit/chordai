---
phase: 05-midi-conveyor-generation
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - Source/MidiGen/BassLineGenerator.cpp
  - Tests/BassLineGeneratorTests.cpp
autonomous: true
requirements: [GEN-03]

must_haves:
  truths:
    - "For every non-NoChord segment, the note sounding at the segment start has pitch class == chord.pitchClass, across all three bass rhythms"
    - "TrapSustain: exactly one sustained note per segment, spanning the full segment, in the C2 register"
    - "RnbRootFifth: 4-beat spans walk root (2 beats) then fifth (root+7 semitones, 2 beats); segments/remainders shorter than 4 beats sustain root only"
    - "HouseFourOnFloor: one short note per beat, all root-class"
    - "NoChord segments emit zero bass notes; two identical calls produce identical output"
  artifacts:
    - path: "Source/MidiGen/BassLineGenerator.cpp"
      provides: "generateBassRow body with three per-style rhythm branches"
      min_lines: 50
    - path: "Tests/BassLineGeneratorTests.cpp"
      provides: "Root-following, per-rhythm timing, register, determinism tests"
      min_lines: 80
  key_links:
    - from: "Source/MidiGen/BassLineGenerator.cpp"
      to: "Source/MidiGen/ChordToneMapper.h"
      via: "rootMidiNote(chord.pitchClass, kAnchorBass) — root derivation shared with every generator"
      pattern: "kAnchorBass"
    - from: "Source/MidiGen/BassLineGenerator.cpp"
      to: "Source/MidiGen/Humanization.h"
      via: "deterministicJitter(noteIndex, kSeedBass, kJitterBass) — only velocity variation"
      pattern: "kSeedBass"
---

<objective>
Implement the bass row generator: detected chord roots at C2 with three style-appropriate rhythms (trap sustain, R&B root+fifth walk, house four-on-the-floor), pure and deterministic.

Purpose: GEN-03 — the bass row follows the detected chord roots with style-appropriate rhythm.
Output: BassLineGenerator.cpp body + BassLineGeneratorTests.cpp green.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-midi-conveyor-generation/05-RESEARCH.md
@.planning/phases/05-midi-conveyor-generation/05-01-SUMMARY.md
@Source/MidiGen/BassLineGenerator.h
@Source/MidiGen/ChordToneMapper.h
@Source/MidiGen/Humanization.h
@Tests/MidiGenFixtures.h

All commands run from /Users/test/Documents/prjcts/chordai. Builds: Bash timeout 600000 ms.
Commits: plain conventional messages, NO co-author/attribution trailers.
CONCURRENCY NOTE: plan 05-02 may execute in parallel in this same repo (disjoint files). Per the 03-04 lesson, run `git status --short` immediately before every `git add`/`git commit` and stage ONLY this plan's two files explicitly (never `git add -A`).

<interfaces>
<!-- Wave 1 (05-01) contracts — implement against these exactly; do not modify the headers. -->

```cpp
// BassLineGenerator.h (frozen signature)
enum class BassRhythm { TrapSustain, RnbRootFifth, HouseFourOnFloor };
std::vector<NoteEvent> generateBassRow (const AnalysisResult&, BassRhythm rhythm);

// ChordToneMapper.h
int rootMidiNote (int pitchClass, int anchorOctaveBase);  // jlimit 0-127
constexpr int kAnchorBass = 36;                            // C2 — SyntheticFixtures' own bass convention

// Humanization.h
float deterministicJitter (uint32_t noteIndex, uint32_t styleSeed, float range);
constexpr uint32_t kSeedBass;  constexpr float kVelBass = 0.85f, kJitterBass = 0.06f; // ±0.03
```

Timing domain: startBeats/lengthBeats derived ONLY from segment beat indices (never seconds/bpm).
Rhythm is segment-relative (Pattern 3). Skip NoChord segments entirely (Pitfall 1).
Velocity: `juce::jlimit (0.0f, 1.0f, kVelBass + deterministicJitter (noteIndex++, kSeedBass, kJitterBass))`,
noteIndex a running counter reset at each generateBassRow call.

Fixtures (Tests/MidiGenFixtures.h): makeFourChordFixture() — Am F C G7, 4 beats each;
makeShortSegmentFixture() — 2-beat C, 3-beat F, 4-beat G; makeNoChordFixture() — C / NoChord / Am.

Test naming: `TEST_CASE ("BassLineGeneratorTests.CaseName", "[basslinegenerator]")`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Root derivation + TrapSustain rhythm</name>
  <files>Source/MidiGen/BassLineGenerator.cpp, Tests/BassLineGeneratorTests.cpp</files>
  <behavior>
    - BassLineGeneratorTests.RootPitchClassMatchesDetectedChord: for ALL THREE rhythms on makeFourChordFixture(): the note whose startBeats == segment.startBeatIndex has pitch % 12 == chord.pitchClass, for every non-NoChord segment; for TrapSustain and HouseFourOnFloor additionally EVERY note is root-class
    - BassLineGeneratorTests.TrapBassSustainsFullSegment: exactly one note per non-NoChord segment; startBeats == startBeatIndex, lengthBeats == endBeatIndex - startBeatIndex; pitch == rootMidiNote(pc, 36) (Am → 45, all pitches in [36, 47])
    - BassLineGeneratorTests.NoChordEmitsNoBassNotes: makeNoChordFixture() → zero notes with startBeats in [4, 8) for all three rhythms
    - BassLineGeneratorTests.BassVelocityDeterministicAndBounded: velocities within [0.82, 0.88]; two identical calls → exactly identical NoteEvent sequences
  </behavior>
  <action>
    Write the TEST_CASEs first (RED — stub returns {}), then implement generateBassRow's shared skeleton: iterate result.chords, skip NoChord, root = rootMidiNote(chord.pitchClass, kAnchorBass), branch on rhythm. Implement the TrapSustain branch: one NoteEvent spanning the whole segment ("sparse, leave room for the 808" — 05-RESEARCH.md Pattern 3 bass table). Leave RnbRootFifth/HouseFourOnFloor branches returning nothing yet (their tests come in Task 2 — keep this commit green by scoping Task 1 assertions to TrapSustain where rhythm-specific).

    NOTE on RootPitchClassMatchesDetectedChord: write it now but parameterize over rhythms via Catch2 GENERATE or three SECTIONs; if the other two branches are still empty this test would fail — so in this task assert TrapSustain only, and EXTEND to all three rhythms in Task 2 (document the extension in the test comment).

    Build + run GREEN. Commit: `feat(05-03): bass row generator — root derivation and trap sustain rhythm`
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build -R "ChordAITests.BassLineGeneratorTests" --output-on-failure</automated>
  </verify>
  <done>Trap bass: one full-length root note per segment in C2 band; NoChord silent; deterministic velocities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RnbRootFifth walk + HouseFourOnFloor rhythms</name>
  <files>Source/MidiGen/BassLineGenerator.cpp, Tests/BassLineGeneratorTests.cpp</files>
  <behavior>
    - BassLineGeneratorTests.RnbBassRootFifthWalk: 4-beat Am segment (start beat 0) → note 1: startBeats 0, lengthBeats 2, pitch 45 (root); note 2: startBeats 2, lengthBeats 2, pitch 52 (root+7, the fifth); makeShortSegmentFixture()'s 2-beat and 3-beat segments → single root note sustaining the whole segment (shorter-than-4 rule); an 8-beat segment (extend fixture locally or reuse scale fixture) → root/fifth, root/fifth (pattern repeats per 4-beat span)
    - BassLineGeneratorTests.HouseBassFourOnTheFloor: 4-beat Am segment → 4 notes at startBeats {0,1,2,3}, each lengthBeats 0.5, each pitch 45 (root class only); 3-beat segment → 3 notes
    - BassLineGeneratorTests.RootPitchClassMatchesDetectedChord now passes for ALL THREE rhythms (extend Task 1's test)
  </behavior>
  <action>
    Write/extend tests first, then implement the two remaining branches:

    RnbRootFifth: for each full 4-beat span within the segment (span offsets 0, 4, 8... where offset+4 ≤ segmentLength): root note at spanStart (2 beats), fifth note (root+7 semitones — stays in-register, no %12 wrap that would drop the fifth BELOW the root) at spanStart+2 (2 beats). Trailing remainder < 4 beats (or a whole segment < 4 beats): one root note sustaining the remainder. Matches the research spec "first half root, second half fifth; shorter segments sustain root only".

    HouseFourOnFloor: one note per whole beat from segment start while beat offset < segmentLength; lengthBeats 0.5 (short, punchy, sits under the off-beat stabs); pitch = root.

    Both use the same running noteIndex/jitter as Task 1. Full BassLineGeneratorTests GREEN + quick regression on MidiGen tests.
    Commit: `feat(05-03): R&B root-fifth walk and house four-on-the-floor bass rhythms`
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build -R "ChordAITests.(BassLineGeneratorTests|ChordToneMapperTests)" --output-on-failure</automated>
  </verify>
  <done>All three rhythms produce exact expected note grids; root-following proven across rhythms; full determinism maintained.</done>
</task>

</tasks>

<verification>
- `ctest --test-dir build -R "ChordAITests.BassLineGeneratorTests" --output-on-failure` — all green
- `ctest --test-dir build --output-on-failure` — full suite green
- `grep -n "juce::Random\|startSeconds" Source/MidiGen/BassLineGenerator.cpp` — empty
</verification>

<success_criteria>
- GEN-03 evidenced at unit level: bass follows detected roots (C2 register) with three style-appropriate rhythms, exact timing per rhythm, silent on NoChord, byte-deterministic
</success_criteria>

<output>
After completion, create `.planning/phases/05-midi-conveyor-generation/05-03-SUMMARY.md`
</output>
