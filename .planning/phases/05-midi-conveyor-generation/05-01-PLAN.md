---
phase: 05-midi-conveyor-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/MidiGen/NoteEvent.h
  - Source/MidiGen/MidiSetRow.h
  - Source/MidiGen/GenerationSettings.h
  - Source/MidiGen/ChordToneMapper.h
  - Source/MidiGen/VoiceLeadingEngine.h
  - Source/MidiGen/Humanization.h
  - Source/MidiGen/AsIsRowGenerator.h
  - Source/MidiGen/AsIsRowGenerator.cpp
  - Source/MidiGen/StyleVoicingGenerators.h
  - Source/MidiGen/StyleVoicingGenerators.cpp
  - Source/MidiGen/BassLineGenerator.h
  - Source/MidiGen/BassLineGenerator.cpp
  - Source/MidiGen/MidiRowBuilder.h
  - Source/MidiGen/MidiRowBuilder.cpp
  - Tests/MidiGenFixtures.h
  - Tests/ChordToneMapperTests.cpp
  - Tests/StyleVoicingTests.cpp
  - Tests/BassLineGeneratorTests.cpp
  - Tests/MidiRowBuilderTests.cpp
  - Tests/PluginProcessorMidiGenTests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [GEN-01]

must_haves:
  truths:
    - "Source/MidiGen/ exists as a pure-C++ module: zero GUI includes, only AnalysisResult.h from Source/Analysis/, beat-domain NoteEvent timing"
    - "Every generator function signature (5 rows) is frozen in headers so Wave 2 plans implement bodies only, with zero further CMakeLists.txt edits"
    - "Interval tables match ChordTemplates.h's root/third/fifth/seventh convention exactly (the detector's own interpretation of quality)"
    - "deterministicJitter is a pure hash function — no juce::Random anywhere under Source/MidiGen/, no mutable state"
    - "A hand-built struct-literal fixture AnalysisResult exists for all downstream tests — no audio rendering needed"
    - "Full existing suite (72/72) still green after CMake wiring"
  artifacts:
    - path: "Source/MidiGen/NoteEvent.h"
      provides: "Pure beat-domain note struct {startBeats, lengthBeats, pitch, velocity}"
      contains: "startBeats"
    - path: "Source/MidiGen/MidiSetRow.h"
      provides: "RowStyle enum + MidiSetRow {id, label, style, notes}"
      contains: "RowStyle"
    - path: "Source/MidiGen/GenerationSettings.h"
      provides: "Empty forward-compatible v1 settings placeholder, documented"
      contains: "GenerationSettings"
    - path: "Source/MidiGen/ChordToneMapper.h"
      provides: "triadIntervals/rnbExtensionIntervals/intervalsToMidiNotes/rootMidiNote + register anchor constants"
      contains: "rnbExtensionIntervals"
    - path: "Source/MidiGen/VoiceLeadingEngine.h"
      provides: "nearestOctaveNote register-aware voice-leading helper"
      contains: "nearestOctaveNote"
    - path: "Source/MidiGen/Humanization.h"
      provides: "deterministicJitter + per-style seed constants"
      contains: "deterministicJitter"
    - path: "Tests/MidiGenFixtures.h"
      provides: "Struct-literal fixture AnalysisResults (4-chord, NoChord, short-segment, ~150-segment scale)"
      min_lines: 60
    - path: "Tests/ChordToneMapperTests.cpp"
      provides: "Interval-table, clamp, voice-leading, jitter-determinism unit tests"
      min_lines: 60
  key_links:
    - from: "Source/MidiGen/ChordToneMapper.h"
      to: "Source/Analysis/AnalysisResult.h"
      via: "ChordQuality switch — the ONLY Analysis include allowed in MidiGen"
      pattern: "ChordQuality::"
    - from: "CMakeLists.txt"
      to: "Source/MidiGen/*.cpp"
      via: "CHORDAI_MIDIGEN_SOURCES list added to BOTH ChordAI and ChordAITests target_sources"
      pattern: "CHORDAI_MIDIGEN_SOURCES"
---

<objective>
Establish the entire Source/MidiGen/ module surface: beat-domain data model, frozen generator signatures, header-only music-math helpers (interval tables, voice leading, deterministic humanization), hand-built test fixture, and one-time CMake wiring for both targets — so Wave 2 plans (05-02 styles, 05-03 bass) run in parallel implementing bodies only, never touching CMakeLists.txt (Phase 3's 03-01 skeleton-freeze precedent).

Purpose: GEN-01's contract foundation — one AnalysisResult fans out into 5 MidiSetRows; everything downstream builds against these headers.
Output: 14 Source/MidiGen files (headers full, .cpps stubs), Tests/MidiGenFixtures.h, real ChordToneMapper tests, 4 registered test scaffolds, CMakeLists.txt frozen for the phase.
</objective>

<execution_context>
@/Users/test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-midi-conveyor-generation/05-RESEARCH.md
@Source/Analysis/AnalysisResult.h
@Source/Analysis/ChordTemplates.h
@Tests/SyntheticFixtures.h
@CMakeLists.txt

All commands run from /Users/test/Documents/prjcts/chordai. Builds can be slow — use Bash timeout 600000 ms.
Commits: plain conventional messages, NO co-author/attribution trailers.

<interfaces>
<!-- FROZEN inputs this plan consumes. Do not modify these files. -->

From Source/Analysis/AnalysisResult.h (FROZEN CONTRACT):
```cpp
enum class ChordQuality { Major, Minor, Dominant7, NoChord };
struct ChordSymbol  { int pitchClass = 0; ChordQuality quality = ChordQuality::NoChord; }; // 0=C..11=B
struct ChordSegment { ChordSymbol chord; double startSeconds = 0.0, endSeconds = 0.0;
                      int startBeatIndex = 0, endBeatIndex = 0; float confidence = 0.0f; };
struct KeyResult    { int tonicPitchClass = 0; bool isMajor = true; float confidence = 0.0f; };
struct AnalysisResult {
    double sampleRate; juce::Range<double> analyzedRegionSeconds;
    double bpm; std::vector<double> beatTimesSeconds; std::vector<int> barStartBeatIndices; // every 4th (4/4)
    KeyResult key; std::vector<ChordSegment> chords; bool wasCancelled;
};
```

From Source/Analysis/ChordTemplates.h — the interval convention to mirror exactly:
Major = {0,4,7}, Minor = {0,3,7}, Dominant7 = {0,4,7,10}.

From CMakeLists.txt — the pattern to mirror: `set(CHORDAI_ANALYSIS_SOURCES ...)` consumed by
`target_sources(ChordAI PRIVATE ... ${CHORDAI_ANALYSIS_SOURCES})` and the same in ChordAITests.

Test naming convention (required for ctest -R filters):
`TEST_CASE ("ChordToneMapperTests.CaseName", "[chordtonemapper]")` — file-name prefix inside the test name.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data model + generator skeleton files + one-time CMake wiring</name>
  <files>Source/MidiGen/NoteEvent.h, Source/MidiGen/MidiSetRow.h, Source/MidiGen/GenerationSettings.h, Source/MidiGen/ChordToneMapper.h, Source/MidiGen/VoiceLeadingEngine.h, Source/MidiGen/Humanization.h, Source/MidiGen/AsIsRowGenerator.h, Source/MidiGen/AsIsRowGenerator.cpp, Source/MidiGen/StyleVoicingGenerators.h, Source/MidiGen/StyleVoicingGenerators.cpp, Source/MidiGen/BassLineGenerator.h, Source/MidiGen/BassLineGenerator.cpp, Source/MidiGen/MidiRowBuilder.h, Source/MidiGen/MidiRowBuilder.cpp, Tests/StyleVoicingTests.cpp, Tests/BassLineGeneratorTests.cpp, Tests/MidiRowBuilderTests.cpp, Tests/PluginProcessorMidiGenTests.cpp, Tests/ChordToneMapperTests.cpp, CMakeLists.txt</files>
  <action>
    Create the full Source/MidiGen/ skeleton per 05-RESEARCH.md Pattern 1 (the three per-style voicing files consolidated into one StyleVoicingGenerators.h/.cpp per the research's own file-count note; AsIsRowGenerator stays separate — it is a reference row, not a style).

    **Full headers (final content, not stubs):**

    NoteEvent.h — pure C++, NO JUCE include at all, verbatim from 05-RESEARCH.md Pattern 1:
    `struct NoteEvent { double startBeats = 0.0; double lengthBeats = 0.0; int pitch = 60; float velocity = 0.8f; };`
    Doc comment: timing is in BEATS (1.0 == one detected beat == one quarter note at AnalysisResult.bpm); bar-aligned by construction since it inherits ChordSegment beat indices; Phase 6 tick conversion is `startBeats * ticksPerQuarterNote`.

    MidiSetRow.h — includes <JuceHeader.h> + "NoteEvent.h":
    `enum class RowStyle { DetectedAsIs, PopHipHopTrap, RnbNeoSoul, ElectronicHouse, Bass };`
    `struct MidiSetRow { juce::String id; juce::String label; RowStyle style = RowStyle::DetectedAsIs; std::vector<NoteEvent> notes; };`

    GenerationSettings.h — `struct GenerationSettings {};` with a doc comment: GEN-04's "style settings" clause has NO v1 UI surface (v1 styles are fixed, always generated); this empty struct exists so a future settings-changed callback calls the same generateAllRows(result, settings) without a shape change. Documented forward-compat plumbing, not speculative UI.

    ChordToneMapper.h, VoiceLeadingEngine.h, Humanization.h — create with final function SIGNATURES and doc comments now; bodies land in Task 2 (they are header-only, so "signature" and "body" arrive together in Task 2 — for Task 1 just create the files with the includes and a `// implemented in plan 05-01 Task 2` placeholder comment so the CMake test-scaffold build compiles).

    **Generator headers (frozen signatures — Wave 2 implements bodies only):**

    AsIsRowGenerator.h: `std::vector<NoteEvent> generateAsIsRow (const AnalysisResult& result);`
    StyleVoicingGenerators.h:
    ```cpp
    std::vector<NoteEvent> generatePopTrapRow          (const AnalysisResult&, const GenerationSettings&);
    std::vector<NoteEvent> generateRnbNeoSoulRow       (const AnalysisResult&, const GenerationSettings&);
    std::vector<NoteEvent> generateElectronicHouseRow  (const AnalysisResult&, const GenerationSettings&);
    ```
    BassLineGenerator.h:
    ```cpp
    enum class BassRhythm { TrapSustain, RnbRootFifth, HouseFourOnFloor };
    std::vector<NoteEvent> generateBassRow (const AnalysisResult&, BassRhythm rhythm);
    ```
    MidiRowBuilder.h: `std::vector<MidiSetRow> generateAllRows (const AnalysisResult& result, const GenerationSettings& settings = {});` — doc comment: pure function, same input always produces byte-identical output; empty/all-NoChord chords produce 5 rows with empty notes vectors, never a crash.

    All four .cpp files: stub bodies returning `{}` with a `// body: plan 05-0N` comment. Headers include only "../Analysis/AnalysisResult.h" from Analysis (folder-boundary rule: MidiGen never includes analyzer internals).

    **Test scaffolds:** Tests/StyleVoicingTests.cpp, Tests/BassLineGeneratorTests.cpp, Tests/MidiRowBuilderTests.cpp, Tests/PluginProcessorMidiGenTests.cpp, Tests/ChordToneMapperTests.cpp — each just `#include <catch2/catch_test_macros.hpp>` + the headers it will test + a comment naming the plan that fills it. Zero TEST_CASEs yet (a TU with no tests compiles and links fine under Catch2WithMain).

    **CMake (the ONLY CMakeLists.txt edit this phase until plan 05-05):**
    - `set(CHORDAI_MIDIGEN_SOURCES Source/MidiGen/AsIsRowGenerator.cpp Source/MidiGen/StyleVoicingGenerators.cpp Source/MidiGen/BassLineGenerator.cpp Source/MidiGen/MidiRowBuilder.cpp)` next to CHORDAI_ANALYSIS_SOURCES.
    - Append `${CHORDAI_MIDIGEN_SOURCES}` to target_sources of BOTH ChordAI and ChordAITests.
    - Add the 5 new Tests/*.cpp files to target_sources(ChordAITests ...).

    Build both targets + run the FULL existing suite (must stay 72/72 — this task adds no behavior).
    Commit: `feat(05-01): MidiGen module skeleton, frozen generator contracts, CMake wiring`
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <done>All 14 Source/MidiGen files + 5 test files exist and compile into both targets; full suite still green; grep confirms no GUI include (`grep -rn "juce_gui\|Component" Source/MidiGen/` empty) and no Analysis include beyond AnalysisResult.h.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Header-only music-math helpers — ChordToneMapper, VoiceLeadingEngine, Humanization</name>
  <files>Source/MidiGen/ChordToneMapper.h, Source/MidiGen/VoiceLeadingEngine.h, Source/MidiGen/Humanization.h, Tests/ChordToneMapperTests.cpp</files>
  <behavior>
    - ChordToneMapperTests.TriadIntervalsMatchDetectorConvention: Major {0,4,7}, Minor {0,3,7}, Dominant7 {0,4,7,10} — identical to ChordTemplates.h
    - ChordToneMapperTests.RnbExtensionsMatchQualityTable: Major → maj9 {0,4,7,11,14}; Minor → min11 {0,3,7,10,14,17}; Dominant7 → dom9 {0,4,7,10,14}
    - ChordToneMapperTests.IntervalsToMidiNotesClampsToValidRange: root 125 + {0,4,7} → all notes ≤ 127; root -5 → all ≥ 0
    - ChordToneMapperTests.RootMidiNoteAnchors: rootMidiNote(9, 48) == 57 (A2... A in C3 octave band); rootMidiNote(0, 60) == 60; rootMidiNote(11, 72) == 83
    - ChordToneMapperTests.NearestOctaveNotePicksClosestOctave: nearestOctaveNote(0 /*C*/, 62 /*prev D4*/) == 60; nearestOctaveNote(11 /*B*/, 60) == 59 (down, not 71); result always within [48, 72] with default clamps
    - ChordToneMapperTests.DeterministicJitterIsPureAndBounded: same (noteIndex, seed, range) twice → exactly equal; |jitter| ≤ range/2 for noteIndex 0..999; different seeds → different sequences (at least one differing value in first 16 indices)
  </behavior>
  <action>
    Write the TEST_CASEs above in Tests/ChordToneMapperTests.cpp FIRST (they fail to compile/link against the Task 1 placeholders — that is the RED state). Then implement:

    ChordToneMapper.h — lift `triadIntervals`, `rnbExtensionIntervals`, `intervalsToMidiNotes` verbatim from 05-RESEARCH.md Pattern 2 (including the "why Minor gets the 11th" avoid-note comment), plus `inline int rootMidiNote (int pitchClass, int anchorOctaveBase)` from the Code Examples section (juce::jlimit clamp 0-127). Add the register-anchor constants (the contracts Wave 2 codes against):
    ```cpp
    inline constexpr int kAnchorAsIs = 60, kAnchorPopTrap = 48, kAnchorRnbSeed = 60,
                         kAnchorHouse = 72, kAnchorBass = 36;
    inline constexpr int kRnbRegisterLow = 48, kRnbRegisterHigh = 72;
    constexpr double kHouseStabOffsetsBeats[4] = { 0.5, 1.5, 2.5, 3.5 };
    constexpr double kHouseStabLengthBeats = 0.25;
    ```
    NoChord handling: triadIntervals/rnbExtensionIntervals must return `{}` for NoChord (add the case explicitly — do NOT fall through to Major; Pitfall 1).

    VoiceLeadingEngine.h — `nearestOctaveNote` verbatim from 05-RESEARCH.md Code Examples (candidate scan by octaves, jlimit clamp to [registerLow=48, registerHigh=72]).

    Humanization.h — `deterministicJitter (uint32_t noteIndex, uint32_t styleSeed, float range)` verbatim from Pattern 5 (Knuth multiplicative hash; NO juce::Random, no state). Add per-style seed constants used by Wave 2/3 generators plus base-velocity/range constants from the Pattern 5 table:
    ```cpp
    inline constexpr uint32_t kSeedPopTrap = 0x50505050u, kSeedRnb = 0x52424242u,
                              kSeedHouse = 0x48534531u, kSeedBass = 0x42415353u;
    inline constexpr float kVelAsIs = 0.75f,               // no jitter
                           kVelPopTrap = 0.78f, kJitterPopTrap = 0.12f,   // ±0.06
                           kVelRnb = 0.62f,     kJitterRnb = 0.08f,       // ±0.04
                           kVelHouse = 0.88f,   kJitterHouse = 0.10f,     // ±0.05
                           kVelBass = 0.85f,    kJitterBass = 0.06f;      // ±0.03
    ```
    (range param = full width; jitter returns [-range/2, +range/2]. These numbers are research-designed defaults, adjustable after Phase 6 audition — say so in a comment.)

    Build + run the new tests GREEN.
    Commit: `feat(05-01): chord-tone tables, voice-leading and deterministic-jitter helpers with tests`
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build -R "ChordAITests.ChordToneMapperTests" --output-on-failure</automated>
  </verify>
  <done>All 6 TEST_CASEs green; interval tables mirror ChordTemplates.h; jitter is provably deterministic and bounded; `grep -rn "juce::Random\|std::mt19937\|rand(" Source/MidiGen/` returns nothing.</done>
</task>

<task type="auto">
  <name>Task 3: Hand-built fixture AnalysisResults for all downstream tests</name>
  <files>Tests/MidiGenFixtures.h, Tests/MidiRowBuilderTests.cpp</files>
  <action>
    Create Tests/MidiGenFixtures.h in namespace `midigen_fixtures` — struct-literal AnalysisResults, NO audio rendering (unlike SyntheticFixtures.h; these feed pure generator functions). Every fixture must be internally consistent: beatTimesSeconds spaced 60/bpm apart starting at 0, barStartBeatIndices every 4th index, each ChordSegment's startSeconds/endSeconds derived from its beat indices (`index * 60.0 / bpm`), sampleRate 44100, analyzedRegionSeconds {0, lastBeatSeconds}.

    Four factory functions:
    1. `makeFourChordFixture()` — 120 BPM, 16 beats (17 beatTimes entries 0.0..8.0 at 0.5s), bars {0,4,8,12}, key A minor. Chords, 4 beats each: Am (pc 9, Minor, beats 0-4), F (pc 5, Major, 4-8), C (pc 0, Major, 8-12), G7 (pc 7, Dominant7, 12-16). Covers all 3 pitched qualities.
    2. `makeNoChordFixture()` — same grid; chords: C Major (0-4), NoChord (4-8), Am (8-12). Pitfall-1 negative case.
    3. `makeShortSegmentFixture()` — 120 BPM; chords: C Major beats 0-2 (2-beat), F Major beats 2-5 (3-beat), G Major beats 5-9 (4-beat). Exercises Pop/Trap re-strike remainder, R&B bass short-segment rule, House span truncation.
    4. `makeRealTrackScaleFixture()` — 150 segments, 4 beats each (600 beats, ~170 BPM to mirror TOCK-scale), cycling pitch classes 0..11 and qualities Major/Minor/Dominant7 (every 10th segment NoChord). For the <1ms performance budget test.

    In Tests/MidiRowBuilderTests.cpp add one sanity TEST_CASE now (the rest of that file lands in plan 05-04):
    `TEST_CASE ("MidiRowBuilderTests.FixtureSelfConsistency", "[midirowbuilder]")` — for each fixture: beatTimesSeconds strictly increasing, every chord's endBeatIndex > startBeatIndex and ≤ last beat index, seconds fields match `index * 60/bpm` within 1e-9, barStartBeatIndices multiples of 4.

    Build + run. Commit: `test(05-01): struct-literal MidiGen fixture AnalysisResults with self-consistency guard`
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build -R "ChordAITests.MidiRowBuilderTests.FixtureSelfConsistency" --output-on-failure</automated>
  </verify>
  <done>Four fixtures exist, self-consistency test green; downstream plans have deterministic known-answer inputs without rendering audio.</done>
</task>

</tasks>

<verification>
- `cmake --build build && ctest --test-dir build --output-on-failure` — full suite green (72 pre-existing + new ChordToneMapper/fixture tests)
- `grep -rn "juce::Random" Source/MidiGen/` — empty (determinism rule)
- `grep -rln "Analysis/" Source/MidiGen/ | xargs grep -l "AnalysisResult.h"` — AnalysisResult.h is the only Analysis include
- `grep -n "CHORDAI_MIDIGEN_SOURCES" CMakeLists.txt` — list defined and consumed by both targets
</verification>

<success_criteria>
- Wave 2 plans (05-02, 05-03) can implement generator bodies in parallel with zero CMakeLists.txt edits and zero header changes
- Beat-domain NoteEvent + 5-row MidiSetRow model frozen; interval convention provably identical to the detector's
- Deterministic-jitter helper tested pure and bounded; fixture AnalysisResults tested self-consistent
</success_criteria>

<output>
After completion, create `.planning/phases/05-midi-conveyor-generation/05-01-SUMMARY.md`
</output>
